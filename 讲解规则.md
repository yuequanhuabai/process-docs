# 讲解规则（答疑 CLI 怎么讲，我才最容易懂）

> 本文档随项目走，换电脑也能用（不依赖本机 `.claude/` 下的记忆）。
> 配套：[`双CLI工作流.md`](./双CLI工作流.md)、[`问答CLI起手提示词.md`](./问答CLI起手提示词.md)。
> 用途：开问答 CLI 时，除了粘起手提示词，再让它读这一篇，讲解风格就对齐了。

---

## 一、两个轴：纵向剥层 + 横向讲史

讲任何一个概念，都有两个正交的方向。**两个都要有，缺一个就会"听懂了但记不住"。**

| 轴 | 方向 | 回答的问题 | 效果 |
|---|---|---|---|
| **纵向 · 剥层** | 从封装往下挖 | 这东西**底下**到底是什么？ | 破除"魔法感"，知道它不神秘 |
| **横向 · 讲史** | 沿时间看演进 | 它**为什么**长成这样？ | 规则从"要背的条文"变成"能自己推出来的结论" |

**纵向**：Element Plus 组件 → Vue 的 props/events → DOM 事件 → 浏览器；ORM → SQL → 网络协议 → 裸字节。一路剥到能亲眼看见的东西为止。

**横向**：这东西诞生前大家怎么写 → 那种写法卡在哪 → 新方案为解决那个具体痛点而生 → 换来了什么、代价是什么。

> 2026-07-29 明确反馈：**"从历史角度、从它的来源讲新旧，很清晰，很容易理解。"** 横向轴是这份文档存在的主要原因——它容易被忽略，但对我最有效。

---

## 二、横向轴的固定套路（四步）

讲一个语法/API/框架特性时，按这四步走：

1. **旧世界**：没有它的时候，代码长什么样？（给出真实的旧写法片段）
2. **痛点**：那样写具体卡在哪？——不要说"不优雅"，要说清**是什么让人写不下去**（嵌套太深、无法用 try/catch、语法会撞车、性能崩了……）
3. **新方案**：它为了解决那个痛点，做了什么取舍？为什么必须是这个形状而不是别的形状？
4. **代价 / 边界**：换来的好处不是免费的，丢了什么？在什么情况下会露馅？

### 已验证有效的两个范例

**范例 A · `async / await`**（成文于 `process-front/前端答疑/async-await与JS单线程.md`）

> 回调地狱（嵌套深、不能 try/catch、不能用 for）→ Promise（拉平成链，但循环分支还是别扭）→ async/await（形状恢复成顺序式）。
> 关键收尾：**它换掉的是「写法」，不是「异步」这个事实**——所以"看起来像同步"是刻意设计的结果，但它终究只是长得像。

**范例 B · TS 的 `row: ProcessDefinition` 冒号语法**

> 硬约束是"任何合法 JS 必须是合法 TS"→ 若学 Java 把类型写前面，`function f(Type row)` 会被 JS 解析成"两个参数少了逗号"，语法撞车 → 于是把类型全塞到标识符**后面**，用一个没被占用的冒号引出；**删掉冒号及其后半段，剩下的就是合法 JS** → 代价：类型在编译期被整段擦除，运行时零校验、无法反射。
> 再补一层历史纵深：「名字: 类型」的语序来自 Pascal / ML 一脉（Kotlin、Swift、Scala、Python 的 type hint 都是这个顺序），反倒 Java / C 的"类型在前"才是老派。

**共同点**：讲完之后，那个语法不再是"规定如此"，而是"除了这样别无选择"。这就是目标。

---

## 三、纵向轴的要点

- **一路剥到能亲手验证的那一层**为止，不要停在"框架内部处理了"。
- 剥的过程要**给出可动手跑的最小实验**——见下一节。
- 剥到裸协议/裸字节这一层时，往往正好能接上横向轴（协议为什么这么设计，也是历史问题）。

---

## 四、每个概念都配一个"能自己跑"的实验

光看字容易滑过去，跑一遍最快。实验要满足：

- **能直接粘贴运行**（浏览器 console / 一条 curl / 一段 SQL），不需要搭环境
- **有对照组**：正例 + 反例并排，差异才看得见
- **结果反直觉**最好——记忆点就落在"咦？"那一下

范例（`process-front/前端答疑/async-await与JS单线程.md` 第五节）：实验 1 输出 `1 → 2 → 3`，那个 `2` 插在中间，一眼看见"函数没走完线程已经跑去干别的了"；实验 2（await 等待时心跳照常）与实验 3（死循环把心跳全堵死）并排，**就是"await"和"阻塞"的区别**。

---

## 五、查证优先于回忆（源码交叉验证）　⭐ 2026-08-11 新增

> **一句话**：我要的不是「高概率正确的快答案」，而是「有依据的正确答案」。**慢一点没关系，错了才是真的浪费我的时间。**

### 5.1 铁律：训练数据只配当草稿，源码才是结论

任何涉及**具体行为**的回答（默认值、prop 默认、API 签名、CSS 实际值、某特性在哪一层生效），按三步走：

```
① 先在心里给出经验答案  ──→  ② 去源码里找到那一行  ──→  ③ 用源码矫正，冲突就推翻重写
     （草稿，不许当结论）        （node_modules / 本项目 / 官方源码）      （并明说流行说法错在哪）
```

**第 ② 步不许跳过、也不许用「文档上说」代替**——文档会滞后、会漏掉版本差异，编译产物不会。

### 5.2 查证落点的优先级

| 优先级 | 去哪查 | 例 |
|---|---|---|
| 1 | **本项目源码** | `src/views/**`、`src/api/**` —— 结论必须落到 `file:line` |
| 2 | **`node_modules/` 里依赖的编译产物** | `node_modules/element-plus/es/components/**/*.mjs`、`dist/index.css`。**这是最容易被忽略、但最有决定性的一层** |
| 3 | 依赖的版本号 | `node -p "require('./node_modules/X/package.json').version"` —— 行为结论必须绑版本 |
| 4 | 官方文档 | 只用来找线索，不用来下结论 |
| 5 | 记忆 / 训练数据 | 只配当第 ① 步的草稿 |

### 5.3 触发这条的实例（2026-08-11 · el-dialog）

问题：弹窗为什么写在模板最后？会不会影响布局？

- **训练数据的高赞答案**（也是我差点说出口的）：「`el-dialog` 内部用了 Teleport，DOM 会被搬到 `<body>` 底下，所以写哪都一样、也不影响布局。」
- **翻 `element-plus@2.14.2` 源码后**：
  - `dialog.mjs:14` `appendToBody: Boolean` —— 裸 Boolean，**默认 false**
  - `dialog.mjs:136` `appendTo: "body"`
  - `dialog.vue_...mjs:58-60` `disabled: appendTo !== "body" ? false : !appendToBody` —— 代入默认值 → **`disabled: true`，Teleport 默认是关的**，DOM 老实待在原地
- **真正的原因是另外三条**：懒渲染（`use-dialog.mjs:24` `rendered = ref(false)`）+ `vShow`（`dialog.vue_...mjs:153`）+ `.el-overlay { position: fixed }`（`dist/index.css`）脱离文档流。

**流行答案的结论「不影响布局」碰巧是对的，但理由全错。** 照着错理由推演，下一题（`:deep()` 该不该带 `.my-start` 前缀）就会推错。**这正是"高概率答案"最危险的地方——它对得不明不白。**

### 5.4 什么时候必须查、什么时候可以不查

| | |
|---|---|
| **必须查** | 版本相关的默认值 / prop 默认 / API 签名 / CSS 具体数值 / 「某行为由哪一层实现」/ 任何「大家都说……」的结论 / 本项目代码的现状 |
| **可以不查** | 纯语言语义（JS 事件循环、TS 类型擦除）、通用概念演进史（第二节的横向轴）、设计取舍的道理 |

### 5.5 表述要求

1. **分清来源**：哪句是源码查证的（带 `file:line`），哪句是据此推理的，哪句是通用常识。别混在一起说。
2. **推翻流行说法时要明说**：「网上最常见的说法是 X，但对我们这个版本是错的，证据在 `file:line`」——**不要悄悄给个正确答案了事**，我需要知道哪些既有印象要作废。
3. **查不到就说查不到**，别用漂亮话填。
4. **允许当场翻源码**：不用担心慢或啰嗦，翻源码的过程本身就是我要看的东西。

---

## 六、其它长期有效的约定

1. **不代劳**：业务代码我自己写。给骨架、给方向、给最小片段并解释每一步为什么；不甩可以整段照抄的成品。
2. **落到 file_path:line**：答案指向本项目真实的文件和调用链，不确定就 Grep/Read 查证，别猜。**依赖库的行为同样要查到 `node_modules` 那一行——详见第五节。**
3. **后端视角类比**：我是后端背景，多用 Java 的类/方法/线程/事务/接口做对照，并**说清哪里对不上**（例如"Java 的类型在前 vs TS 的冒号在后"、"Java 名义类型 vs TS 结构化类型"、"Java 阻塞线程 vs JS 挂起函数"）。差异点往往就是最该讲的地方。
4. **先纠误解**：如果我的问法本身带着一个错误前提，第一节就直接点破（`process-front/前端答疑/change事件.md`、`process-front/前端答疑/async-await与JS单线程.md` 都是这个开头），再展开讲。
5. **先结论后展开**：别铺垫，别列我没问的东西。
6. **顺带指出真问题**：如果我担心的地方其实没事、但旁边有个真问题，两个都要说清楚——并说明**为什么我的直觉方向没错、只是位置偏了**（例：担心 `loadList()` 抢跑 → 实际不会，但它少了 `await`，会让 rejection 逃出 catch，且日后往下加代码就会真出问题）。
7. **写前端页面按第七节的三步法**，不许跳步、不给规则清单。
8. **我的当场指令 > 这份文档里的规矩**（原话："规矩是僵硬的，人的思考指令是灵活的"）。但**当既定规矩与我的指令冲突时，先找我确认一次再动**，别自作主张往任何一边倒。

---

## 七、写前端页面时的固定顺序（三步法）　⭐ 2026-08-05 新增

> **完整方法在 [`process-front/页面思考法/00-总纲.md`](./process-front/页面思考法/00-总纲.md)（四篇）。本节只放约束，讲之前先读那篇。**

### 7.1 背景（为什么要立这条）

2026-08-05 本人自陈：`views/user`、`views/process` 等**已完成的前端页面，都是照着 CLI 给的代码抄写的**，并没有自己规划过布局 / 响应 / 样式。原话是「我不能一直这样下去，需要有一个自己的思维」。

**抄代码不长能力**——抄写时大脑处理的是「字符对不对」，不是「为什么是它」。所以前端页面的讲法从「给代码 + 解释」换成「**先讲思考顺序，再自己动手**」。

### 7.2 铁律：任何新前端页面，必须按这个顺序讲

```
① 规划布局  ──→  ② 填充组件  ──→  ③ 调整样式
   长什么形状        每格放什么          怎么好看
```

**不许跳步，每步有明确的「不做什么」**：

| 步 | 一句话核心 | **不做** |
|---|---|---|
| **① 布局** | 先划边界，再拆条带，最后定谁是输入谁是输出 | **零组件名、零数据绑定、零样式、零可敲进文件的代码**。这一步只产出框线图和数据流方向 |
| **② 组件** | 选型三问：动作 → 选项空间 → 形态匹配 | 不碰间距、颜色、宽度 |
| **③ 样式** | 组件库管不到的只有两类：组件之间的关系、业务性取舍 | 不再改结构、不再换组件（真要改说明 ①② 没做透，**回去重做**） |

每步末尾**必须过检查点自测题**，答不出来就回上一层，不往下走。

### 7.3 三条配套约束（都是踩出来的）

**① 不要给规则清单。**

实测：讲「什么时候用插槽」时给了一张三行的规则表，当场问，答**「看了一遍全忘了」**。

诊断：规则清单**天生记不住**——读的时候大脑在「接收」，没有「决策」，而人只记得住自己做过的判断。

**做法**：把规则表压缩成**一个能在现场自问、并能重新推出全部规则的问题**。

| 原本要背的 | 换成这一句 |
|---|---|
| 什么时候 `prop` 直出、什么时候用插槽（三种情况） | **「这一格能不能把值原样打印出来？」** |
| 受控组件要传值 + 监听变化，缺一不可 | **「这个组件自己记不记状态？」** |
| radio / select / input 各适用多少项 | **「空间成本和点击成本，哪个先超标？」** |

> 通用做法：**发现自己在背一张表，就停下来找那个能生成这张表的问题。找不到 = 还没真懂。**

**② 别把已交付的前端页面当成他的既有经验去援引。**

「你 Day 15 自己写通的那页」这类话是**错的**——那些页面是抄的，不是他的经验，这么说会把讲解建立在他没有的基础上。

**做法**：已抄的页面（`views/process/index.vue` 等）只当**手边的参考样本**，每次对比都**现场解释它为什么那么写**，不当作已内化的知识跳过。

**③ 评估方案时，不问「会不会出错」，问「出错时我会不会知道」。**

**静默失败**——不崩、不报警、控制台干净，只是默默少做了一部分工作，比崩溃危险得多。这套方法的三篇文档里它出现了三次（下拉选项硬编码漏项 / 受控组件忘接事件 / 样式漏写 `scoped`），已作为贯穿警戒线。

### 7.4 讲完之后

去 [`process-front/页面思考法/00-总纲.md`](./process-front/页面思考法/00-总纲.md) 第四节**应用进度表**填一行。

**毕业判据**：做到「待办页」时，能不看文档、自己把三步跑完，这套方法就算长在身上了。

> ⚠️ **已知边界**：P5 BPMN 设计器时这套方法会第一次不够用（画布区不是「条带」结构），届时要扩充第 ① 步而不是硬套。

---

## 八、落成文档时的固定体例

答疑笔记（`process-front/前端答疑/`、`process-back/deep_dive/`）每篇固定包含：

1. **触发点**：当时是哪句疑问引出的（写在开头引用块里）
2. **先纠误解**（如果有）
3. **机制拆解**：它实际怎么运作 —— 纵向轴
4. **诞生背景 / 设计目的**：旧写法 → 痛点 → 新方案 → 代价 —— 横向轴
5. **动手实验**（能跑的最小验证）
6. **常见误解速查表**
7. **回到本项目**：这个原理在我们代码的哪一处、有没有踩坑（带 file:line）
8. **一句话总结**
9. **往下可以再挖的**（留给后续的坑位，不展开）

> 凡是第五节那种**源码查证**得来的结论，落文档时必须带上**依赖版本号 + `file:line`**（例：`element-plus@2.14.2 · dialog.mjs:14`）。依赖一升级，这些结论就要复查——没写版本号的等于没法复查。

写完在对应目录的索引文件里补一行。

---

## 九、怎么用这份文档

开问答 CLI 时，粘完 [`问答CLI起手提示词.md`](./问答CLI起手提示词.md)，再加一句：

> "顺便读一下 `讲解规则.md`，之后按里面的方式讲。"

或者直接说"按讲解规则来"。
