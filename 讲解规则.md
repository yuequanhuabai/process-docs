# 讲解规则（答疑 CLI 怎么讲，我才最容易懂）

> 本文档随项目走，换电脑也能用（不依赖本机 `.claude/` 下的记忆）。
> 配套：[`双CLI工作流.md`](./双CLI工作流.md)、[`问答CLI起手提示词.md`](./问答CLI起手提示词.md)。
> 用途：开问答 CLI 时，除了粘起手提示词，再让它读这一篇，讲解风格就对齐了。

---

## 一、两个轴：纵向剥层 + 横向讲史

讲任何一个概念，都有两个正交的方向。**两个都要有，缺一个就会"听懂了但记不住"。**

| 轴 | 方向 | 回答的问题 | 效果 |
|---|---|---|---|
| **纵向 · 剥层** | 从封装往下挖 | 这东西**底下**到底是什么？ | 破除"魔法感"，知道它不神秘 |
| **横向 · 讲史** | 沿时间看演进 | 它**为什么**长成这样？ | 规则从"要背的条文"变成"能自己推出来的结论" |

**纵向**：Element Plus 组件 → Vue 的 props/events → DOM 事件 → 浏览器；ORM → SQL → 网络协议 → 裸字节。一路剥到能亲眼看见的东西为止。

**横向**：这东西诞生前大家怎么写 → 那种写法卡在哪 → 新方案为解决那个具体痛点而生 → 换来了什么、代价是什么。

> 2026-07-29 明确反馈：**"从历史角度、从它的来源讲新旧，很清晰，很容易理解。"** 横向轴是这份文档存在的主要原因——它容易被忽略，但对我最有效。

---

## 二、横向轴的固定套路（四步）

讲一个语法/API/框架特性时，按这四步走：

1. **旧世界**：没有它的时候，代码长什么样？（给出真实的旧写法片段）
2. **痛点**：那样写具体卡在哪？——不要说"不优雅"，要说清**是什么让人写不下去**（嵌套太深、无法用 try/catch、语法会撞车、性能崩了……）
3. **新方案**：它为了解决那个痛点，做了什么取舍？为什么必须是这个形状而不是别的形状？
4. **代价 / 边界**：换来的好处不是免费的，丢了什么？在什么情况下会露馅？

### 已验证有效的两个范例

**范例 A · `async / await`**（成文于 `process-front/前端答疑/async-await与JS单线程.md`）

> 回调地狱（嵌套深、不能 try/catch、不能用 for）→ Promise（拉平成链，但循环分支还是别扭）→ async/await（形状恢复成顺序式）。
> 关键收尾：**它换掉的是「写法」，不是「异步」这个事实**——所以"看起来像同步"是刻意设计的结果，但它终究只是长得像。

**范例 B · TS 的 `row: ProcessDefinition` 冒号语法**

> 硬约束是"任何合法 JS 必须是合法 TS"→ 若学 Java 把类型写前面，`function f(Type row)` 会被 JS 解析成"两个参数少了逗号"，语法撞车 → 于是把类型全塞到标识符**后面**，用一个没被占用的冒号引出；**删掉冒号及其后半段，剩下的就是合法 JS** → 代价：类型在编译期被整段擦除，运行时零校验、无法反射。
> 再补一层历史纵深：「名字: 类型」的语序来自 Pascal / ML 一脉（Kotlin、Swift、Scala、Python 的 type hint 都是这个顺序），反倒 Java / C 的"类型在前"才是老派。

**共同点**：讲完之后，那个语法不再是"规定如此"，而是"除了这样别无选择"。这就是目标。

---

## 三、纵向轴的要点

- **一路剥到能亲手验证的那一层**为止，不要停在"框架内部处理了"。
- 剥的过程要**给出可动手跑的最小实验**——见下一节。
- 剥到裸协议/裸字节这一层时，往往正好能接上横向轴（协议为什么这么设计，也是历史问题）。

---

## 四、每个概念都配一个"能自己跑"的实验

光看字容易滑过去，跑一遍最快。实验要满足：

- **能直接粘贴运行**（浏览器 console / 一条 curl / 一段 SQL），不需要搭环境
- **有对照组**：正例 + 反例并排，差异才看得见
- **结果反直觉**最好——记忆点就落在"咦？"那一下

范例（`process-front/前端答疑/async-await与JS单线程.md` 第五节）：实验 1 输出 `1 → 2 → 3`，那个 `2` 插在中间，一眼看见"函数没走完线程已经跑去干别的了"；实验 2（await 等待时心跳照常）与实验 3（死循环把心跳全堵死）并排，**就是"await"和"阻塞"的区别**。

---

## 五、其它长期有效的约定

1. **不代劳**：业务代码我自己写。给骨架、给方向、给最小片段并解释每一步为什么；不甩可以整段照抄的成品。
2. **落到 file_path:line**：答案指向本项目真实的文件和调用链，不确定就 Grep/Read 查证，别猜。
3. **后端视角类比**：我是后端背景，多用 Java 的类/方法/线程/事务/接口做对照，并**说清哪里对不上**（例如"Java 的类型在前 vs TS 的冒号在后"、"Java 名义类型 vs TS 结构化类型"、"Java 阻塞线程 vs JS 挂起函数"）。差异点往往就是最该讲的地方。
4. **先纠误解**：如果我的问法本身带着一个错误前提，第一节就直接点破（`process-front/前端答疑/change事件.md`、`process-front/前端答疑/async-await与JS单线程.md` 都是这个开头），再展开讲。
5. **先结论后展开**：别铺垫，别列我没问的东西。
6. **顺带指出真问题**：如果我担心的地方其实没事、但旁边有个真问题，两个都要说清楚——并说明**为什么我的直觉方向没错、只是位置偏了**（例：担心 `loadList()` 抢跑 → 实际不会，但它少了 `await`，会让 rejection 逃出 catch，且日后往下加代码就会真出问题）。

---

## 六、落成文档时的固定体例

答疑笔记（`process-front/前端答疑/`、`process-back/deep_dive/`）每篇固定包含：

1. **触发点**：当时是哪句疑问引出的（写在开头引用块里）
2. **先纠误解**（如果有）
3. **机制拆解**：它实际怎么运作 —— 纵向轴
4. **诞生背景 / 设计目的**：旧写法 → 痛点 → 新方案 → 代价 —— 横向轴
5. **动手实验**（能跑的最小验证）
6. **常见误解速查表**
7. **回到本项目**：这个原理在我们代码的哪一处、有没有踩坑（带 file:line）
8. **一句话总结**
9. **往下可以再挖的**（留给后续的坑位，不展开）

写完在对应目录的索引文件里补一行。

---

## 七、怎么用这份文档

开问答 CLI 时，粘完 [`问答CLI起手提示词.md`](./问答CLI起手提示词.md)，再加一句：

> "顺便读一下 `讲解规则.md`，之后按里面的方式讲。"

或者直接说"按讲解规则来"。
