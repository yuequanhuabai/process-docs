# 测试进度断点（下次从这里继续）

> 最后更新：**2026-07-30** ｜ 主线位置：**P4 · Day 16 实例 + 我的发起（下一站，未开工）**
>
> 📌 **本文档即进度依据**：会话开场按此文档定位，**不必重新扫代码核对**；代码一改完就当场回写这里。
> 本文档只装「现在在哪 / 下一步做什么 / 现场什么样」三件事，**篇幅刻意压住不让它增长**——历史流水账去 [`变更日志.md`](./变更日志.md)，知识沉淀去 `process-back/deep_dive/` 与 `process-front/前端答疑/`。

---

## 〇、文档地图（开场只读带 ★ 的）

| 文档 | 作用 | 开场读 |
|---|---|---|
| **本文档** | 当前位置 / 下一步 / 数据库现场 | ★ |
| [`plan/P4-业务前端批量.md`](./plan/P4-业务前端批量.md) | 当前阶段的每日任务与打卡 | ★ 只读当前阶段这一篇 |
| [`05-重写排期计划.md`](./05-重写排期计划.md) | 总控：阶段总览 + 技术栈 + ⚠️ 挂起项总表 | 需要看全局时 |
| [`plan/P0`–`P8`](./plan/) 其余八篇 | 其他阶段明细 | 不读 |
| [`变更日志.md`](./变更日志.md) | 历次产出流水账 + 已消化归档 | 不读，只追加 |
| `process-back/deep_dive/`、`process-back/process_*/`、`process-front/前端答疑/` | 学习材料与落地手册 | 不读，按需查 |

> **双 CLI 协作**：一个 CLI 改代码（主），一个只读答疑（问答）。新开任一 CLI 先读本仓库根目录三篇——`双CLI工作流.md`（分工总纲）、`问答CLI起手提示词.md`（开问答 CLI 时整体粘贴）、`讲解规则.md`（纵向剥层 + 横向讲史 + 动手实验）。答疑笔记落在 `process-front/前端答疑/`（索引 `process-front/前端答疑/索引.md`）与 `process-back/deep_dive/`。

---

## 一、当前位置

**P0–P3 全部关闭**（Day 1–14）。后端引擎四门面（Repository / Runtime / Task / History）全就位。

**P4 · Day 15 流程管理页 ✅ 已关闭**（2026-07-30 浏览器实测）：查询 / 挂起 / 激活三项 OK；删除带在途实例的部署被友好拦下 —— **这是预期行为不是失败**，`cascade=false` 遇在途实例走 catch 分支（底层 `PROC_DEF_ID_` 外键冲突），与 Day 12 后端实测同一条路径。前端第一个业务页跑通。

**下一站：Day 16 · 实例 + 我的发起（未开工）**，详见下方「三」。

**两笔顺延的小尾巴**（Day 16 开工前顺手做掉，都在 `views/process/index.vue`）：
- 并发竞态：`<el-switch>` 加 `:loading="loading"`（`:5-9`）、`<el-pagination>` 加 `:disabled="loading"`（`:39-46`）。治的是快速连点并发、响应乱序覆盖 `list`，`loading` ref 现成，零额外代码。见 `process-front/前端答疑/原生控件的外衣与el-switch.md` 第八节。
- 命名：`api/process.ts:29` 形参名写作 `definitionId` 实为 deploymentId，不影响运行。

**学习支线**：`process-back/deep_dive/hi_tables/` 只剩 `05-外围四表.md` 收官，进度表见 `process-back/deep_dive/待深入主题清单.md` 第 3 条。

---

## 二、当前数据库现场（flowable_db2）

> 🔄 **2026-07-30 已整库重置**：执行 `resources/sql/00_reset_db.sql` 清空 → 重启建表 → 跑 `sys_user.sql`。
> **旧的 test-0004~0007 四条实例样本全部作废**，历史表清空，实例数据从零开始。

**流程定义**：四个模板，随启动自动部署（见下方开关说明），全部 v1、激活状态。

| 模板文件 | key | 名称 | 结构（三级） |
|---|---|---|---|
| `leave.bpmn20.xml` | `leaveProcess` | 请假流程 | 发起 → 经理审批[manager] → 结束　*（两级，原有）* |
| `reimburse.bpmn20.xml` | `reimburseProcess` | 报销流程 | 发起 → 经理审批[manager] → **财务审批[admin]** → 结束 |
| `purchase.bpmn20.xml` | `purchaseProcess` | 采购流程 | 发起 → 经理审批[manager] → **总监审批[admin]** → 结束 |
| `travel.bpmn20.xml` | `travelProcess` | 出差流程 | 发起 → 经理审批[manager] → **总监审批[admin]** → 结束 |

三个新模板的节点 id 统一为 `startEvent` / `managerApprove` / `adminApprove` / `endEvent`，连线 `flow1`–`flow3`，均带手写 DI 坐标（P5 的 bpmn-js 渲染要用）。

> ⚠️ **`check-process-definitions` 目前是 `true`**（`application.yml:33`）—— 启动时会扫 `processes/` 自动部署四个模板。**确认四个都入库后记得改回 `false`**，否则每次启动都重复扫描。
> ⚠️ 后端服务需**重新启动**才会部署：`process-back` 下 `./mvnw spring-boot:run`，`http://localhost:9090/workflow/doc.html` 能开即可。

**实例样本：暂无**（清库后还没发起过）。P4 后面三个页面（我的发起 / 待办 / 已审批）需要样本时，用 employee 账号各发几条，并让其中一条停在 `managerApprove`、一条停在 `adminApprove`、一条走完、一条终止，就能覆盖全部状态列。

账号（`sys_user.sql` 已重新灌入）：

| 用户 | id | role | 在流程里的角色 |
|---|---|---|---|
| employee | 3 | `employee` | 发起人 |
| manager | 2 | `manager` | 一级审批（`candidateGroups="manager"`） |
| admin | 1 | `admin` | 二级审批（`candidateGroups="admin"`） |

密码均 `123456`。role 字面值与 BPMN 里的 `candidateGroups` 严格对应 —— 待办查询走 `taskCandidateGroup(UserContext.role)`，改任一边都要同步改另一边。

> 💡 **三级流程带来的新测试维度**：以前 leave 只有一个审批节点，manager 办完就结束；现在报销/采购/出差办完一级会**流转到 admin 的待办**，可以真实验证「任务在两个角色间流转」。也会第一次真实撞上多级审批的 `comment` 变量互相覆盖问题（实例级变量只有一份，已在三个模板的注释里标注，治理点在 `plan/P6`）。

> **本机环境踩坑**：带中文的请求体经 Git Bash 会以 GBK 发出触发后端 `Invalid UTF-8`，curl 测试用纯 ASCII 值绕过（浏览器走 UTF-8 无此问题）；`HTTP_PROXY=127.0.0.1:10808`，curl 打 localhost 要加 `--noproxy '*'`；context-path 是 `/workflow`。
>
> **办结 vs 终止的判据**（前端状态列会用）：办结 `END_ACT_ID_=endEvent` + `DELETE_REASON_` NULL + 有 `endEvent` 行；终止则 `END_ACT_ID_` 空 + `DELETE_REASON_` 有值 + 无 `endEvent` 行。完整三种结局判据表见 `process-back/deep_dive/hi_tables/03` 第五节。另：`terminate` 清 RU **保** HI，而 `cascade=true` 删部署会 RU + HI **一并抹除**（审计线索归零）——前端删除二次确认拦的就是后者。

---

## 三、下次开场直接说（两条支线，任选其一）

### A · 继续推进度（主线）—— P4 · Day 16 实例 + 我的发起

> "接着 `测试进度断点.md`，开 Day 16。"

**⚠️ 开工第一件事是拍板一个决策，它决定 Day 16 到底改不改后端：**

`ProcessInstanceServiceImpl.myStarted()` 目前查 RuntimeService，**只翻 RU 表** → 实例一办结/终止就从「我的发起」里消失，用户会以为丢单（库里 test-0004/0006 办结、0007 终止，只有 0005 在途 —— 照现状做，这一页只会显示 1 条）。二选一：

- **① 后端改查 `HistoricProcessInstanceQuery.startedBy()`**（推荐）：在途 + 办结 + 终止一页看全，配 `process-back/deep_dive/hi_tables/03` 第五节的三种结局判据渲染状态列。**代价是 Day 16 变成双端任务，不再是纯前端。**
- ② 保持 RU 版但改名「进行中的发起」，另开 HI 版做历史页。

**然后按 Day 16 清单做**（见 [`plan/P4`](./plan/P4-业务前端批量.md)）：`views/instance` + `views/myStart` + `api/instance.ts`，同构复用 `views/process` 那套（表格 + 分页 + 操作列）。顺手把「一」里那两笔小尾巴清掉。

**验收**：界面发起流程，并在「我的发起」看到。
完成后回填 [`plan/P4`](./plan/P4-业务前端批量.md) 的 Day 16 条目与打卡行，更新本文档「一」。
卡住问答疑 CLI。学习方式是**你写、我答**，不代写。

### B · 继续写学习笔记（支线）

> "接着写 `process-back/deep_dive/hi_tables/05`。"

按既定体例写最后一篇 `05-外围四表`（COMMENT / ATTACHMENT / IDENTITYLINK / DETAIL 速查合集），写完 hi_tables 支线收官。体例与进度见 `process-back/deep_dive/待深入主题清单.md` 第 3 条。

---

## 四、维护约定（防止本文档再次膨胀）

- **本文档只留三件事**：当前位置（一）、数据库现场（二）、下一步（三）。写完就该有人删掉过期的部分。
- **新产出/新决策** → 追加进 [`变更日志.md`](./变更日志.md)，不要往这里堆。
- **知识性发现** → 写进 `process-back/deep_dive/` 或 `process-front/前端答疑/`，这里最多留一行指针。
- **每日任务勾选与打卡** → 写进 `plan/P*.md`，不写这里。
- **阶段推进时**：把「一」整节重写成新阶段的现状，旧内容摘要一句进变更日志。
