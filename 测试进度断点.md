# 测试进度断点（下次从这里继续）

> 最后更新：**2026-08-11** ｜ 主线位置：**P4 · Day 16（代码 6/6 全关，只欠实测）** ｜ 当前在做：**造实例样本 + 浏览器实测**，`views/myStart/index.vue` 已 292 行、三步走完、build 转绿
>
> 📌 **本文档即进度依据**：会话开场按此文档定位，**不必重新扫代码核对**；代码一改完就当场回写这里。
> 本文档只装「现在在哪 / 下一步做什么 / 现场什么样」三件事，**篇幅刻意压住不让它增长**——历史流水账去 [`变更日志.md`](./变更日志.md)，知识沉淀去 `process-back/deep_dive/` 与 `process-front/前端答疑/`。

---

## 〇、文档地图（开场只读带 ★ 的）

| 文档 | 作用 | 开场读 |
|---|---|---|
| **本文档** | 当前位置 / 下一步 / 数据库现场 | ★ |
| [`plan/P4-业务前端批量.md`](./plan/P4-业务前端批量.md) | 当前阶段的每日任务与打卡 | ★ 只读当前阶段这一篇 |
| [`05-重写排期计划.md`](./05-重写排期计划.md) | 总控：阶段总览 + 技术栈 + ⚠️ 挂起项总表 | 需要看全局时 |
| [`plan/P0`–`P8`](./plan/) 其余八篇 | 其他阶段明细 | 不读 |
| [`变更日志.md`](./变更日志.md) | 历次产出流水账 + 已消化归档 | 不读，只追加 |
| [`process-front/页面思考法/`](./process-front/页面思考法/00-总纲.md) | **写页面的思考顺序**（① 布局 → ② 组件 → ③ 样式），跨页面复用的方法论 | 写任何新页面前读 `00-总纲` |
| `process-back/deep_dive/`、`process-back/process_*/`、`process-front/前端答疑/` | 学习材料与落地手册 | 不读，按需查 |

> **双 CLI 协作**：一个 CLI 改代码（主），一个只读答疑（问答）。新开任一 CLI 先读本仓库根目录三篇——`双CLI工作流.md`（分工总纲）、`问答CLI起手提示词.md`（开问答 CLI 时整体粘贴）、`讲解规则.md`（纵向剥层 + 横向讲史 + 动手实验）。答疑笔记落在 `process-front/前端答疑/`（索引 `process-front/前端答疑/索引.md`）与 `process-back/deep_dive/`。

---

## 一、当前位置

**P0–P3 全部关闭**（Day 1–14）。后端引擎四门面（Repository / Runtime / Task / History）全就位。

**P4 · Day 15 流程管理页 ✅ 已关闭**（2026-07-30 浏览器实测）：查询 / 挂起 / 激活三项 OK；删除带在途实例的部署被友好拦下 —— **这是预期行为不是失败**，`cascade=false` 遇在途实例走 catch 分支（底层 `PROC_DEF_ID_` 外键冲突），与 Day 12 后端实测同一条路径。前端第一个业务页跑通。

**Day 16 · 实例 + 我的发起：代码 6/6 全部关闭，只欠浏览器实测**

| # | 任务 | 状态 |
|---|---|---|
| 1 | `ProcessInstanceVO` 加 5 字段 + `of(HistoricProcessInstance)` + `statusOf()` | ✅ `5dabc3b` |
| 2 | `myStarted()` 改查 `HistoricProcessInstanceQuery.startedBy()` | ✅ `122418d`（`ProcessInstanceServiceImpl.java:100-108`） |
| 3 | 新增 `enrich()` 批量补发起人姓名 + 当前节点 | ✅ 同上（`:194`，整页固定 2 次查询） |
| 4 | ~~`toPage()` 泛型化~~ → **`toHiPage()` 复制版**（零泛型） | ✅ 同上（`toPage:131` 吃 RU、`toHiPage:180` 吃 HI，各 5 行、末尾都调 `enrich()`） |
| 5 | 前端 `views/instance` + `views/myStart` + `api/instance.ts` | ✅ **已关闭**（2026-08-11）：`api/instance.ts` 59 行（`68eb001`）｜ `views/instance/index.vue` 177 行（`548f236`→`5ccf065`）｜ `views/myStart/index.vue` **292 行，三步走完、build 转绿**，详见下方 🔻 |
| 6 | 2 条路由 + 2 个菜单项 | ✅ `ada25b4`：`router/index.ts:34-38` 挂 `/instance`、`:39-43` 挂 `/my-start`（name `Mystart`）；`layout/index.vue:28-29` 两个菜单项都在 |

> ✅ **后端超出清单顺手做掉的两件**（都在 `122418d` 里）：`applyStatusFilter()`（`:146-161`，if/else 三态过滤，未知值抛 `BizException`）；Controller `my-started` 已加 `status` 入参（`ProcessInstanceController.java:39-46`）。

> 🔻 **`views/myStart/index.vue` 已收工**（2026-08-11，292 行，分支 `dev`）。**三步法第二次完整走通**，`npm run build` 转绿。
>
> **① 布局 ✅ / ② 组件 ✅ / ③ 样式 ✅**
>
> - **② 组件**：状态下拉（`:7-14`）｜ 表格 7 列 + `STATUS_MAP` 状态标签（`:22-57`）｜ 分页（`:62-70`）｜ 撤回 + `handleRevoke`（`:231`）｜ **发起弹窗**（`:74-109`，`el-dialog` + `el-form` + `rules` + `handleSubmit`）｜ `onMounted` 双加载（`:258`）
> - **③ 样式**：`.title` / `.toolbar-right`（`display:flex` + `gap:10px`）/ `.muted` / `.pager` 四条全部接上
> - 竞态两处都挂了：下拉 `:disabled="loading"`（`:10`）、分页 `:disabled="loading"`（`:68`）
>
> **弹窗这一段的六个决策**（都验过，别回头推翻）：
> - `:value="d.key"` 不是 `d.id` —— `StartInstanceBody` 要的是 key
> - `FormRules<typeof form>`（`:139`）—— 带泛型才能在字段名拼错时编译报错，不带的话键是开放 `string`
> - `businessKey.trim() || undefined`（`:215-216`）—— 用 `undefined` 不用 `''`，`JSON.stringify` 时键会消失＝不传
> - `status.value = ''`（`:221`）—— 发起成功后连筛选一起重置，否则筛选停在「已办结」时**新实例不出现在列表里**（提示成功却看不见）
> - `:close-on-click-modal="false"` + `:close-on-press-escape="!submitting"` + **`:show-close="!submitting"`**（`:78-80`）—— 前两个管不到右上角 X，X 要靠第三个
> - `loadDefs()` 的 catch 自己弹 warning（`:186`）—— 全项目唯一自己弹错的 catch，因为拉不到 defs 的后果是弹窗下拉空着、用户看不出原因
>
> ⚠️ **别读 `suspended` 字段**：本页走 HI 查询，该字段恒 false（哑雷，见下方小尾巴清单 Day 17 那条）。
>
> 📌 **本页踩过的坑与新沉淀**：五个坑见 [`变更日志.md`](./变更日志.md) 2026-08-10 节；弹窗段的新产出见 08-11 节（含新答疑篇 [`Teleport与弹窗渲染位置.md`](./process-front/前端答疑/Teleport与弹窗渲染位置.md)）。
>
> ⚠️ **有一处改动尚未提交**：操作列的 `label="操作" width="90" fixed="right"`（`:45`），工作区 `M` 状态。**下次开场先 commit 掉。**
>
> ---
>
> **留档：`views/instance/index.vue`**（177 行，08-06 一天写完，`548f236`→`5ccf065`）—— **本项目第一个「本人从零写」的页面**，三步法首次走通，CLI 全程只答疑未代写一行。它定下的 **4 div 结构**（`.instance-page` > `.toolbar` / `.table-wrap` / `.pager-wrap`）是 P4 后面两页的范本。

> ✅ **契约已两边对齐**（后端 `30a7314` 顺手改的）：终止接口从 `DELETE /process-instance/{instanceId}` 改形为 `DELETE /process-instance/{instanceId}/terminate`，去迁就 `api/instance.ts:55`。**后端需重启一次**才生效。四个接口 `start` / `page` / `my-started`（带 `status`）/ `{instanceId}/terminate` 与 `api/instance.ts` 已于 2026-08-10 再次逐个核对，无错配，前端不用再改契约。

> 📌 **2026-08-03 决策：第 4 项改走无泛型版，主线立即复工**（已落地，保留备查）。
> 原暂停原因是泛型版 `toPage` 读不动（根因：泛型基础没打牢）。八篇泛型文档已快速通读，判定**继续停留在泛型上收益低**，遂改走 `08` 篇 §4 早已认定为**正当选项**的路线——保留 `toPage` 吃 `ProcessInstanceQuery`，另复制 `toHiPage` 吃 `HistoricProcessInstanceQuery`，两个方法各 5 行、末尾都调 `enrich()`。功能与泛型版完全等价，代价只是分页收尾的代码存在两份。
> **照着敲的文档**：[`10-Day16-完整代码-无泛型版.md`](./process-back/process_instance/10-Day16-完整代码-无泛型版.md)（当前采用，含 if/else 版状态过滤）。步骤仍看 `08-Day16-操作步骤.md`。
> **`09-Day16-完整代码.md`（泛型版）原样保留**，是将来的重构目标不是当前任务——`10` 第 5 节已列出「换回泛型版只动 4 处」的清单，那一步同时就是 `泛型/08` 的编译验收。

**小尾巴清单**（各带处理时机，别一股脑现在做）：

| 时机 | 项 |
|---|---|
| ~~Day 16 顺手~~ | ✅ **已关闭**（2026-08-11，`3024e5e`）**并发竞态**：`views/process/index.vue:7` 的 `<el-switch>` 已挂 `:loading="loading"`、`:43` 的 `<el-pagination>` 已挂 `:disabled="loading"`。记了四次的欠账终于清掉。📖 原理见 [`请求竞态与控件禁用.md`](./process-front/前端答疑/请求竞态与控件禁用.md) |
| ~~Day 16 顺手~~ | ✅ **已关闭**（2026-08-11，`ab4dd5d`）**命名**：`api/process.ts:29` 的 `deleteDeployment` 形参已从 `definitionId` 改为 `deploymentId` |
| **项目完工后**<br>（本人 2026-08-06 决定推迟） | **div 包裹风格不一致**：`views/instance/index.vue` 由本人定为**外框 + 三条带各包一个 div**（扫一眼 div 就能看清宏观布局，且以后往某条带里加东西时结构不用动）；而 CLI 生成的 `views/process/index.vue` 只有工具条包了 div，表格和分页是裸放的（`:13`、`:39`）。<br>✅ **实例页已按此写完并 build 转绿**（08-06），**范本已成立、不再是设想**：`.instance-page` > `.toolbar` / `.table-wrap` / `.pager-wrap`。P4 后面三页（myStart / task / approved）一律照它写；流程管理页留到项目完工后统一。<br>📌 注意 `views/process/index.vue` **是 CLI 生成的样本，不是本人定的惯例**，没有约束力 —— 实例页才是本项目第一个本人自定的结构 |
| **Day 18 顺手** | 🔗 **myStart 操作列补「轨迹」按钮**（2026-08-10 登记）：原型第 ③ 节是**每行都有轨迹** + 撤回只给进行中的行；Day 16 只做了撤回，空格子用 `<span v-else class="muted">—</span>` 占位。**Day 18 写 `api/history.ts` 时回头把轨迹接上，并删掉那个破折号**。后端接口 Day 14 就绪（`HistoryController.java:33-34`）。⚠️ 这条**不是 Day 16 的遗漏**，是排期切分——但当初三篇文档一个字没提，照着 `03-完整代码.md` 写完对原型会以为漏了，故补登记。详见 `process-front/process_instance/01-页面定位与修改点.md` 第三节末尾 |
| **Day 17 前必看** | ⚠️ **哑雷 `suspended`**（2026-08-04 发现）：`ProcessInstanceVO.of(HistoricProcessInstance)`（`ProcessInstanceVO.java:77-90`）没 set `suspended`，Java `boolean` 默认 false —— **凡走 `myStarted()`/HI 查询回来的实例，`suspended` 恒为 `false`**，不管实际是否挂起。当前无害（前端注释已写"本页不用"），但 **Day 17 待办页 / Day 18 历史页别读这个字段**，TS 也查不出来（类型是 boolean，值也确实是 boolean）。建议改法：把 `03-完整代码.md:35` 的注释从「本页不用」改成「⚠️ HI 路径恒 false，别读」，不动类型（动了会牵连 RU 那条路，为一个没人用的字段不值当）。原理见 `process-front/前端答疑/null与undefined.md` 第七节 ③ |
| **P4 收尾时评估**<br>（Day 18 之后） | **`request.get<never, X>` 的重复**（2026-08-04 登记）：`api/user.ts` 5 处、`api/process.ts` 4 处、`api/instance.ts` 5 处都在重复写两个泛型参数。根因是响应拦截器（`utils/request.ts:45`）拆了包，而 axios 的类型不知道，只能每处手动把第二个泛型 `R` 纠正成真实载荷类型。<br>**候选解法**：在 `utils/request.ts` 末尾包一层 `http` 门面（`get: <T>(url, config) => request.get(url, config) as Promise<T>`），泛型从两个减到一个，语义也更清楚。<br>⚠️ **现在别做**：① 是跨三个 api 文件 + 全部调用处的重构，Day 16 主线还有两页没写；② 现写法虽丑但**是对的、三个文件完全一致**；③ 等 Day 18 四个页面写完，有 4 文件 20+ 处调用时样本才够，能一次看清哪种封装合适。<br>📌 `api/process.ts:18` 那行注释掉的无泛型版是 Day 15 试过又换回来的，**别当成可用写法**：去掉泛型会退化成 `Promise<AxiosResponse<any>>`，调用处 `page.records` 会报错，而报错信息会把人引向 `page.data.records` 这个**错误修法**（`data` 是 `any`，编译通过、运行时 undefined）。原理尚未成文，可落成 TS 链的一篇 |

**学习支线（当前主战场）**：

| 支线 | 进度 | 位置 |
|---|---|---|
| **泛型系列** | **🔸 2026-08-03 告一段落**：九篇已快速通读（不懂的先跳过），**不再作为主线阻塞项**。<br>已达 ② 档：自己写出了等价的 `common/Result.java`（`08` 闭卷第一题实测通过）。<br>③ 档（通配符 / 自限定泛型）留待将来，**触发点 = 觉得 `toPage`/`toHiPage` 两份重复碍眼时**，回 `09` + `10` 第 5 节做重构 | `process-back/deep_dive/泛型/`<br>进度表见 `待深入主题清单.md` 第 5 条 |
| hi_tables | 只剩 `05-外围四表.md` 收官 | `process-back/deep_dive/hi_tables/` |

两条支线的进度表都在 `process-back/deep_dive/待深入主题清单.md`（泛型第 5 条，hi_tables 第 3 条）。

---

## 二、当前数据库现场（flowable_db2）

> 🔄 **2026-07-30 已整库重置**：执行 `resources/sql/00_reset_db.sql` 清空 → 重启建表 → 跑 `sys_user.sql`。
> **旧的 test-0004~0007 四条实例样本全部作废**，历史表清空，实例数据从零开始。

**流程定义**：四个模板，随启动自动部署（见下方开关说明），全部 v1、激活状态。

| 模板文件 | key | 名称 | 结构（三级） |
|---|---|---|---|
| `leave.bpmn20.xml` | `leaveProcess` | 请假流程 | 发起 → 经理审批[manager] → 结束　*（两级，原有）* |
| `reimburse.bpmn20.xml` | `reimburseProcess` | 报销流程 | 发起 → 经理审批[manager] → **财务审批[admin]** → 结束 |
| `purchase.bpmn20.xml` | `purchaseProcess` | 采购流程 | 发起 → 经理审批[manager] → **总监审批[admin]** → 结束 |
| `travel.bpmn20.xml` | `travelProcess` | 出差流程 | 发起 → 经理审批[manager] → **总监审批[admin]** → 结束 |

三个新模板的节点 id 统一为 `startEvent` / `managerApprove` / `adminApprove` / `endEvent`，连线 `flow1`–`flow3`，均带手写 DI 坐标（P5 的 bpmn-js 渲染要用）。

> ✅ **`check-process-definitions` 已经是 `false`**（`application.yml:33`，2026-07-31 核对）—— 四个模板确认入库后已关掉，启动**不再**自动扫 `processes/`。今后要加模板，走 Day 11 的手动部署接口，或临时改回 `true` 重启一次再关掉。
> ⚠️ 后端服务改了代码需**重新启动**：`process-back` 下 `./mvnw spring-boot:run`，`http://localhost:9090/workflow/doc.html` 能开即可。

**实例样本：暂无**（清库后还没发起过）。P4 后面三个页面（我的发起 / 待办 / 已审批）需要样本时，用 employee 账号各发几条，并让其中一条停在 `managerApprove`、一条停在 `adminApprove`、一条走完、一条终止，就能覆盖全部状态列。

账号（`sys_user.sql` 已重新灌入）：

| 用户 | id | role | 在流程里的角色 |
|---|---|---|---|
| employee | 3 | `employee` | 发起人 |
| manager | 2 | `manager` | 一级审批（`candidateGroups="manager"`） |
| admin | 1 | `admin` | 二级审批（`candidateGroups="admin"`） |

密码均 `123456`。role 字面值与 BPMN 里的 `candidateGroups` 严格对应 —— 待办查询走 `taskCandidateGroup(UserContext.role)`，改任一边都要同步改另一边。

> 💡 **三级流程带来的新测试维度**：以前 leave 只有一个审批节点，manager 办完就结束；现在报销/采购/出差办完一级会**流转到 admin 的待办**，可以真实验证「任务在两个角色间流转」。也会第一次真实撞上多级审批的 `comment` 变量互相覆盖问题（实例级变量只有一份，已在三个模板的注释里标注，治理点在 `plan/P6`）。

> **本机环境踩坑**：带中文的请求体经 Git Bash 会以 GBK 发出触发后端 `Invalid UTF-8`，curl 测试用纯 ASCII 值绕过（浏览器走 UTF-8 无此问题）；`HTTP_PROXY=127.0.0.1:10808`，curl 打 localhost 要加 `--noproxy '*'`；context-path 是 `/workflow`。
>
> **办结 vs 终止的判据**（前端状态列会用）：办结 `END_ACT_ID_=endEvent` + `DELETE_REASON_` NULL + 有 `endEvent` 行；终止则 `END_ACT_ID_` 空 + `DELETE_REASON_` 有值 + 无 `endEvent` 行。完整三种结局判据表见 `process-back/deep_dive/hi_tables/03` 第五节。另：`terminate` 清 RU **保** HI，而 `cascade=true` 删部署会 RU + HI **一并抹除**（审计线索归零）——前端删除二次确认拦的就是后者。

---

## 三、下次开场直接说（两条路，A 是当前默认）

### A · 回主线 —— Day 16 造样本 + 浏览器实测（🔸 当前默认）

> "接着 `测试进度断点.md`，造实例样本，实测「我的发起」页。"

**Day 16 代码已全部写完**（六项全关）。剩下的**纯粹是实测**，一行新代码都不用写。

**0. 开场先 commit** 那一笔未提交的操作列改动（`views/myStart/index.vue:45`）。

**1. 起服务**：后端 `process-back` 下 `./mvnw spring-boot:run`（`http://localhost:9090/workflow/doc.html` 能开即可），前端 `npm run dev`。

**2. 造样本** —— 库里**一条实例都没有**（07-30 清库后没发起过）。用 employee 账号（密码 `123456`）在**界面上**发起，顺便就把发起弹窗验了：

| 要造的状态 | 怎么造 |
|---|---|
| 在途，停 `managerApprove` | 发起后不办 |
| 在途，停 `adminApprove` | 发起报销/采购/出差（三级），再用 manager 办掉一级 |
| 已办结 | 上一条再让 admin 办掉 |
| 已终止 | 在「我的发起」页点撤回，填 reason |

⚠️ Day 17 待办页还没写，**办理只能走 knife4j**（`/task/{taskId}/complete`），manager / admin 各自登录拿 token。

**3. 验收清单**（四条，缺一不可）：
- 界面能发起，发起后**立刻在列表里看到**（`status.value=''` 那行的意义）
- 状态列四种情况都渲染正确（进行中 / 已办结 / 已终止 三个 tag + 当前节点）
- **办结 / 终止的实例仍留在列表里** ← 这是 Day 16 改查 HI 的全部意义所在
- `startUserName` 显示的是姓名不是 userId、`currentActivityName` 有值

**4. 顺带关掉后端那条 knife4j 验收**（`plan/P4` Day 16 第 40 行）：样本一造出来就能验 `my-started` 的三态 `status` 过滤。

**5. 全部通过后**：回填 [`plan/P4`](./plan/P4-业务前端批量.md) 的验收行与打卡行，把本文档「一」推进到 **Day 17 待办页**，Day 17 开工前先看小尾巴清单里 `suspended` 哑雷那条。

> 📌 **实测出问题不要急着改代码**，先照 `讲解规则.md` 第五节的规矩定位：先看是后端返回不对（knife4j 直接打接口）还是前端渲染不对（F12 看 Network 的响应体）。

### B · hi_tables 收官（支线）

> "接着写 `process-back/deep_dive/hi_tables/05`。"

按既定体例写最后一篇 `05-外围四表`（COMMENT / ATTACHMENT / IDENTITYLINK / DETAIL 速查合集），写完 hi_tables 支线收官。体例与进度见 `process-back/deep_dive/待深入主题清单.md` 第 3 条。

---

## 四、维护约定（防止本文档再次膨胀）

- **本文档只留三件事**：当前位置（一）、数据库现场（二）、下一步（三）。写完就该有人删掉过期的部分。
- **新产出/新决策** → 追加进 [`变更日志.md`](./变更日志.md)，不要往这里堆。
- **知识性发现** → 写进 `process-back/deep_dive/` 或 `process-front/前端答疑/`，这里最多留一行指针。
- **每日任务勾选与打卡** → 写进 `plan/P*.md`，不写这里。
- **阶段推进时**：把「一」整节重写成新阶段的现状，旧内容摘要一句进变更日志。
