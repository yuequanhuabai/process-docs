# 测试进度断点（下次从这里继续）

> 🤖 **给 CLI**：若你是被「定位当前项目的进度」这类指令带到本文件的，**先确认三个仓库已 `git pull` 过**（`process-back`/`process-front` 在 `dev`，本仓库在 `master`；工作区脏就先停下报告）。完整规则见 [`指令码.md`](./指令码.md) 指令码 #1，摘要在 [`CLAUDE.md`](./CLAUDE.md)。

> 最后更新：**2026-08-14** ｜ 主线位置：**P4 · Day 17 待办页（后端 ✅ / 前端代码 ✅ / 浏览器验收 ⬜）** ｜ 当前在做：**代码双端都完工、build 转绿**，下一步就是**跑验收**：manager 界面办掉一级 → admin 待办出现 → admin 再办 → 「我的发起」变已办结
>
> 📌 **本文档即进度依据**：会话开场按此文档定位，**不必重新扫代码核对**；代码一改完就当场回写这里。
> 本文档只装「现在在哪 / 下一步做什么 / 现场什么样」三件事，**篇幅刻意压住不让它增长**——历史流水账去 [`变更日志.md`](./变更日志.md)，知识沉淀去 `process-back/deep_dive/` 与 `process-front/前端答疑/`。

---

## 〇、文档地图（开场只读带 ★ 的）

| 文档 | 作用 | 开场读 |
|---|---|---|
| **本文档** | 当前位置 / 下一步 / 数据库现场 | ★ |
| [`plan/P4-业务前端批量.md`](./plan/P4-业务前端批量.md) | 当前阶段的每日任务与打卡 | ★ 只读当前阶段这一篇 |
| [`05-重写排期计划.md`](./05-重写排期计划.md) | 总控：阶段总览 + 技术栈 + ⚠️ 挂起项总表 | 需要看全局时 |
| [`plan/P0`–`P8`](./plan/) 其余八篇 | 其他阶段明细 | 不读 |
| [`变更日志.md`](./变更日志.md) | 历次产出流水账 + 已消化归档 | 不读，只追加 |
| [`process-front/页面思考法/`](./process-front/页面思考法/00-总纲.md) | **写页面的思考顺序**（① 布局 → ② 组件 → ③ 样式），跨页面复用的方法论 | 写任何新页面前读 `00-总纲` |
| `process-back/deep_dive/`、`process-back/process_*/`、`process-front/前端答疑/` | 学习材料与落地手册 | 不读，按需查 |

> **双 CLI 协作**：一个 CLI 改代码（主），一个只读答疑（问答）。新开任一 CLI 先读本仓库根目录三篇——`双CLI工作流.md`（分工总纲）、`问答CLI起手提示词.md`（开问答 CLI 时整体粘贴）、`讲解规则.md`（纵向剥层 + 横向讲史 + 动手实验）。答疑笔记落在 `process-front/前端答疑/`（索引 `process-front/前端答疑/索引.md`）与 `process-back/deep_dive/`。

---

## 一、当前位置

**P0–P3 全部关闭**（Day 1–14）。**P4 已关两天**：Day 15 流程管理页（07-30 实测关闭）、Day 16 实例 + 我的发起（08-11 晚实测关闭，两页均本人从零写成；细节见 [`变更日志.md`](./变更日志.md) 08-11 两节）。

**Day 17 · 待办页 —— 代码双端已完工，只差浏览器验收（当前就在这）**

**后端小改（决策①）✅ 已关闭**（2026-08-12 上午，knife4j 双接口实测）：`TaskVO` 补 `processDefinitionName` / `businessKey` / `startUserName` 三字段；`FlowableTaskServiceImpl` 加 `enrich()` 并在 `toPage()`、`detail()` **两个调用点**挂载（`b25c356`→`e141dab`）。中途两处静默失效（编译全绿、字段恒 null）与自查手段「全项目搜 `enrich(` 应为 3 行」详见变更日志 08-12 节；那行残留的无用 `import Autowired` 也已删（`27fa849`）。

**前端 ✅ 已完工**（2026-08-12~08-14，`c081d70`→`901aaf8` 八笔，**三步法第三次走通**）：`api/task.ts` 43 行（`pageTodo` / `getTaskDetail` / `completeTask` + 3 类型）、`views/task/index.vue` 187 行（表格 5 列 + 分页 + 办理弹窗 `:48-84`）、路由与菜单已挂。`npm run build`（含 `vue-tsc -b`）**08-14 实跑转绿**。开工前列的六条约束逐条对照都写对了（对照表在变更日志 08-13~14 节）；顺手把 08-12 建的大写 `views/Task/` 改成小写 `task/`——本地大小写不敏感看不出，Linux/CI 上会直接构建失败。

**⬜ 剩下的唯一一件事：浏览器验收**，动作见下面第三节 A。两笔小尾巴不阻塞验收，已登记进下面的清单。

**小尾巴清单**（各带处理时机，别一股脑现在做）：

| 时机 | 项 |
|---|---|
| **Day 18 前必看** | ⚠️ **哑雷 `suspended`**（2026-08-04 发现）：`ProcessInstanceVO.of(HistoricProcessInstance)`（`ProcessInstanceVO.java:77-90`）没 set `suspended`，Java `boolean` 默认 false —— **凡走 `myStarted()`/HI 查询回来的实例，`suspended` 恒为 `false`**，不管实际是否挂起。当前无害，但 **Day 18 历史页别读这个字段**，TS 也查不出来。建议改法：把 `03-完整代码.md:35` 的注释从「本页不用」改成「⚠️ HI 路径恒 false，别读」，不动类型。原理见 `process-front/前端答疑/null与undefined.md` 第七节 ③<br>📌 **2026-08-12 澄清范围**：哑雷**只在 `ProcessInstanceVO`（HI 路径）**。`TaskVO.suspended` 是真的——`fill()` 里实打实写了 `vo.setSuspended(task.isSuspended())`，值来自 `ACT_RU_TASK.SUSPENSION_STATE_`。**待办页不读它的理由是「用不上」，不是「它假」**，两者别混 |
| **Day 18 顺手** | 🔗 **myStart 操作列补「轨迹」按钮**（2026-08-10 登记）：原型第 ③ 节是**每行都有轨迹** + 撤回只给进行中的行；Day 16 只做了撤回，空格子用 `<span v-else class="muted">—</span>` 占位。**Day 18 写 `api/history.ts` 时回头把轨迹接上，并删掉那个破折号**。后端接口 Day 14 就绪（`HistoryController.java:33-34`）。这条**不是 Day 16 的遗漏**，是排期切分。详见 `process-front/process_instance/01-页面定位与修改点.md` 第三节末尾 |
| **P4 收尾时评估** | **待办页「办理」按钮拉详情期间没禁用**（2026-08-14 登记）：`views/task/index.vue:27`，`openHandle` 里 `await getTaskDetail(row.id)` 期间按钮可连点，会发多次详情请求。比分页那种「静默错位」轻——最后一个响应覆盖 `detail`，弹窗内容仍对——只是多余请求。同页的 div 类名（`.top`/`.middle`/`.bottom`）没照实例页范本，那笔归到本表最后一行「项目完工后统一」，不单独立项 |
| **P4 收尾时评估** | **发起弹窗下拉未过滤挂起定义**（2026-08-11 登记）：`loadDefs()` 拉全部定义，挂起的也能选，用户填完表单提交才被后端预检拦（`ProcessInstanceServiceImpl.java:68-69`）。后端兜底本来就该在，前端过滤/置灰属体验优化，做不做到时候定 |
| **P4 收尾时评估**（Day 18 之后） | **`request.get<never, X>` 的重复**（2026-08-04 登记）：`api/user.ts` 5 处、`api/process.ts` 4 处、`api/instance.ts` 5 处都在重复写两个泛型参数。根因是响应拦截器（`utils/request.ts:45`）拆了包而 axios 类型不知道。候选解法：`utils/request.ts` 末尾包一层 `http` 门面（`get: <T>(url, config) => request.get(url, config) as Promise<T>`）。⚠️ 等 Day 18 四个页面写完、有 4 文件 20+ 处调用时样本才够。📌 `api/process.ts:18` 那行注释掉的无泛型版是 Day 15 试过又换回来的，**别当成可用写法** |
| **项目完工后**（2026-08-06 决定推迟） | **div 包裹风格不一致**：实例页定下的**外框 + 三条带各包一个 div** 是范本（`.instance-page` > `.toolbar` / `.table-wrap` / `.pager-wrap`），P4 后面两页照它写；CLI 生成的 `views/process/index.vue` 表格和分页裸放（`:13`、`:39`），**它是样本不是惯例、没有约束力**，留到项目完工后统一 |

**学习支线**：

| 支线 | 进度 | 位置 |
|---|---|---|
| **泛型系列** | **🔸 2026-08-03 告一段落**：九篇已快速通读，不再作为主线阻塞项。已达 ② 档（自己写出等价 `common/Result.java`）。③ 档触发点 = 觉得 `toPage`/`toHiPage` 两份重复碍眼时，回 `09` + `10` 第 5 节做重构 | `process-back/deep_dive/泛型/`，进度表见 `待深入主题清单.md` 第 5 条 |
| hi_tables | 只剩 `05-外围四表.md` 收官 | `process-back/deep_dive/hi_tables/` |

---

## 二、当前数据库现场（flowable_db2）

**流程定义**：四个模板，全部 v1、**当前全部激活**。⚠️ 定义的挂起态会**跨会话残留**（`ACT_RE_PROCDEF.SUSPENSION_STATE_`，1 激活 / 2 挂起）：`leaveProcess` 曾因 Day 15 挂起实测遗留在挂起档，08-11 已 swagger 激活（`REV_` 随改动 +1，非 1 属正常）。以后发起报「已挂起」先查这列。

| 模板文件 | key | 名称 | 结构（三级） |
|---|---|---|---|
| `leave.bpmn20.xml` | `leaveProcess` | 请假流程 | 发起 → 经理审批[manager] → 结束　*（两级，原有）* |
| `reimburse.bpmn20.xml` | `reimburseProcess` | 报销流程 | 发起 → 经理审批[manager] → **财务审批[admin]** → 结束 |
| `purchase.bpmn20.xml` | `purchaseProcess` | 采购流程 | 发起 → 经理审批[manager] → **总监审批[admin]** → 结束 |
| `travel.bpmn20.xml` | `travelProcess` | 出差流程 | 发起 → 经理审批[manager] → **总监审批[admin]** → 结束 |

> ✅ `check-process-definitions` 是 `false`（`application.yml:33`）——启动不自动扫 `processes/`。要加模板走手动部署接口，或临时改回 `true` 重启一次再关掉。
> ⚠️ 后端改了代码需重启：`process-back` 下 `./mvnw spring-boot:run`，`http://localhost:9090/workflow/doc.html` 能开即可。

**实例样本：已造齐三态**（2026-08-11，「我的发起」页逐态验过）：进行中 / 已办结 / 已终止都有，其中含 08-03 发起、停在 `managerApprove` 的 leave 与 purchase 两条在途。具体条数以库为准（`SELECT ID_, BUSINESS_KEY_, PROC_DEF_ID_ FROM ACT_RU_EXECUTION WHERE PARENT_ID_ IS NULL` 看在途；HI 全量看 `ACT_HI_PROCINST`）。

> 💡 **Day 17 的样本提示**：manager 视角的待办有现成样本（停 `managerApprove` 的行）；验 **admin 视角 + 两级流转**需要一条三级流程（报销/采购/出差）被 manager 办掉一级停在 `adminApprove`——待办页写好后正好用它自己办，这就是 Day 17 的验收动作。

账号（`sys_user.sql`）：

| 用户 | id | role | 在流程里的角色 |
|---|---|---|---|
| employee | 3 | `employee` | 发起人 |
| manager | 2 | `manager` | 一级审批（`candidateGroups="manager"`） |
| admin | 1 | `admin` | 二级审批（`candidateGroups="admin"`） |

密码均 `123456`。role 字面值与 BPMN 里的 `candidateGroups` 严格对应——待办查询走 `taskCandidateGroup(UserContext.role)`，改任一边都要同步改另一边。

> **本机环境踩坑**：带中文的请求体经 Git Bash 会以 GBK 发出触发后端 `Invalid UTF-8`，curl 测试用纯 ASCII 值绕过（浏览器走 UTF-8 无此问题）；`HTTP_PROXY=127.0.0.1:10808`，curl 打 localhost 要加 `--noproxy '*'`；context-path 是 `/workflow`。
>
> **办结 vs 终止的判据**：办结 `END_ACT_ID_=endEvent` + `DELETE_REASON_` NULL + 有 `endEvent` 行；终止则 `END_ACT_ID_` 空 + `DELETE_REASON_` 有值 + 无 `endEvent` 行。完整判据表见 `process-back/deep_dive/hi_tables/03` 第五节。另：`terminate` 清 RU **保** HI，而 `cascade=true` 删部署会 RU + HI **一并抹除**——前端删除二次确认拦的就是后者。

---

## 三、下次开场直接说（两条路，A 是当前默认）

### A · 回主线 —— Day 17 待办页**浏览器验收**（🔸 当前默认）

> "接着 `测试进度断点.md`，跑 Day 17 待办页验收。"

**0. 开场先看**：`plan/P4` Day 17 条目即可，代码已全部就位，不用再读需求文档与原型。

**1. 起服务**：后端 `process-back` 下 `./mvnw spring-boot:run`（`doc.html` 能开即可），前端 `npm run dev`。

**2. 验收三步**（即 P4 Day 17 验收行）：

1. **manager 登录** → 待办页应看到三条（含停 `managerApprove` 的 leave / purchase）→ 挑一条**三级流程**（报销/采购/出差）点「办理」，弹窗只读区应显示流程/节点、业务标识与 `variables`，填不填意见都行，提交；
2. **切 admin 登录** → 待办页出现 `adminApprove` 的那条 ⇒ **界面办理 + 两级流转**都通；
3. **admin 再办掉它** → 用 employee 登录看「我的发起」，该条变「已办结」。

⚠️ 发起/办理若报「流程定义已挂起」，先查 `ACT_RE_PROCDEF.SUSPENSION_STATE_`（Day 15 挂起实测的遗留态），swagger 激活即可。

**3. 收工回填**：`plan/P4` Day 17 验收行勾选与打卡行 + 本文档「一」推进到 Day 18。

> 📌 `POST /task/complete` **故意一直没在 swagger 打过**——在 swagger 里办理会把 `managerApprove` 的样本消耗掉，那条路径就是留给这次页面验收的。出问题照 `讲解规则.md` 第五节定位：先分清是后端返回不对（knife4j 直接打接口）还是前端渲染不对（F12 看 Network 响应体）。

### B · hi_tables 收官（支线）

> "接着写 `process-back/deep_dive/hi_tables/05`。"

按既定体例写最后一篇 `05-外围四表`（COMMENT / ATTACHMENT / IDENTITYLINK / DETAIL 速查合集），写完 hi_tables 支线收官。体例与进度见 `process-back/deep_dive/待深入主题清单.md` 第 3 条。

---

## 四、维护约定（防止本文档再次膨胀）

- **本文档只留三件事**：当前位置（一）、数据库现场（二）、下一步（三）。写完就该有人删掉过期的部分。
- **新产出/新决策** → 追加进 [`变更日志.md`](./变更日志.md)，不要往这里堆。
- **知识性发现** → 写进 `process-back/deep_dive/` 或 `process-front/前端答疑/`，这里最多留一行指针。
- **每日任务勾选与打卡** → 写进 `plan/P*.md`，不写这里。
- **阶段推进时**：把「一」整节重写成新阶段的现状，旧内容摘要一句进变更日志。
