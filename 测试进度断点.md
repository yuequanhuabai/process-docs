# 测试进度断点（下次从这里继续）

> 🤖 **给 CLI**：若你是被「定位当前项目的进度」这类指令带到本文件的，**先确认三个仓库已 `git pull` 过**（`process-back`/`process-front` 在 `dev`，本仓库在 `master`；工作区脏就先停下报告）。完整规则见 [`指令码.md`](./指令码.md) 指令码 #1，摘要在 [`CLAUDE.md`](./CLAUDE.md)。

> 最后更新：**2026-08-18** ｜ 主线位置：**P5 · Day 19 设计器入口 + bpmn-js 预览（⬜ 未开工）** ｜ 上一步：**Day 18 已审批 + 轨迹 08-18 浏览器实测关闭 ⇒ P4 收官**（登录 → 发起 → 审批 → 历史全程界面可走）｜ 下一步：进 P5 —— 全项目最难的一块，先 Viewer 后编辑
>
> 📌 **本文档即进度依据**：会话开场按此文档定位，**不必重新扫代码核对**；代码一改完就当场回写这里。
> 本文档只装「现在在哪 / 下一步做什么 / 现场什么样」三件事，**篇幅刻意压住不让它增长**——历史流水账去 [`变更日志.md`](./变更日志.md)，知识沉淀去 `process-back/deep_dive/` 与 `process-front/前端答疑/`。

---

## 〇、文档地图（开场只读带 ★ 的）

| 文档 | 作用 | 开场读 |
|---|---|---|
| **本文档** | 当前位置 / 下一步 / 数据库现场 | ★ |
| [`plan/P5-BPMN设计器.md`](./plan/P5-BPMN设计器.md) | 当前阶段的每日任务与打卡 | ★ 只读当前阶段这一篇 |
| [`05-重写排期计划.md`](./05-重写排期计划.md) | 总控：阶段总览 + 技术栈 + ⚠️ 挂起项总表 | 需要看全局时 |
| [`plan/P0`–`P8`](./plan/) 其余八篇 | 其他阶段明细（P4 已于 08-18 关闭） | 不读 |
| [`变更日志.md`](./变更日志.md) | 历次产出流水账 + 已消化归档 | 不读，只追加 |
| [`process-front/页面思考法/`](./process-front/页面思考法/00-总纲.md) | **写页面的思考顺序**（① 布局 → ② 组件 → ③ 样式），跨页面复用的方法论 | 写任何新页面前读 `00-总纲` |
| `process-back/deep_dive/`、`process-back/process_*/`、`process-front/前端答疑/` | 学习材料与落地手册 | 不读，按需查 |

> **双 CLI 协作**：一个 CLI 改代码（主），一个只读答疑（问答）。新开任一 CLI 先读本仓库根目录三篇——`双CLI工作流.md`（分工总纲）、`问答CLI起手提示词.md`（开问答 CLI 时整体粘贴）、`讲解规则.md`（纵向剥层 + 横向讲史 + 动手实验）。答疑笔记落在 `process-front/前端答疑/`（索引 `process-front/前端答疑/索引.md`）与 `process-back/deep_dive/`。

---

## 一、当前位置

**P0–P4 全部关闭**（Day 1–18）。**P4 于 2026-08-18 收官** —— 四个页面（流程管理 / 实例·我的发起 / 待办 / 已审批·轨迹）**均本人从零写成**，**登录 → 发起 → 审批 → 历史全程界面可走**。Day 18 的完整记录见 [`变更日志.md`](./变更日志.md) 08-17~18 节与 [`plan/P4`](./plan/P4-业务前端批量.md) 打卡行。

**P5 · Day 19 设计器入口 + bpmn-js 预览 —— ⬜ 未开工（当前就在这）**

**⚠️ 这是全项目最难的一块**（排期第五节把它列为头号风险），单列 4 天、故意排在四个表格页热手之后。风险缓解策略是**先 Viewer 再编辑，分步推进**——Day 19 只要求"能把一份 BPMN XML 渲染成流程图"，别一上来就想拖拽。

- **阶段清单**：[`plan/P5-BPMN设计器.md`](./plan/P5-BPMN设计器.md)（Day 19 预览 → Day 20 编辑 + flowable 扩展 → Day 21 属性面板 → Day 22 整合部署）
- **现成弹药三样**（P5 第三节）：① 模板 `process-back/src/main/resources/processes/leave.bpmn20.xml` 含完整 DI 坐标，可直接喂 bpmn-js 渲染；② 后端 `activities()` 不过滤 `sequenceFlow`（P3 Day 14 决策 2），返回的连线 id 正好给路径高亮用——**Day 18 前端过滤掉的那批数据，在这里是有用的**；③ 部署接口 `POST /process-definition/deploy`（multipart）P3 就绪，`api/process.ts` 特意把 `deploy` 留空给本阶段接
- **本阶段不用改后端**（Day 22 部署直接用现成接口）
- 🆕 新东西：第一次装并用一个**重量级第三方库**（bpmn-js 18），第一次碰 moddle 扩展

**⚠️ P4 收尾时评估的四项还挂着**，进 P5 前建议先过一遍（下表），做不做都要给个结论，别就这么飘着。

**小尾巴清单**（各带处理时机）：

| 时机 | 项 |
|---|---|
| **进 P5 前过一遍** | **`STATUS_MAP` 仍是两份**（08-18 数过：`views/myStart/index.vue:170` + `componnets/TraceDialog.vue:61`，`views/instance` 那页没有）。**触发点「第三处 + 看着难受」尚未到**，按既定判据**这条应当继续挂着不动**；等 P5/P6 出现第三处再提到 `api/instance.ts` 旁边导出（它和 `InstanceStatus` 类型天然同源） |
| **进 P5 前过一遍** | **姓名批量查询三份重复**：`ProcessInstanceServiceImpl.enrich()` ①段、`FlowableTaskServiceImpl.enrich()` ②段、`FlowableHistoryServiceImpl.nameMap()`，逐行重复（userIds → nameMap，含两头转型 / 空集合短路 / realName 兜底三个坑）。Day 18 只在新类内部抽了私有 `nameMap()` 给两个 enrich 共用，跨类没动。收编清单：新建 `UserNameResolver`（`@Component` 注入 `SystemUserMapper`，暴露 `namesOf(Collection<Long>)`）→ 替换三处。详见 `process-back/process_history/08-Day18-已审批补字段与轨迹姓名.md` 第五节。⚠️ **P5 不碰后端**，这条不做就得推到 P8 |
| **进 P5 前过一遍** | **`request.get<never, X>` 的重复**（08-04 登记）：08-18 数过共 **20 处 / 6 文件**——`api/user.ts` 5、`api/instance.ts` 4、`api/process.ts` 4、`api/task.ts` 3、`api/auth.ts` 2、`api/history.ts` 2 —— **样本已够**，当初等的就是 Day 18 写完。根因是响应拦截器（`utils/request.ts:45`）拆了包而 axios 类型不知道。候选解法：`utils/request.ts` 末尾包一层 `http` 门面（`get: <T>(url, config) => request.get(url, config) as Promise<T>`）。📌 `api/process.ts:18` 那行注释掉的无泛型版是 Day 15 试过又换回来的，**别当成可用写法** |
| **进 P5 前过一遍** | **发起弹窗下拉未过滤挂起定义**（08-11 登记）：`loadDefs()` 拉全部定义，挂起的也能选，用户填完表单提交才被后端预检拦（`ProcessInstanceServiceImpl.java:68-69`）。后端兜底本来就该在，前端过滤/置灰属体验优化 |
| **随手** | `views/myStart/index.vue:306` 的 `.muted` 样式已无使用者（用它的 `<span>` 破折号 Day 18 删了），CSS 死代码编译器不报，删不删随意 |
| **Day 19 起长期有效** | ⚠️ **验收一律 `npm run build`，别跑 `npx vue-tsc --noEmit`** —— 根 `tsconfig.json` 是 `{"files": [], "references": [...]}`，`--noEmit` 走根配置**检查零个文件、永远 exit 0**。Day 18 有个 TS2322 就是被它假绿放过去的 |
| **长期有效** | ⚠️ **哑雷 `suspended`**（08-04 发现）：`ProcessInstanceVO.of(HistoricProcessInstance)`（`ProcessInstanceVO.java:77-90`）没 set `suspended`，Java `boolean` 默认 false ⇒ **凡走 `myStarted()`/HI 查询回来的实例，`suspended` 恒为 `false`**，TS 也查不出来。📌 范围仅限 `ProcessInstanceVO`（HI 路径）；`TaskVO.suspended` 是真的（`fill()` 里实打实写了，值来自 `ACT_RU_TASK.SUSPENSION_STATE_`），两者别混 |
| ~~已结案~~ | ~~div 包裹风格不一致~~ ✅ 08-14 决定不统一，本人怎么顺手怎么写，新页面不必对齐任何一页命名，不再复查 |

**学习支线**：

| 支线 | 进度 | 位置 |
|---|---|---|
| **泛型系列** | **🔸 2026-08-03 告一段落**：九篇已快速通读，不再作为主线阻塞项。已达 ② 档（自己写出等价 `common/Result.java`）。③ 档触发点 = 觉得 `toPage`/`toHiPage` 两份重复碍眼时，回 `09` + `10` 第 5 节做重构 | `process-back/deep_dive/泛型/`，进度表见 `待深入主题清单.md` 第 5 条 |
| hi_tables | 只剩 `05-外围四表.md` 收官 | `process-back/deep_dive/hi_tables/` |

---

## 二、当前数据库现场（flowable_db2）

**流程定义**：四个模板，全部 v1、**当前全部激活**。⚠️ 定义的挂起态会**跨会话残留**（`ACT_RE_PROCDEF.SUSPENSION_STATE_`，1 激活 / 2 挂起）：`leaveProcess` 曾因 Day 15 挂起实测遗留在挂起档，08-11 已 swagger 激活（`REV_` 随改动 +1，非 1 属正常）。以后发起报「已挂起」先查这列。

| 模板文件 | key | 名称 | 结构（三级） |
|---|---|---|---|
| `leave.bpmn20.xml` | `leaveProcess` | 请假流程 | 发起 → 经理审批[manager] → 结束　*（两级，原有）* |
| `reimburse.bpmn20.xml` | `reimburseProcess` | 报销流程 | 发起 → 经理审批[manager] → **财务审批[admin]** → 结束 |
| `purchase.bpmn20.xml` | `purchaseProcess` | 采购流程 | 发起 → 经理审批[manager] → **总监审批[admin]** → 结束 |
| `travel.bpmn20.xml` | `travelProcess` | 出差流程 | 发起 → 经理审批[manager] → **总监审批[admin]** → 结束 |

> ✅ `check-process-definitions` 是 `false`（`application.yml:33`）——启动不自动扫 `processes/`。要加模板走手动部署接口，或临时改回 `true` 重启一次再关掉。
> ⚠️ 后端改了代码需重启：`process-back` 下 `./mvnw spring-boot:run`，`http://localhost:9090/workflow/doc.html` 能开即可。

**实例样本：已造齐三态**（2026-08-11，「我的发起」页逐态验过）：进行中 / 已办结 / 已终止都有，其中含 08-03 发起、停在 `managerApprove` 的 leave 与 purchase 两条在途。具体条数以库为准（`SELECT ID_, BUSINESS_KEY_, PROC_DEF_ID_ FROM ACT_RU_EXECUTION WHERE PARENT_ID_ IS NULL` 看在途；HI 全量看 `ACT_HI_PROCINST`）。

> 💡 **样本现状**（2026-08-18 Day 18 实测后更新）：库里已有**一条走完全程的三级流程**（采购 08-03 发起 → 08-14 两级办完，轨迹四节点齐）、**一条被终止的**（请假 08-11）、以及在途行；manager 名下已审批 **4 条**。P5 只做设计器渲染与部署，**不依赖实例样本**，除非要验图上的路径高亮（那时用采购那条完整实例）。

账号（`sys_user.sql`）：

| 用户 | id | role | 在流程里的角色 |
|---|---|---|---|
| employee | 3 | `employee` | 发起人 |
| manager | 2 | `manager` | 一级审批（`candidateGroups="manager"`） |
| admin | 1 | `admin` | 二级审批（`candidateGroups="admin"`） |

密码均 `123456`。role 字面值与 BPMN 里的 `candidateGroups` 严格对应——待办查询走 `taskCandidateGroup(UserContext.role)`，改任一边都要同步改另一边。

> **本机环境踩坑**：带中文的请求体经 Git Bash 会以 GBK 发出触发后端 `Invalid UTF-8`，curl 测试用纯 ASCII 值绕过（浏览器走 UTF-8 无此问题）；`HTTP_PROXY=127.0.0.1:10808`，curl 打 localhost 要加 `--noproxy '*'`；context-path 是 `/workflow`。
>
> **办结 vs 终止的判据**：办结 `END_ACT_ID_=endEvent` + `DELETE_REASON_` NULL + 有 `endEvent` 行；终止则 `END_ACT_ID_` 空 + `DELETE_REASON_` 有值 + 无 `endEvent` 行。完整判据表见 `process-back/deep_dive/hi_tables/03` 第五节。另：`terminate` 清 RU **保** HI，而 `cascade=true` 删部署会 RU + HI **一并抹除**——前端删除二次确认拦的就是后者。

---

## 三、下次开场直接说（三条路，A 是当前默认）

### A · 回主线 —— P5 Day 19 设计器入口 + bpmn-js 预览（🔸 当前默认，**新阶段第一天**）

> "接着 `测试进度断点.md`，开工 P5 Day 19 bpmn-js 预览。"

**0. 开场先看**：`plan/P5-BPMN设计器.md`（四天清单 + 第三节「现成弹药」）、`页面思考法/00-总纲.md`（第五次走三步法）。⚠️ **P5 没有需求文档与原型**——前四页都有，这次没有，**开工第一件事是先把 Day 19 要做成什么样说清楚**，别直接动手。

**1. Day 19 的验收线故意压得很低**：把一份 BPMN XML 渲染成流程图，**就这一件事**。别在第一天碰拖拽、属性面板、保存。P5 的风险缓解策略写在排期第五节：**先 Viewer 再编辑，分步推进**。

**2. 起手三个决定**（开工前先答，答完再写代码）：

| 要决定的 | 备选 | 备注 |
|---|---|---|
| XML 从哪来 | ① 硬编码一段字符串塞进组件 ② 前端直接 `?raw` 导入一份本地 `.bpmn20.xml` ③ 从后端拉 | ⚠️ **08-18 查实：后端没有「取某定义 XML」的接口** —— `ProcessDefinitionController` 只有 `deploy` / `page` / `{id}/suspend` / `{id}/activate` / `delete/{deploymentId}` 五个。所以 ③ 需要新增后端接口（`RepositoryService.getProcessModel(definitionId)` 返回 `InputStream`），**与 P5「本阶段不用改后端」的前提冲突，见下方缺口提示**。Day 19 走 ① 或 ②，别为这个卡住 |
| 装哪几个包 | `bpmn-js` 起步；`bpmn-js-properties-panel` / `moddle` 是 Day 20–21 的事 | ⚠️ **08-18 查实：`package.json` 里还没有 bpmn-js**（当前只有 vue/router/pinia/axios/element-plus/sass 六个运行时依赖），Day 19 第一步就是 `npm i bpmn-js`。**只装这一个**，装多了出错时分不清是谁的问题 |
| 组件放哪 | `components/BpmnDesigner/` 下 `index.vue` + `BpmnViewer.vue` | 目录名沿用 P5 清单里写的；⚠️ 现有共享组件目录**拼写是 `src/componnets/`**（少个 e，Day 18 建的），新组件放进去要跟着这个拼写，或者趁现在只有一个文件时改名 |

> 🕳️ **08-18 查出的排期缺口（P5 开工前请自行定夺）**：`plan/P5` 写着「本阶段不用改后端」，依据是 deploy 接口已就绪 —— 这对 **Day 22 保存部署**成立，但**对「打开已有流程去改」不成立**：设计器要编辑库里已部署的定义，就得先把它的 XML 取回来，而这个接口不存在。三条出路：① P5 只做「新建 → 部署」，不做「打开已有」（**最省，Day 19–22 清单原样成立**）；② 顺手加一个 `GET /process-definition/{definitionId}/xml`（后端约 10 行，`RepositoryService.getProcessModel()`）；③ 推到 P8。**建议 ①**，理由是 P5 已经是全项目最难的一块，别再往里塞需求。

**3. 三个可预见的坑**（bpmn-js 是本项目第一个重量级第三方库）：

- **容器尺寸**：`new BpmnViewer({ container })` 挂的 DOM 必须**有确定高度**，`height: 100%` 在父元素没高度时算出 0，图渲染了但看不见——不报错，又一个"静默失效"；
- **CSS 必须手动引**：`bpmn-js/dist/assets/diagram-js.css` 与 `bpmn-font/dist/css/bpmn.css` 不引的话，图形能画出来但箭头/图标全乱；
- **生命周期**：Viewer 要在 `onMounted` 之后创建（容器得先在），组件卸载时 `viewer.destroy()`，否则切页面会泄漏。

**4. 老规矩**：`npm run build` 转绿再算完（⚠️ **不是 `vue-tsc --noEmit`**，那条命令在本项目检查零个文件，见小尾巴清单）。写页面按三步法：① 布局 → ② 组件 → ③ 样式。

**5. 收工回填**：`plan/P5` Day 19 勾选与打卡行 + 本文档「一」。**阶段状态总控已在 08-18 改过（P4 ✅ / P5 🔄），本阶段内不用再动总控。**

> 📌 实测出问题不要急着改代码，先照 `讲解规则.md` 第五节定位：分清是后端返回不对（knife4j 直接打接口）还是前端渲染不对（F12 看 Network / Elements）。bpmn-js 的行为**一律翻 `node_modules/bpmn-js/` 源码查证**，别凭记忆——这是本项目最陌生的一个库，记忆最不可靠。

### B · 先清 P4 收尾的四项欠账（半天，可选）

> "进 P5 之前，先把小尾巴清单里标『进 P5 前过一遍』的四条过一遍。"

四条见第一节表格。**建议至少给每条一个结论**（做 / 不做 / 推到 P8），别让它们一直飘着。其中 `UserNameResolver` 那条要注意：**P5 整个阶段不碰后端**，现在不做就得推到 P8。

### C · hi_tables 收官（学习支线）

> "接着写 `process-back/deep_dive/hi_tables/05`。"

按既定体例写最后一篇 `05-外围四表`（COMMENT / ATTACHMENT / IDENTITYLINK / DETAIL 速查合集），写完 hi_tables 支线收官。体例与进度见 `process-back/deep_dive/待深入主题清单.md` 第 3 条。

---

## 四、维护约定（防止本文档再次膨胀）

- **本文档只留三件事**：当前位置（一）、数据库现场（二）、下一步（三）。写完就该有人删掉过期的部分。
- **新产出/新决策** → 追加进 [`变更日志.md`](./变更日志.md)，不要往这里堆。
- **知识性发现** → 写进 `process-back/deep_dive/` 或 `process-front/前端答疑/`，这里最多留一行指针。
- **每日任务勾选与打卡** → 写进 `plan/P*.md`，不写这里。
- **阶段推进时**：把「一」整节重写成新阶段的现状，旧内容摘要一句进变更日志。
