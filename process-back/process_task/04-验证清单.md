# 04 · 验证清单

> 全部打勾 = Day 13 关闭。□ 未验 ｜ ☑ 通过 ｜ ⚠ 有问题（记录到 05 或排期文档）
>
> **2026-07-21 按 `06-重置后knife4j重测步骤.md` 全程实测，Day 13 已关闭。**
> 本轮现场：定义 `leaveProcess:1:e531deaa-…`（deploymentId `e4854af7-…`）；办结实例 test-0004 `175ffafb-…`（taskId `1764dc04-…`）；在途实例 test-0005 `6a897d3d-…`（留给 Day 14）。

## 编译与接口行为

- [x] `mvnw compile` BUILD SUCCESS
- [x] 「待办任务」分组出现在 knife4j，3 个端点齐全
- [x] `todo`：manager token 下看到「经理审批」，`assignee` 为 null、`createTime` 格式化正确（`2026-07-21 12:24:40`，非时间戳/ISO 串）
- [x] `detail`：返回 `TaskFormVO`，`variables` 含 `days`/`reason`/`initiator` 三项，`formKey` 为 null
  - 附：类型保真 —— `days` 返数字 `2`、`initiator` 返字符串 `"3"`，与 `ACT_RU_VARIABLE` 的 `TYPE_` 列一致，未被压成字符串（P6 表单渲染可直接用）
  - 附：三个变量在 `ACT_RU_VARIABLE` 里 `TASK_ID_` 均为空、只挂 `PROC_INST_ID_` —— 变量属于**实例**，但 `getVariables(taskId)` 沿执行树向上取，办理人照样拿得到
- [x] `complete`：返回 200，日志打出 `assignee=2`
- [x] 重复办理同一 taskId → 「待办任务不存在或已办理」
  - 说明：实测时传入的 id 带了多余前缀，走的是"id 不存在"入口；与"已办理"是**同一分支**（`getTaskOrThrow` 在 `ACT_RU_TASK` 查不到即抛），故文案写成「不存在**或**已办理」，功能等价
- [x] taskId 不存在 → 同上友好报错（实测 `…cc1d1`，返回 500 + 完整 id 回显）
- [x] taskId 缺失 → 400 参数校验错误（`@NotBlank` + `@Valid` 生效）
  - ⚠️ 踩坑记录：swagger UI 的**前端必填校验**会先拦下空字段，请求根本发不出去，等于没测后端；传字面量 `""`（两个引号字符）也不行，那是长度 2 的非空串，会穿到 `getTaskOrThrow` 报"不存在"。**正确造法是传一个空格 `" "`** —— `@NotBlank` 连纯空白都拦，这正是它与 `@NotNull` 的区别。实测返回 `{code:400, msg:"taskId 任务 id 不能为空"}`

## 权限与隔离（本块的安全点）

- [x] `todo`：**manager 1 条 / employee 0 条 / admin 0 条**（三行都对上才算数）
  - employee 是本单**发起人**却看不到 —— `todo` 问的是"谁该办"，发起人 ≠ 办理人（他走 `my-started`）
  - admin 也是 0 —— 引擎里**没有"管理员看得见一切"**，准入名单只有 `ACT_RU_IDENTITYLINK` 里那行 `GROUP_ID_='manager'`。将来要做"管理员总览待办"必须另开接口绕过候选组
  - manager 命中的是 `taskCandidateGroup(role)` 一支，非 `taskAssignee(userId)` 一支；`or()` 双条件目前只有前者起作用
  - 三次查询后四张 RU 表 `REV_` 全是 1 —— 查待办是纯读，不会隐式认领
- [x] 越权 `detail`：employee token + 泄漏的 taskId → 「你不在该任务的候选办理人范围内」
- [x] 越权 `complete`：employee token + 泄漏的 taskId → 同样被拒
  - 这两发是本块**唯一的安全测试**：步骤 1 里 employee 看到 0 条只证明"藏起来了"，接口本身是裸的、URL 拼出来就能调；`assertCanOperate` 才是"锁上了"
  - 顺带验出执行顺序：`getTaskOrThrow` 在前、`assertCanOperate` 在后 —— id 错时先报"不存在"，不泄漏"任务存在但你没权限"的信息
- [x] （可选）manager 认领后换另一个 manager 账号办 → 「该任务已被他人认领」（当前只有一个 manager 账号，**已跳过**，Day 30 补人再验）

## 数据库表现象

- [x] 办理后 `ACT_RU_TASK` / `ACT_RU_EXECUTION` / `ACT_RU_VARIABLE` / `ACT_RU_IDENTITYLINK` 该实例的行**全部消失**（四表 COUNT 全 0；办完即完结，不是 bug）
- [x] **`ACT_HI_TASKINST.ASSIGNEE_ = '2'`**（自动认领生效 —— **Day 14 的前提**）
- [x] `ACT_HI_TASKINST.END_TIME_` / `DURATION_` 有值（`14:48:29.993` / `8629374`ms ≈ 2h23m）
- [x] `ACT_HI_VARINST` 有 5 行：`days` / `reason` / `initiator` + 办理时新增的 `approved` / `comment`
  - 附：`approved` 的 `VAR_TYPE_='boolean'` 存进 `LONG_=1`（SQL Server 无原生 bool，引擎用 1/0），又一个多态类型列的例子
- [x] `ACT_HI_ACTINST` 轨迹完整，`END_TIME_` 都有值
  - ⚠️ **实际是 5 行不是 3 行**：`startEvent` / **`flow1`** / `managerApprove` / **`flow2`** / `endEvent` —— **连线（`ACT_TYPE_='sequenceFlow'`）也进轨迹**，且 `ACT_NAME_` 为空。影响 Day 14，已登记待决策（见排期 Day 14 条目）
  - 附：只有 `managerApprove` 有 `ASSIGNEE_`，其余节点与连线均为空 —— **只有人工节点才有办理人**。这正是「我的已审批」查 `ACT_HI_TASKINST`（只装 userTask）而非 `ACT_HI_ACTINST` 的原因
- [x] `ACT_HI_PROCINST` 该实例 `END_TIME_` 有值、`END_ACT_ID_='endEvent'`、**`DELETE_REASON_` 为 NULL**（正常办结）

## Day 12 欠账（已写未测的 terminate）→ **已于 2026-07-21 在 Day 12 实测中清零**

- [x] 终止一条新实例成功：`ACT_RU_*` 清空，`ACT_HI_PROCINST.DELETE_REASON_ = '测试终止'`（Day 12 步骤 4，终止 test-0002）
- [x] 对已终止的 id 再终止 → 「运行中的流程实例不存在」（Day 12 步骤 4）
- [x] **对照确认**：办结的那条 `DELETE_REASON_` 为 NULL，终止的那条有值 —— 两者靠这一列区分
  - 完整判据（本轮 test-0004 办结 vs Day 12 test-0002 终止）：<br>· 办结：`END_ACT_ID_='endEvent'` + `DELETE_REASON_` NULL + `ACT_HI_ACTINST` 有 `endEvent` 行<br>· 终止：`END_ACT_ID_` 空 + `DELETE_REASON_` 有值 + 无 `endEvent` 行<br>⇒ 前端历史列表的状态标签就靠这两列判

## Day 11 欠账（cascade 最后一半）→ **已于 2026-07-21 在 Day 12 实测中清零**

- [x] `cascade=true` → 部署/定义/在途实例/**历史** 全部级联清除（Day 12 步骤 5：三表 COUNT 全 0，连步骤 4 的终止留痕一并抹除）
- [x] 测试现场已恢复：`leave.bpmn20.xml` 重新部署，Day 14 有定义可发起（`06` 步骤 0，新 v1 `e531deaa-…`）

## 本轮额外发现（超出原清单）

- [x] **运行时不止四张表**：日志里出现 `ACT_RU_ACTINST`（运行时活动实例表，记录"当前正在经历哪些节点"），与 `ACT_HI_ACTINST` 是孪生表、同步更新（`updateActivityInstance` 与 `updateHistoricActivityInstance` 成对出现且改同一 ID），实例完结时 RU 那份删掉（`Updates: 3`）、HI 那份留下。之前深挖的"四表模型"漏了它
- [x] **claim 有独立留痕**：`insertComment … ACTION_='AddUserLink', MESSAGE_='2_|_assignee'` —— 认领动作被记进 `ACT_HI_COMMENT`（操作日志表，也不在四表模型里）
- [x] **部署会自动生成流程图 PNG**：`ACT_GE_BYTEARRAY` 是 **2 行**（上传的 XML `GENERATED_=0` + 引擎生成的 `leave.leaveProcess.png` `GENERATED_=1`）。这让 Day 10 挂起的「中文字体」标记具体化了 —— 该 PNG 里的中文可能是方框；但前端用 bpmn-js 自渲染、不读这张图，Day 31 复查大概率结论为"不用管"
- [x] **在途实例的轨迹形态**（test-0005，给 Day 14 预习）：3 行 `startEvent` / `flow1` / `managerApprove`，且 `managerApprove` 的 `END_TIME_` **为空** —— 未完结节点 `END_TIME_` 为空，正是前端画流程图时判断"高亮哪个节点为当前位置"的依据
- [x] ⚠️ **接口鉴权粒度缺口**（已登记 Day 31）：`deploy` 等管理类接口无角色校验，任何登录用户可调；`terminate`、用户 CRUD 同理
- [x] ⚠️ **业务异常状态码粒度**（已登记 Day 31）：越权返回 500，语义上应为 403；`BizException` 一律映射 500，前端无法按码分流

## 收尾

- [x] 05 排期文档：Day 13 任务打勾 + 打卡表填行；Day 12 打卡行已补完（terminate 已测、cascade 两分支已清）
- [x] 本目录 05 文档：过程中的疑问已记录（本轮疑问均已就地澄清并写入本清单）
- [x] **P3 进度**：Day 10/11/12/13 已关闭，只剩 Day 14（历史接口 + 整体冒烟）；Day 14 代码早已就位，缺的是实测
