# 04 · 验证清单

> 全部打勾 = Day 14 关闭 = **P3 关闭**。□ 未验 ｜ ☑ 通过 ｜ ⚠ 有问题（记录到 05 或排期文档）
>
> **2026-07-22 全程实测通过并关闭**（curl 双 token，见排期 Day 14 打卡）。落地两决策点：① `activities()` 加 `TRANSACTION_ORDER_` 二次排序；② 不过滤 `sequenceFlow`（前端过滤）。

## 编译与接口行为

- [x] `mvnw compile` BUILD SUCCESS
- [x] 「流程历史」分组出现在 knife4j，2 个端点齐全（`approved` / `instance/{id}/activities`）
- [x] `approved`：manager token 下返回办结任务，`assignee='2'`、`endTime` 有值、`durationInMillis` 是毫秒数、`deleteReason` 为 null
- [x] `activities`：返回节点列表，`startTime` 升序，时间字段格式化为 `yyyy-MM-dd HH:mm:ss`（不是时间戳）
- [x] `orderBy` 后带了 `.asc()/.desc()`（漏方向会抛异常，能跑起来即已满足）
- [x] ⚠→☑ **二次排序**：同事务撞车（test-0005/test-0006 三行同秒）经 `TRANSACTION_ORDER_` 二次排序后顺序稳定 `startEvent→flow1→managerApprove`

## 边跑边记（本块最该看透的一点）

- [x] **发起后立刻**查（在途 test-0005）：`ACT_HI_ACTINST` 里该实例行已存在（HI 边跑边写，01 文档②）
- [x] **对运行中实例**查 `activities`：`startEvent` 有 `endTime`、`managerApprove` 的 `endTime` 为 **null**（当前停留节点）——test-0005 实测
- [x] **办完后**再查 `activities`：`startEvent → managerApprove → endEvent`（含 flow1/flow2 共 5 行），`endTime` 全部有值——test-0006 实测

## 权限与隔离

- [x] `approved`：**manager ≥1 条 / employee 0 条 / admin 0 条**（三行都对上——证明 `taskAssignee(me)` 真按人分）
- [x] `activities`：任意 token 都能查（employee 查 test-0006 得 200/5 行；本块读接口不做越权校验，Demo 预期行为，01 文档⑦）

## 状态判定（完成 vs 终止，回收 Day 13⑨）

- [x] 办完的实例（test-0006）：`activities` 走到 `endEvent`、`deleteReason=null`
- [x] 终止的实例（test-0007，reason=test-terminate）：`activities` 停在 `managerApprove`、**没有** `endEvent`（没走到结束就被终止）
- [x] 终止后 RU 清空（`process-instance/page` 里 test-0007 消失），HI 轨迹仍可查

## P3 整体冒烟（双 token 完整生命周期）

- [x] employee 发起 → manager 待办 → 办理 → 各查历史，全程无阻断
- [x] 亲眼确认数据流：`ACT_RU_*`（发起后 `page`/`todo` 有）→ 办理后 test-0006 从 RU `page` 消失 → `approved`/`activities` 接住
- [x] 对照：`approved` 从 1 条（test-0004）变 2 条（+test-0006）

## 收尾

- [x] 05 排期文档：Day 14 任务打勾 + 打卡表填行；**阶段总览 P3 标 ✅、P3 关闭**
- [x] 本目录 05 文档：过程中的疑问已记录（test-0004 name 乱码 = 遗留脏数据非 bug；curl 中文 GBK 编码坑）
- [x] **P3 完结确认**：Day 10/11/12/13/14 全部关闭，四个引擎门面（Repository/Runtime/Task/History）各就位；下一步进 P4 · Day 15 流程管理页
- [x] 顺手清账：Day 12 `terminate`、Day 11 `cascade` 两分支已于 2026-07-21 补清（见 process_instance/04）；本轮 test-0007 再验一次终止分支的历史轨迹
