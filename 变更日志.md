# 变更日志

> **性质：只追加、开场不读。** 每轮会话的顺带产出、配置改动、新登记的决策与欠账都往这里堆，让 [`测试进度断点.md`](./测试进度断点.md) 保持精简。
> 需要回顾「这周做了啥」时才翻本文档；定位当前进度**永远看断点文档**。
> **按日期倒序，最新的在最上面。**

---

## 2026-08-17 ~ 08-18 · **Day 18 关闭 ⇒ P4 关闭**：已审批页 + 轨迹弹窗浏览器实测全过

**登录 → 发起 → 审批 → 历史，全程界面可走。** P4 四个页面（流程管理 / 实例·我的发起 / 待办 / 已审批·轨迹）全部本人从零写成，下一站 P5 BPMN 设计器。

### 一、代码产出

| 端 | 提交 | 内容 |
|---|---|---|
| 后端 | `ecf4ea9`→`12f1ddc`（08-17） | `ApprovedTaskVO` 补 `processDefinitionName`/`businessKey`/`startUserName`；`ActivityTraceVO` 补 `assigneeName`；`FlowableHistoryServiceImpl` 加 `enrich()` + `enrichAssigneeName()` + 私有 `nameMap()` |
| 前端 | `ac924ad`→`c351901`（08-17~18，7 笔） | `api/history.ts` + `utils/format.ts` + `componnets/TraceDialog.vue` + `views/approved/index.vue` + 路由菜单各 1 行 |
| 前端 | `e4dfd64`→`1265252`（08-18，3 笔） | 对账查出的三处问题当天修完（见下） |

**决策②「补后端」这一步的价值当场兑现**：已审批列表四行的「流程名称 / 业务标识 / 发起人」全部有值。之所以有值，是因为 `enrich()` 走的是 `historyService.createHistoricProcessInstanceQuery()` —— 那四条实例里至少三条已办结，`ACT_RU_EXECUTION` 里早没了行，照抄 Day 17 的 `RuntimeService` 版本会得到整页 null（**第三次静默失效**，这次靠开工前的警告避开了）。

### 二、对账查出三处问题，当天清零

| # | 问题 | 症状 | 修法 |
|---|---|---|---|
| 1 | `api/history.ts` 轨迹 URL 尾部多一个 `}` —— `` `/history/instance/${instanceId}/activities}` `` | 接口必 404，弹窗恒空 | `e4dfd64` 删掉 |
| 2 | `layout/index.vue` 菜单项 `{index:'approved'}` 缺前导斜杠 | **点击会高亮、刷新不高亮**（详见下节） | `5ca5b02` 改 `/approved` |
| 3 | `openTrace(row: ApprovedTask)` 类型标错（myStart 页的行是 `ProcessInstance`） | `npm run build` 报 TS2322 | `1265252` 改类型 + 删多余 import |

### 三、两条工程性发现（比上面三个 bug 更值得记）

**① `npx vue-tsc --noEmit` 在本项目是无效检查，永远 exit 0。**

根 `tsconfig.json` 是 `{"files": [], "references": [...]}` 的 project-references 结构，`--noEmit` 走根配置 ⇒ **检查零个文件**。真正的类型检查必须走 `npm run build`（即 `vue-tsc -b && vite build`）或 `npx vue-tsc -b`。上面第 3 个类型错误就是先用 `--noEmit` 假绿、换 `-b --force` 才暴露的。**以后验收一律 `npm run build`，别图快跑 `--noEmit`。**

**② `el-menu` 的 router 模式有两条互不相干的高亮路径**（源码 `node_modules/element-plus/es/components/menu/src/menu.mjs`，已翻证）：

- **点击路径**（`handleMenuItemClick`，196–212 行）：`router.push(route).then(res => { if (!res) activeIndex.value = index })` —— **根本不比对 `$route.path`**，把被点项的 `index` 原样赋给 `activeIndex`。所以 `'approved'`（无斜杠）点着也会亮，一测就"没问题"。
- **路由驱动路径**（`watch(() => props.defaultActive)`，268 行 → `updateActiveIndex`，219–221 行）：拿 `$route.path`（`/approved`）去 `items` 里查，查不到 ⇒ `?? val` 兜底成 `/approved`，与菜单项的 `'approved'` 对不上 ⇒ **不亮**。

⇒ **判据：这类"点着好像也行"的现象，验证方法是刷新页面（F5）或地址栏直接输入路径**，把 click 路径排除掉再看。这一条与 08-11 `.muted` 样式、08-12 `enrich()` 漏挂同属一类——**表面正常、路径没走全**。

### 四、浏览器实测记录（四条路径全覆盖）

| 验收点 | 结果 |
|---|---|
| 已审批列表（manager 登录，4 行） | 流程名称 / 业务标识 / 发起人三列全有值；耗时列「10 天 17 小时」「8 天」「2 天 12 小时」，`formatDuration` 的天/时分支都走到 |
| 已办结轨迹（采购流程，4 节点） | 发起 → 经理审批**王主管** → 总监审批**系統管理員** → 结束。**姓名回填 ✅、无 `sequenceFlow` 混入 ✅**；首尾节点与相邻节点时间戳相同却顺序正确 ⇒ `thenComparing(getTransactionOrder)` 的二次排序兜底生效 |
| 已终止轨迹（请假流程，2 节点） | 最后一个节点**红色** ✅（`status==='TERMINATED' && i===length-1`）。「经理审批」**无姓名是正确的**——实例被终止，该任务从未被认领，`ASSIGNEE_` 为 null |
| 在途轨迹（myStart 页） | 绿色实心 + **蓝色空心圈** ✅（`hollow="n.endTime===null"`），耗时那半截被 `v-if` 挡住 |
| 两个入口的传参差异 | myStart 传 `row.id`（行即实例）、已审批传 `row.processInstanceId`（行是任务）—— 两边都开得出轨迹；已审批页顶部**只有「流程」没有「状态」行**（`ApprovedTaskVO` 无 status 字段，故意不传），符合预期 |

### 五、新观察一条（未登记为挂起项，留个记录）

**终止实例的末节点耗时语义歧义**：请假流程终止那条显示「经理审批 · 耗时 19 秒」，这 19 秒不是"审批花了 19 秒"，而是"该节点存活 19 秒后被 terminate 抹掉"——终止时引擎给未完成的活动实例补写 `END_TIME_`，于是它有 endTime、显示成实心节点、也算得出 duration。**显示不算错，只是读起来容易误解**。真要区分得读 `DELETE_REASON_`，与挂起项 #7（审批意见）同属"HI 表信息够不够还原语义"的问题，届时若做一并考虑。

### 六、Day 16 欠账已清

myStart 操作列补上「轨迹」按钮（每行都有，撤回仍只给进行中的行），`<span v-else class="muted">-</span>` 占位破折号已删。⚠️ 残留：`views/myStart/index.vue:306` 的 `.muted` 样式规则现在没有使用者了（CSS 死代码，编译器不提醒），删不删随意。

---

## 2026-08-14（晚·续 2） · Day 18 前端落地四篇成文；本 Day 有四个「第一次」

`process-front/process_history/` 下新增 `01-页面定位与修改点.md`、`组件选型解析.md`、`02-操作步骤.md`、`03-完整代码.md`，与已有的 `00-需求文档.md` + 原型凑齐六篇，体例照 Day 17 的 `process_task`。

**6 处改动**：`api/history.ts`（新建）、`utils/format.ts`（新建）、`components/TraceDialog.vue`（新建）、`views/approved/index.vue`（新建）、`router` + `layout` 各 1 行、`views/myStart/index.vue` 改操作列。

### 这一天真正的新东西：四个「第一次」

前三页都是"又一个表格页"，Day 18 不是——四个没做过的东西各来一遍：

| 第一次 | 内容 | 为什么现在才出现 |
|---|---|---|
| **抽共享组件** | 轨迹弹窗被「我的发起」和「我的已审批」两页复用 | 前三页每段 UI 只有一个调用方，抽了是浪费；这次是**两个真实调用方**，不抽就得写两遍 |
| **自己实现 `v-model`** | `defineModel<boolean>({required: true})`（Vue 3.4+ 稳定，本项目 3.5.39） | 以前 `v-model` 都是往 EP 组件上挂，这次反过来——**你的组件要能被挂** |
| **接非分页接口** | `getActivities` 返回 `ActivityTrace[]`，不是 `PageResult` | 前三页的 GET 全是分页；照抄会写成 `PageResult<ActivityTrace>`，**TS 不报错**、运行时 `.records` 是 undefined |
| **写 `utils/` 工具函数** | `formatDuration(ms)`：毫秒 → 「3 天 6 小时」 | 第一次出现"这段逻辑不属于任何一个页面"（两处都要用），`utils/` 之前只有 `request.ts` |

### 两个组件设计上的判断（写进 `组件选型解析.md`）

- **为什么是 `el-timeline` 不是 `el-steps`**：`el-steps` 的模型是**固定步骤集合 + 一个 `active` 指针**，而轨迹是**条数不定的历史记录**（同一流程被终止和走完，行数不一样；将来加了网关还可能同节点出现两次），`active` 索引模型直接崩。且每行要挂三段副文本，`el-steps` 的 `description` 只有一行。
- **组件自己拉数据，而不是父传 `items`**：判据不是"哪种更纯"，而是**重复落在谁那里**——两个父页面要的是同一个接口的同一份数据，让组件自己拿，`loading`/`catch` 只写一份；若做成纯展示组件，这套要在两个父页面各写一遍，**抽组件的收益就被搬到外面抵消了**。

📌 `el-timeline-item` 的 props 全表（含关键的 **`hollow`** 空心圆点）是翻 `node_modules/element-plus/es/components/timeline/src/timeline-item.mjs` 抄的，不是凭印象——`hollow` 在官网示例里没单独一节，靠记忆写不出来。

### 顺带登记一条新的重复

`STATUS_MAP`（三态 → 文案 + tag 类型）现在有两份（myStart、TraceDialog）。**第二份不算难受，登记为 P4 收尾评估**；若出现第三处，就提到 `api/instance.ts` 旁边导出（它与 `InstanceStatus` 类型天然同源）。判据与后端 `nameMap` 那条一致：**「第三处 + 看着难受」才动手，不是「写第二遍」**。

---

## 2026-08-14（晚·续） · Day 18 后端落地三篇成文；`assignee` 未必是数字（新防御）

`process-back/process_history/` 下新增 `08`（原理/改动清单）、`09`（操作步骤，自己写）、`10`（完整代码，参考答案），体例照 Day 17 的 `process_task/07-08-09`。改动共 3 个文件、接口签名零变化。

### 本次的核心判断只有一个：**Day 17 的 `enrich()` 不能照抄**

| | Day 17（待办） | Day 18（已审批） |
|---|---|---|
| 查实例用 | `runtimeService.createProcessInstanceQuery()` | **`historyService.createHistoricProcessInstanceQuery()`** |
| 为什么 | 待办任务必属在途实例，RU 里一定有 | **我批完的任务，其实例多半已办结，那行早已从 `ACT_RU_EXECUTION` 删掉** |
| 照抄的后果 | —— | 整页三字段恒 null，**编译全绿、不报错** ⇒ 本项目第三次同类静默失效 |

API 已 `javap` 打 `flowable-engine-7.1.0.jar` 查证：`HistoricProcessInstanceQuery.processInstanceIds(Set<String>)` 存在，参数是 **`Set`** 不是 `List`。

### 顺带查出一个 Day 16/17 都没有的新坑：`ASSIGNEE_` 未必是数字

Day 16 的注释写过「`Long.valueOf` 不设防**是有前提的**——`START_USER_ID_` 唯一写入口是 `setAuthenticatedUserId(userId)`，恒为数字」。**轨迹的 `ASSIGNEE_` 没有这个前提**：除了 `claim()` 写的数字 userId，还有第二个写入口——**BPMN 里静态写死的 `flowable:assignee`**，而我们四个模板的注释里就白纸黑字写着「若想固定指派某个人，可改为 `flowable:assignee="具体用户名"`」（`leave.bpmn20.xml:7` 等四处）。照那句话改一次模板，`Long.valueOf` 当场 `NumberFormatException`，**整个轨迹接口 500**。

⇒ 轨迹的姓名回填里加一道纯数字过滤（`s.chars().allMatch(Character::isDigit)`），成本一行，最坏结果降级成 `assigneeName` 为 null、前端回退显示原值。

### 「第三处重复」的触发点到了 —— 评估后决定推迟

Day 17 的 `07` 文档预埋过：「Day 18 已审批页如果还要第三份，届时再收编成一个公共的姓名批量服务」。现在正是第三处（算上轨迹是第四处）。**结论：本次只在 `FlowableHistoryServiceImpl` 内部抽私有 `nameMap()` 给两个 enrich 共用，跨类收编推到 P4 收尾。**

理由三条：① Day 16/17 的代码 08-11、08-14 刚验收关闭，**收官日不翻修已验收代码**；② 跨类收编要先定放哪层（util 静态？`@Component`？），是个设计问题，不该塞进"补字段"任务；③ 原判据是「第三处要用**且重复看着难受了**」——第一个条件满足了，第二个由本人说了算。收编清单（新建 `UserNameResolver` + 替换三处）已登记进断点文档小尾巴清单「P4 收尾时评估」。

---

## 2026-08-14（晚） · Day 18 需求与原型定稿；查出「审批意见拿不到」的硬缺口（新挂起项 #7）

Day 17 关掉后直接进 Day 18 的需求评审，产出两份：`process-front/process_history/00-需求文档.md`（✅ 已确认）+ `原型-已审批与轨迹.html`。

### 四个决策（均采纳推荐）

| # | 决策 | 结论 | 影响 |
|---|---|---|---|
| ① | 轨迹用什么形态承载 | **A · `el-dialog` 弹窗** | 两个入口（我的发起 / 我的已审批）共用一个弹窗组件，**不新增路由与菜单**。⇒ P4 清单里字面写的 `views/history` **不作为页面存在**，措辞已改 |
| ② | 已审批列表缺的三字段补不补后端 | **B · 补** | **Day 18 从纯前端变双端**，约 +0.3 块 |
| ③ | 轨迹用什么控件 | **A · `el-timeline`** | 本项目第一次用；实心圆点=已完成、空心=当前停留、红色=流程在此终止 |
| ④ | 已审批页也放轨迹入口 | **要** | 审批人回看"我批过的这单后来批了吗"，除此无入口 |

### 评审中查出的两个"画原型前必须知道"

**1. 「全站历史实例检索页」没有后端接口**（这也是决策① 的真实起因）。三个列表接口各自锁死了口径：`/process-instance/page` 查 RU（在途）、`/process-instance/my-started` 查 HI 但锁 `startedBy(我)`、`/history/approved` 查 HI 任务但锁 `taskAssignee(我)`。**没有"查全部历史实例"这条路**，所以 P4 清单里那个独立「历史页」点进去会是空的——轨迹只能挂在已有列表的行上。

**2. ⚠️ 审批意见（comment）无法按节点还原 —— 已登记为挂起项 #7**（三处齐：P4 Day 18 条目 + `05` 第四节总表 + P8 第三节第 7 条）。

两件事叠加：

- Day 17 办理走 `taskService.complete(taskId, variables)`，写的是**实例级**变量 ⇒ manager 与 admin 的 `comment` 是**同一个键**，后者直接覆盖前者；
- `history-level: audit`（`application.yml:53`）只把变量**当前值**写进 `ACT_HI_VARINST`，**变更明细表 `ACT_HI_DETAIL` 不写**（那要 `full`）。

⇒ **一条两级审批的实例最终只剩最后一句意见，且挂在实例上、不挂在节点上，无法还原"哪句是哪个节点填的"。** 所以 Day 18 的轨迹**只展示「节点 + 办理人 + 时间 + 耗时」，不展示意见**——这是查过代码的结论，不是漏画。

三条出路（task-local 变量 / `history-level: full` / 改用 `addComment()` 走 `ACT_HI_COMMENT`）都要动刚验收完的办理链路，推到 Day 31，且与挂起项 #6「真拒绝」有耦合（真拒绝落地时，拒绝理由必须能追溯到节点），届时一并做。

> 🔬 **本条结论是从代码推的，开工前要实测一次**（《讲解规则》第五节）：`SELECT * FROM ACT_HI_VARINST WHERE PROC_INST_ID_ = '<Day 17 验收那条走完两级的实例 id>'`，预期 `comment` 只有一行。不符则结论作废。

### 顺带记下的三个"别照抄"

- **后端 `enrich()` 这次要查 HI 不查 RU**：Day 17 用 `RuntimeService` 是因为待办任务的实例必然在途；**已审批对应的实例大概率已办结，`ACT_RU_EXECUTION` 里没有那行**，照抄 = 整页 null = 第三次"编译全绿、字段恒 null"的静默失效。正解 `historyService.createHistoricProcessInstanceQuery().processInstanceIds(ids).list()`。
- **轨迹接口返回的是数组不是分页**，`request.get<never, ActivityTrace[]>`，别套 `PageResult`。
- **轨迹排序前端不要再排**：`START_TIME_` 毫秒级会撞车，后端已用 `TRANSACTION_ORDER_` 做过二次排序兜底（`FlowableHistoryServiceImpl.java:49-56`），照返回顺序渲染即可。

---

## 2026-08-14（下午） · **Day 17 关闭**：两级流转浏览器实测通过

manager 登录 → 待办页看到待办 → 界面点「办理」办掉**一条三级流程的一级** → **切 admin 登录，`adminApprove` 的待办确实出现** → admin 点同意办掉。**界面办理 ✅ + 两级流转 ✅，Day 17 关闭，P4 只剩 Day 18。**

这一步验的东西比"按钮能点"多得多，四条一次性反证：

| 验到的 | 靠什么反证的 |
|---|---|
| 后端待办查询的**过滤方向**对 | manager 查到的是 `managerApprove`、admin 查到的是 `adminApprove` —— `taskCandidateGroup(UserContext.role)` 没接反 |
| BPMN 的 `candidateGroups` 与 `sys_user.role` **字面值对得上** | 换个账号就换一批待办；对不上的话 admin 那步会是空列表 |
| 流程**真的往前走了一个节点**，不是"办理成功但直接结束" | 只有三级流程办完一级还剩一条待办，这是**唯一**能在界面上区分两者的现象。（若当初拿两级的请假流程验，办完就结束，看到的也是"提示成功"，但什么都没证明） |
| Day 17 后端补的三字段在真实页面上渲染正常 | 列表里流程名/业务标识/发起人三列有值，不是 `-` |

📌 **样本状态随之变化**（Day 18 直接受益）：库里已有一条走完全程的实例，`ACT_HI_TASKINST` 里 manager、admin 各留下一条办结记录 —— **Day 18 的「我的已审批」两个账号都有数据，轨迹页也有一条完整链路（发起 → managerApprove → adminApprove → endEvent），不用另造样本**。断点文档第二节的样本提示已同步改写。

---

## 2026-08-13 ~ 08-14 · Day 17 待办页**前端代码完工**（`api/task.ts` + `views/task`），build 转绿

> 本条为 08-14 进度对账（指令码 #1）补记：**文档停在 08-12，代码已跑到 08-14**，Day 17 前端不再是「待开工」。

**三步法第三次走通，仍是本人从零写、CLI 只答疑。** 八个提交 `c081d70`→`901aaf8`：

| 日期 | 提交 | 内容 |
|---|---|---|
| 08-12 | `c081d70` | ① 布局：路由 `/task`（`router/index.ts:45-47`）+ 菜单「待办任务」（`layout/index.vue:28`）+ 页面骨架 |
| 08-13 | `dc0486d` `82ab541` | `api/task.ts`（43 行）。**同时把 08-12 建的 `views/Task/`（大写 T）删掉换成 `views/task/`** —— Windows 文件系统大小写不敏感，本地看不出问题，但 import 路径与目录名不一致在 Linux/CI 上会直接构建失败，早改早好 |
| 08-13 | `a5713ea` `0fff33d` `5780f8d` `a626842` | 页面主体：表格 5 列 + 分页 + `loadList`/`handlePageChange`/`onMounted` |
| 08-14 | `901aaf8` | **办理弹窗**（`:48-84`，+31 行）：`el-descriptions` 只读区 + 审批意见 textarea + `handleSubmit` |

**产出**：`api/task.ts` 43 行（3 函数 `pageTodo`/`getTaskDetail`/`completeTask` + 3 类型 `Task`/`TaskForm`/`CompleteTaskRequest`）、`views/task/index.vue` 187 行。`npm run build`（含 `vue-tsc -b`）**08-14 实跑转绿**。

**断点文档列的六条约束，逐条对照都写对了**：

| 约束 | 页面里的写法 |
|---|---|
| `variables` 可能缺键，别假设某键存在 | `computed` 里 `Object.entries(detail.value.variables ?? {})` 遍历渲染（`:107-109`、`:64`），键有几个渲染几个 |
| `assignee` 未认领为 `null` | 直接**不放这一列**（比 `—` 占位更干净） |
| `POST /task/complete`，taskId 在 body | `completeTask({taskId, variables})`（`api/task.ts:42-44`），没写成 `/task/{id}/complete` |
| 弹窗体例照 myStart 复用 | `:close-on-click-modal="false"` + `:close-on-press-escape="!submitting"` + **`:show-close="!submitting"`**（右上角 X 那条坑没重踩）、`submitting` 双向锁按钮 |
| 竞态控件禁用 | 分页器 `:disabled="loading"`（`:43`） |
| 删末页回退 | `handleSubmit` 里 `list.length === 1 && current > 1 → current -= 1`（`:123-125`），沿用实例页的处理 |

**同日对账查出的两笔，当天就地了结（08-14）：**

### 1. 「办理」按钮连点已修（CLI 代改，本页唯一一处）

`openHandle` 里 `await getTaskDetail(row.id)` 期间按钮可连点，会发多次详情请求。后果比列表分页那种「静默错位」轻——最后一个响应覆盖 `detail`，弹窗内容仍对——但请求是白发的。

改法：加 `openingId = ref<string | null>(null)`，**用行 id 而不是 boolean**，模板上才分得清两种状态：

```
:loading="openingId === row.id"                        // 被点的这一行：转圈
:disabled="openingId !== null && openingId !== row.id" // 其余行：禁用
```

函数体首行 `if (openingId.value !== null) return` 兜底，`finally` 里复位（`catch` 里的 `return` 也会先走 `finally`，不会漏放）。

📌 **源码交叉验证（`讲解规则.md` 第五节）**：`el-button` 的 `loading` **本身就管点击**——`element-plus/es/components/button/src/use-button.mjs:31-32` 把 `_disabled.value || props.loading` 写进原生 `disabled`，`:50` 的 `handleClick` 里还有一道 `if (_disabled.value || props.loading) return` 拦着不 emit。所以**被点的那一行光挂 `:loading` 就已经点不动了**；`:disabled` 那半句是为**同页其他行**加的——它们各是独立按钮，不会因为别人在 loading 就自动锁上。这与 `请求竞态与控件禁用.md` 是同一条主线：`await` 治不了并发点击，只有禁用控件治得了。

### 2. div 类名不统一 —— ✅ 结案：不统一

本页用 `.task-page` > `.top` / `.middle` / `.bottom`，实例页/myStart 用 `.toolbar` / `.table-wrap` / `.pager-wrap`，`views/process/index.vue` 干脆裸放。**2026-08-14 决定：保持现状，以后新页面也不必对齐任何一页的命名。** 理由：结构上三者都是「外框 + 三条带」，只有叫法不同；样式全 `scoped` 不会互相干扰；类名是给自己读的，本人认哪个就用哪个，统一它不产生功能收益。**断点文档那条挂了两个月的「项目完工后统一 div 风格」就此划掉，不再复查。**

**顺带补记 08-12 后端的一笔**：`FlowableTaskServiceImpl.java` 那行无用 `import ...Autowired` **已删**（`27fa849`，08-12 15:20），当时没回写文档。全项目搜 `enrich(` 复核仍是 3 行（定义 `:115` + 调用 `:63`、`:159`），两处挂载都在。

**验收已于同日下午通过，Day 17 关闭**（见上一条目）。按钮连点那笔已提交 `2503640`（`front 防重`）。

---

## 2026-08-12 · Day 17 后端小改关闭；两处「静默失效」；新增指令码机制（跨机器）

### Day 17 后端（决策①）落地并 knife4j 实测关闭

按 [`process-back/process_task/09-Day17-完整代码.md`](./process-back/process_task/09-Day17-完整代码.md) 改 2 个文件，提交 `b25c356`→`e141dab`（三笔：初版 + 两轮修补）。

- `TaskVO` 补 `processDefinitionName` / `businessKey` / `startUserName`。`TaskFormVO extends TaskVO` 自动继承，**详情接口出参零改动**。
- `FlowableTaskServiceImpl` 注入 `RuntimeService` + `SystemUserMapper`，新增 `enrich()`，在 `toPage()`（`:159`）与 `detail()`（`:63`）**两处**挂载。`FlowableTaskService` 接口 / `TaskController` / `CompleteTaskRequest` 未动。
- `enrich()` 方法体一次写对，四个坑全避开：整页固定 **2 次**查询（`processInstanceIds` 一把捞实例 + `userIds` 去重后查 `sys_user`）、`userIds.isEmpty()` 短路避开 MP 的 `IN ()` 非法 SQL、`real_name` 为空回退 `username` 避开 `Collectors.toMap` 的 NPE、`instance == null` 防御式 `continue`。
- **实测**（manager token）：`GET /task/todo` 3 条待办三字段全有值；`GET /task/{taskId}` 三字段 + `variables` 齐。

### ⚠️ 两处静默失效（本条是重点）

两个 bug **都能编译通过**（`mvnw compile` exit 0），跑起来的表现是「三个新字段恒 null」——不报错、不警告，等前端页面写好看到三列空白时，第一反应会误判成前端渲染问题。

| # | 位置 | 错法 | 正解 |
|---|---|---|---|
| 1 | `detail()` | `enrich(List.of(vo));` 之后 `return TaskFormVO.of(task, variables);` —— **又造了一个新对象**，enrich 原地填好的 `vo` 被丢弃等 GC | `return vo;` |
| 2 | `toPage()` | `setRecords(...)` 之后直接 `return page;`，**漏挂 `enrich()`** | 补 `enrich(page.getRecords());` |

📌 **自查手段（下次照做）**：全项目搜 `enrich(`，**应为 3 行 = 1 处定义 + 2 处调用**。出事时只有 2 行，一数就露馅。这条 `09` 文档验收清单第三条本就写了，当时跳过没做；`toPage()` 的方法注释里也写着「漏掉不会编译报错，只会让三个新字段整页全空」，那段注释自己写的时候没抄进去，坑就照踩。

**与 08-11 `.muted` 样式两次静默失效同类**：编译器与类型系统都管不到的「填了没用上 / 该调没调」，只能靠**可数的自查断言**（数调用点、看编译产物里的选择器）兜底。

### 三个顺带确认的点

- **`variables` 可能缺键**：leave 实例详情只回了 `reason` + `initiator`，**没有 `days`** —— 发起时该字段为空，被 myStart 弹窗的 `.trim() || undefined` 在 `JSON.stringify` 时整个键抹掉了（那正是当初想要的效果）。**Day 17 弹窗只读区渲染 `variables` 不能假设某个键一定存在。**
- **`assignee: null` 不是 bug**：候选组任务被认领前 `ASSIGNEE_` 就是空的，`complete()` 里「未认领则自动认领」正为它准备。
- **`initiator: "3"` 与 `startUserName: "李員工"` 互相印证**：一个是发起时写进流程变量的 userId，一个是 `enrich()` 从 `START_USER_ID_` 查 `sys_user` 换来的姓名，两条独立链路指向同一人 ⇒ enrich 的查库方向与两头 `String`↔`Long` 转型都没接错。

### `suspended` 哑雷的范围澄清（订正断点文档旧表述）

旧表述是「Day 17 待办页 / Day 18 历史页别读 `suspended`」，容易被读成「这字段到处都假」。**实际哑雷只在 `ProcessInstanceVO`（HI 路径，工厂方法没 set）**；`TaskVO.suspended` 是真的，`fill()` 里写了 `vo.setSuspended(task.isSuspended())`，值来自 `ACT_RU_TASK.SUSPENSION_STATE_`。**待办页不读它的理由是「用不上」，不是「它假」。** 断点文档小尾巴清单该行已加 📌 澄清，时机也从「Day 17 前必看」改为「Day 18 前必看」。

### 新增：指令码机制（`指令码.md` + `CLAUDE.md`）

一句固定触发语 = 一套固定动作，**首条是「进度定位」**：说出触发语 ⇒ CLI 先 `git pull` 三个仓库（脏工作区停下报告，禁止 stash/reset/强拉），再读 `05-重写排期计划.md` + `测试进度断点.md` 并**对照前后端实际代码交叉核对**，最后输出「位置 / 文档与代码是否一致 / 下一步」。

**为什么写进仓库而不是 CLI 的本地记忆**：本地记忆是**单机**的，换台电脑就是空白上下文；本文件随 `process-docs` 提交，clone 到哪台机器都在。落成三层冗余：`CLAUDE.md`（自动加载，主路径）+ `05`/`测试进度断点` 两篇**顶部横幅**（触发语点名了这两个文件，必被读到，是保底路径）+ `指令码.md`（权威定义，将来加 #2 #3 有固定位置）+ README 两处指针。

> 📌 平时约定是「问进度直接读断点文档、别扫代码核对」；**指令码 #1 明确要求交叉核对**，那里以指令码为准。两条规则的适用边界已在两个文件里各写一句。

---

## 2026-08-11（晚） · Day 16 关闭：造样本 + 浏览器实测全过；「定义挂起」拦截排查 + businessKey 不查重结论

**Day 16 正式关闭**（当天最晚一笔，压在同日两条目之上）。造样本 + 浏览器实测四条验收全过：发起后立刻可见（`status.value=''` 重置生效）；三态 tag + 当前节点渲染正确；**办结/终止实例仍留在「我的发起」列表**（改查 HI 的目标达成）；`startUserName` 姓名、`currentActivityName` 有值。状态下拉三态筛选逐个正确 ⇒ 顺带关闭 P4 那条「knife4j 后端验收」（下拉走的就是 `my-started?status=` 同一条接口，`applyStatusFilter()` 实测通过，不必再单独打 knife4j）。上一条目遗留的操作列改动已随下午收尾七笔（`575c68c`→`e96c1e1`，弹窗细节 + css）入库，工作区干净。

### 实测撞上的拦截：「流程定义已挂起，无法发起：leaveProcess」

- **现象**：发起弹窗选请假流程提交被拦，后端 WARN `业务异常: 流程定义已挂起`。
- **根因**：`ACT_RE_PROCDEF` 里 `leaveProcess` 行 `SUSPENSION_STATE_=2`（挂起；1=激活）、`REV_=2`（部署后被改过一次状态）——**Day 15 流程管理页挂起/激活实测的遗留态**，最后一次拨在了挂起档；其余三个定义都是 1。
- **链路**：前端提示语来自后端预检 `ProcessInstanceServiceImpl.java:68-69`（`def.isSuspended()` → `BizException`），读的就是该表字段。预检的价值：不加它引擎也会拒绝，但抛的是 Flowable 生硬异常，报错不如这句友好。
- **处置**：swagger `POST /process-definition/{id}/activate` 激活后发起成功。
- 📌 **教训已写进断点文档第二节**：定义挂起态跨会话残留在库里，以后发起报「已挂起」先查 `SUSPENSION_STATE_` 列。

### 同日答疑：重复发起与 businessKey（源码级结论）

- 定义:实例 = 类:对象。引擎允许同一定义无限次发起，**businessKey 相同也不拦**。发起链路 `RuntimeServiceImpl.startProcessInstanceByKey` → `StartProcessInstanceCmd.execute()`（查最新版定义，即日志里那条 `max(VERSION_)` SQL；与我们 `start()` 的预检各查一次）→ `ProcessInstanceHelper.createProcessInstance` → `ExecutionEntityManager`，实例 id 由 `StrongUuidGenerator` 生成纯随机 UUID——**全链路没有一条按 businessKey 的查重 SELECT**，`BUSINESS_KEY_` 列上也没有唯一索引，只是一句 `setBusinessKey()` 存进 `ACT_RU_EXECUTION` 即止。
- businessKey 的定位是**关联字段（correlation）不是唯一约束**：把实例和业务表记录挂钩，可用 `processInstanceBusinessKey()` 反查。唯一性属业务语义，引擎不越俎代庖（jBPM → Activiti → Flowable 一脉如此，Camunda 同）。
- **将来若要防重**（现在不做）：`start()` 预检处加 `processInstanceBusinessKey(bk).count() > 0 → BizException`；注意**只查 RU**（办结后可重发）与**连 HI 查**（永久防重）是两种业务语义。更工程化的做法：业务表唯一索引，引擎侧不管。
- 📌 顺带登记一笔体验项进断点小尾巴清单（P4 收尾时评估）：发起弹窗下拉 `loadDefs()` 未过滤挂起定义，用户填完表单才被后端拦；前端过滤/置灰属体验优化，后端兜底已在。

### 断点文档一处旧错顺带修正

旧断点文档第三节写「办理只能走 knife4j（`/task/{taskId}/complete`）」——**URL 记错了**，实际是 `POST /task/complete`，taskId 在 body（`CompleteTaskRequest`）里（`TaskController.java:40-41`）。重写断点文档时已按代码修正。

---

## 2026-08-11 · `views/myStart/index.vue` 收工（292 行），Day 16 代码 6/6 全关

一天从「只差发起弹窗」推到整页完工，`npm run build` 转绿。`0d83a61`→`3eaf0d7` 共 8 个提交。**三步法第二次完整走通**，仍是本人写、CLI 只答疑与验收。

**② 组件 · 发起弹窗**（`:74-109`，本项目第一次向后端提交表单）。六个决策，都验过：

| 决策 | 为什么 |
|---|---|
| `:value="d.key"` 不是 `d.id` | `StartInstanceBody.processDefinitionKey` 要的是 key |
| `FormRules<typeof form>` | **不带泛型时键是开放 `string`，字段名拼错编译不报错**。实测探针：不带泛型放行，带泛型报 TS2561 并给拼写建议 |
| `.trim() \|\| undefined` | `undefined` 的键在 `JSON.stringify` 时消失＝不传，`''` 会真的传个空串 |
| 成功后 `status.value = ''` | 筛选停在「已办结」时，新发起的 RUNNING 实例**不出现在列表里** —— 提示成功却看不见，又一个静默错位 |
| `:show-close="!submitting"` | ⚠️ `close-on-click-modal` 与 `close-on-press-escape` **都管不到右上角的 X**，X 由 `showClose` 单独控制（`dialog-content.mjs:56`，默认 true） |
| `loadDefs` 的 catch 自己弹 warning | 全项目唯一自己弹错的 catch（其余靠 `utils/request.ts` 拦截器统一弹）。拉不到 defs 的后果是弹窗下拉空着、用户看不出原因。已加注释说明，防止将来被当冗余删掉 |

**③ 样式 · 同一个坑踩了两次**，都是**静默失效**：

1. `.muted`、`.pager` 用了但 `<style>` 里没定义
2. 修第 1 条时把 `.muted` 嵌进了 `.toolbar` —— 编译成 `.my-start .toolbar .muted`，而那个 span 在**表格**里，跟 `.toolbar` 是兄弟不是后代，**依然选不中**

> 📌 **教训**：SCSS 嵌套写起来像"归类整理"，编译出的却是**后代选择器**，是对 DOM 结构的断言。断言错了 CSS 不抗议，只是默默不生效。**写嵌套前先问「这个元素在 DOM 里真是那个父元素的后代吗」。**
> **验收手段**：`grep -o "\.my-start[^{,]*" dist/assets/myStart-*.css | sort -u` —— 直接看编译产物里的选择器，比在浏览器里瞪着页面猜快得多。

**顺带清掉两个记了四次的欠账**：竞态 `3024e5e`（`views/process/index.vue:7` `:loading` + `:43` `:disabled`）、命名 `ab4dd5d`（`deleteDeployment` 形参改 `deploymentId`）。

> 📌 **竞态那条为什么拖了四次才做**：一个**说不出后果**的待办永远排不进优先级。直到 08-10 把「静默错位」讲透、成文 `请求竞态与控件禁用.md`，第二天就做掉了。

### 同日答疑：三段问答，一篇成文

1. **`el-dialog` 位置与布局影响** → 已成篇 [`前端答疑/Teleport与弹窗渲染位置.md`](./process-front/前端答疑/Teleport与弹窗渲染位置.md)（见下方条目）
2. **`ref` vs `reactive`**（未成篇）—— 触发点「vue 所有对象声明都是这样吗」。核心纠误：**不是"基本类型用 ref、对象用 reactive"，而是 `ref` 什么都能装、`reactive` 只能装对象**，且**源码里 `ref(对象)` 内部就是调 `reactive`**（`@vue/reactivity` `reactivity.cjs.js:1519` `toReactive(value)` → `:1492` `isObject(value) ? reactive(value) : value`）。`.value` 是 JS 逼出来的唯一解（基本类型无法被 Proxy 拦截）。**本项目实证**：`reactive` 只出现 3 次全是表单，其余对象/数组都用 `ref` 或裸着。判断法压成两问：「会不会变、变了界面要不要动」→ 不变就什么都不包；要包则**默认 ref**。**建议落成 `前端答疑/ref与reactive.md`**（第一条链地基级）
3. **`FormRules` 与校验规则**（未成篇）—— `FormItemRule extends RuleItem`（`form/src/types.d.ts:11`），**EP 自己只加了 `trigger` 一个字段，其余 15 个全来自 `async-validator@4.2.5`** ⇒ 查规则字段该看 async-validator 文档而非 EP 文档。`rules` 是**例外清单**不是字段清单，靠 `<el-form-item prop>` 字符串配对（`form-item.vue_...mjs:88-90`），`prop` 没写就整列不校验（`:141`）。`min`/`max` 语义看**运行时值类型**（字符串比长度、数字比大小，`async-validator/dist-node/index.js:542-551`）

### 同日：`fixed="right"` 的"看不出区别"

本人实测后问「加没加 `fixed` 好像没差别」。**答案是对的，且这是 EP 刻意设计**：`fixed` 的真身是 `position: sticky`（`dist/index.css`），不滚动就和普通元素一模一样；阴影分割线由 `:before` 画、默认 `box-shadow: none`，只在 `.is-scrolling-left`/`.is-scrolling-middle` 下才给阴影，而 `style-helper.mjs:92` 明确写着没有横向滚动就打 `is-scrolling-none` 类，该类又把阴影抹成 `none`。本页七列合计 910px，宽屏放得下 ⇒ 不滚动 ⇒ 零可见变化。

> 📌 **由此提炼出一条通用验收原则**（已第三次遇到"写了看不出变化"）：**验收一个属性前，先问「它的生效条件是什么」**。无条件生效的（`color`/`margin`）打开页面直接比对；**条件生效的（`fixed`/`:disabled="loading"`/`v-if`）必须先把条件造出来再验**。前两次是静默失效（是 bug），这次是条件未触发（不是 bug）——区分这两者，靠的就是这一问。

⚠️ **遗留**：操作列的 `label="操作" width="90" fixed="right"`（`:45`）尚未提交，工作区 `M` 状态。

---

## 2026-08-11 · 《讲解规则》新增第五节「查证优先于回忆（源码交叉验证）」

本人明确反馈：**「我要求的并不是基于训练数据高概率的快速的回答，而是正确有依据的回复」**，要求把这个习惯固化成跨机器可用的规则。已落三处：`讲解规则.md` 新增第五节、`问答CLI起手提示词.md` 加规则 3.2、本机记忆加 `verify-against-source.md` 作指针。

> ⚠️ **章节号变了**：原第五~八节顺延为**六~九**。已同步修掉四处外部引用 —— `README.md:73`、`问答CLI起手提示词.md:21`（两处「第六节」→「第七节」）、`前端答疑/请求竞态与控件禁用.md` 的三处 `§6.3`→`§7.3`。**本文档 2026-08-10 条目里那句「《讲解规则》§6.3 ③」是旧号，按只追加原则未改，读到时请当作 §7.3。**

**规则要点**：涉及具体行为的回答走三步 —— ① 经验答案只当草稿 → ② 去 `node_modules/` 编译产物找到那一行 → ③ 用源码矫正，冲突就推翻并**明说流行说法错在哪**。查证落点优先级：本项目源码 > `node_modules/` 编译产物 > 依赖版本号 > 官方文档 > 记忆。落文档时结论必须带**依赖版本号 + `file:line`**，依赖升级后才有得复查（已写进 §8 体例）。

> ⚠️ 规则里特意写明**不是"去联网搜"**。真实流程是「训练数据出草稿 → 本机源码做裁决」，写歪了换台机器会让 CLI 跑偏方向。

### 触发实例：`el-dialog` 的位置与布局影响（本条结论已查证，可直接引用）

问题是「点发起流程没反应」→「弹窗组件为什么写在分页后面」→「会不会影响布局」。**流行答案「el-dialog 内部用 Teleport 搬去 body，所以写哪都一样、也不影响布局」——对 `element-plus@2.14.2` 是错的**：

- `dialog.mjs:14` `appendToBody: Boolean`（裸 Boolean，默认 false）、`:136` `appendTo: "body"`
- `dialog.vue_vue_type_script_setup_true_lang.mjs:58-60` `disabled: appendTo !== "body" ? false : !appendToBody` → 代入默认值得 **`disabled: true`，Teleport 默认关闭**，DOM 就留在你写它的位置（`.my-start` 的第 4 个子节点）
- 真正让它不影响布局的是**三道各自独立的保险**：懒渲染（`use-dialog.mjs:24` `rendered = ref(false)`，`:149`/`:171` 首次打开才置 true）+ `vShow`（`dialog.vue_...mjs:153` 的 `[[vShow, visible]]`，关闭时 `display:none`）+ `.el-overlay { position: fixed }`（`dist/index.css`）**脱离文档流**
- **流行答案的结论碰巧对、理由全错**，照错理由推下一题就会翻车：`:deep()` 给弹窗写样式时**不能带 `.my-start` 祖先前缀** —— 一旦哪天加了 `append-to-body`，Teleport 真启用，带前缀的选择器会当场静默失效

### 同日已落成独立篇：`process-front/前端答疑/Teleport与弹窗渲染位置.md`

本人反馈「还是没有特别懂，可能是我前端基础问题，整理成文档方便回顾，这个支线不影响主流程，我先记住结论」——遂按 §8 体例落成十节全篇，并**为"先记住结论"专设第零节**（五条速记 + 一句核心），推导看不懂可以先跳过。

- **补了大量前端基础**（这是本人自陈的缺口）：文档流是什么、`position:fixed` 为什么脱离它、层叠上下文、`scoped`/`data-v`/`:deep()`、`v-if` vs `v-show`。均配后端类比（依赖收集≈EventBus 订阅名单为空、文档流≈LayoutManager、fixed≈摘出去的 overlay 层）。
- **纵向三层**：① 点按钮没反应＝没人读那个 ref（Vue 响应式是"拉"不是"推"）→ ② 写在哪 ≠ 渲染在哪（Teleport 默认关）→ ③ 渲染在哪 ≠ 占不占位（三道保险）。
- **横向讲史**：弹窗必须盖住全页 → 祖先的 `overflow:hidden`／层叠上下文／`transform` 三种笼子（**第 3 条会让"fixed 脱离文档流"当场失效**，最阴）→ Vue2 手动 `appendChild` 的代价 → React 16 Portal「DOM 搬走、组件树不变」→ Vue3 `<Teleport>` 多给 `disabled` 开关 → 代价是 scoped 祖先选择器失效。
- **新查证到的两条**（上条未记）：`overlay.mjs:31-53` 两个分支**都是 `position:fixed`**，所以 `:modal="false"` 去掉遮罩也照样不占位；`destroyOnClose` 默认 false（`dialog.mjs:29`）⇒ **关闭后表单内容会留着**，`openDialog()` 里手动清三个字段是必要的不是多余的。
- **四个动手实验**，核心是**实验 2（打开后 DOM 仍在 `.my-start` 里 ⇒ 推翻流行说法）与实验 4（加 `append-to-body` 后跑去 body）的对比**。
- **§9 附了换版本后的重查命令清单**——这是《讲解规则》新 §8「结论要带版本号 + file:line」的第一次落地实践。

索引已补一行，并**新起第四条链「CSS 与渲染层」**（本篇为地基，另登记 4 篇待写：层叠上下文与 z-index／`scoped` 编译产物／定位五值与包含块／`v-if` vs `v-show`）。其中**「层叠上下文与 z-index」标注为 P5 前必补**——BPMN 设计器画布 + 浮层 + 属性面板三层叠一定会撞上。

### 同日顺带对账：myStart 页已到 200 行，build 由绿转红

`0d83a61「弹窗」`（断点文档当时停在 `e39a798`）加了发起按钮（`:15`）、`openDialog()`（`:115-121`）、表单四件套 `form`/`formRef`/`dialogVisibal`/`submitting`（`:93-100`），**但 `<el-dialog>` 模板一行没写，`startInstance` 也没 import** —— 所以点按钮无反应：`dialogVisibal` 是个没有任何消费者的 ref，Vue 依赖收集表里它那行是空的。

`npm run build` 现为红：`myStart/index.vue(95,7): error TS6133: 'submitting' is declared but its value is never read` —— 同一病根，写完提交函数自动转绿。另：`dialogVisibal` 拼写应为 `dialogVisible`，趁只有两处赶紧改。

---

## 2026-08-10 · 进度对账（文档 ↔ 代码）：Day 16 实为 5.5/6，myStart 页已起头

断点文档停在 08-07 上午的状态，**代码比它新两个提交**，对账后已回写三处文档（断点 / `05` 总控 / `plan/P4`）。

- **`ada25b4`「add myStart menu,router」→ Day 16 第 6 项就此关闭**（原记「各 1 条已加」）。`router/index.ts:39-43` 挂 `/my-start`（`name: 'Mystart'`，指向 `@/views/myStart/index.vue`），`layout/index.vue:29` 加菜单「我的发起」。Day 15 踩过的三条约定这次都写对了：URL 短横线 `my-start` / 目录小驼峰 `myStart` / 子路由 `path` 不带前导斜杠而菜单 `index` 带。
- **`e15e2fa`「第一行，标题和下拉框」→ `views/myStart/index.vue` 已建，74 行**（原记「未建」）。按《页面思考法》推进到第 ② 步中途：
  - ① **布局已完**：照实例页的 4 div 结构搭出 `.my-start` > `.toolbar` / 表格 div / 分页 div，后两个是空壳。**范本第一次被复用，结构照对了。**
  - ② **组件只到状态下拉的空壳**：`el-select` 已挂 `:disabled="loading"` 与 `@change`，但**没绑 `v-model="status"`**，`statusOptions` 整块注释着（`:35-40`），模板里只有一个写死的「全部状态」`el-option`；表格、分页、发起弹窗都未写。
  - ③ 样式未开始；`.title`（`:68`）**选不中任何元素**——模板 `:5` 那行是 `<h2>` 不是 `class="title"`，两边得统一。
  - 数据侧 `loadList()`（`:47-58`）逻辑完整正确，`handleFilterChange` 也把 `current` 归了 1，但**没有 `onMounted` 调用**，进页面不拉数据。
- **契约再核一遍无错配**：`ProcessInstanceController` 的 `start` / `page` / `my-started`（带 `status`）/ `DELETE {instanceId}/terminate` 与 `api/instance.ts`（59 行）四个函数逐个对上，前端不用再动契约层。
- **三笔欠账仍在**（第四次记）：`views/process/index.vue` 的 `el-switch` 无 `:loading`（`:5`）、`el-pagination` 无 `:disabled`（`:39`）；`api/process.ts` 里 `activateDefinition`(`:25`) 与 `deleteDeployment`(`:29`) 形参都叫 `definitionId`，后者实为 deploymentId；库里仍无实例样本（07-30 清库后一条未发起），myStart 验收前得先造。

> 📌 **教训同 08-05 那次对账**：断点文档的「下一步」写完就该在动手当天回写，否则隔几天再看会低估自己的进度（这次低估了整整一个提交的路由菜单 + 一个页面的骨架）。

### 同日新增答疑笔记：`process-front/前端答疑/请求竞态与控件禁用.md`

触发点是「为什么要加 loading 和 disabled，之前的没理解，忘记了；**不加有什么影响？**」——**这才是那两笔欠账被记了四次都没做的真正原因**：一个说不出后果的待办，永远排不进优先级。

从 `原生控件的外衣与el-switch.md` 第八节第 1 条**抽出并大幅扩写**（那边只留结论 + 指针，避免两处维护）。九节体例齐全，要点：

- **结论换了个说法**：`loading` 不是 UI 状态，是**并发闸门**；不加的后果是**静默错位**（不报错、不崩、控制台干净，只是表格数据和开关/页码对不上）——正是《讲解规则》§6.3 ③ 那条警戒线。
- **纵向**：拿 `views/process/index.vue:134-137` 的真实时序走了一遍（B 先回 A 后回 ⇒ 开关停在 B、数据是 A）；补上**响应为什么会乱序的四个来源**（服务端耗时 / HTTP2 多路复用 / HTTP1.1 连接池 / 代理）；`loading` ref 被两次调用共用 ⇒ **遮罩提前消失反而成了错误信号**。
- **后端类比**：等价于两个线程写同一个成员变量的 lost update；**但不需要锁**——JS 单线程没有撕裂和可见性问题，要拦的只是"第二个请求出生"。
- **横向**：jQuery 时代手动 `prop('disabled')` → 痛点是**恢复语句要在每个出口写一遍，漏一个就永久卡死**（所以现实中大家干脆不做）+ 状态只存在于 DOM 里 → 新方案是「声明式 UI + `try/finally`」两件事咬合的**必然结果**，少任何一半都不成立 → 代价：只挡入口不取消请求，完备解法是 `AbortController` 或 latest-wins。
- **源码为证**：`switch.vue_vue_type_script_setup_true_lang.mjs:42-44` 确认 `loading` 为真直接算作 disabled 并传到底层原生 `<input>`；`pagination.mjs:151` 确认分页器**只有** `disabled` 没有 `loading`——**两处 prop 名不同是组件能力差异，不是风格不一致**。（旧篇引的 `switch.vue.mjs:42-44` 路径不准，新篇已订正。）
- **三个实验**：纯 Promise 复现乱序覆盖（含对照组）/ Slow 3G 在真页面撞出来 / **实验 3 专证"再加个 await 治不了"**——await 只序列化同一函数内部的语句，两次点击是两个独立回调。
- **回到本项目**：欠账两笔的确切改法；实例页 `:11`/`:50` 已自愈；**myStart 页 `:9` 的下拉已经挂上了**（范本在起作用），但分页器还没写，写的时候一起挂；顺带说清表格操作列按钮**不禁用是合理的**——它每次都弹 `ElMessage`，出错看得见，优先级低于静默错位。

索引已补一行，并把它编进「JS 异步」链第 2 篇（`async-await与JS单线程.md` 的直接后果与账单），同时新登记待写的第 5 篇 `AbortController 与请求取消`。

### 同日 · `views/myStart/index.vue` 从 74 行写到 177 行，build 转绿

`e15e2fa`→`e39a798` 四个提交。表格 7 列 + 分页 + 撤回 + `onMounted` 全部就位，**② 组件只差发起弹窗**。踩了五个坑，都已讲透并修掉，**每一个都值得记住失败的形状**：

| # | 坑 | 形状 |
|---|---|---|
| 1 | `STATUS_MAP.[row.status]` 多一个点 | **编译期就炸**，唯一一个不静默的 |
| 2 | `onMounted` 被写进了 `handleRevoke` 函数体里（右花括号在它下面） | 页面**一个请求都不发**。`onMounted` 必须在 setup 顶层**同步**调用，事件回调里调等于没调 |
| 3 | `defs = page.records`（`const` + 漏 `.value`） | 真抛了 `TypeError`，**但被同一个 try 的空 `catch {}` 吃掉** → 下拉永远空、控制台干净 |
| 4 | `inputValidator` 从文档的表达式体 `=> (…)` 抄成了语句块 `=> { … }`，漏 `return` | 返回 `undefined`；查 EP 源码（`message-box/…lang.mjs:195-206`）只拦 `=== false` 和字符串 → **非空校验完全失效，空原因照样提交** |
| 5 | `TS7053`：`row` 隐式 any，不能索引 `Record<InstanceStatus,…>` | `vite dev` 全程绿灯，`vue-tsc` 才报。**采用「给插槽标类型」解决**：`#default="{row}:{row:ProcessInstance}"`，治根且白送补全 |

**三条沉淀下来的方法**：

1. **读 Vite 的 `Error parsing JavaScript expression`**：里面有两套坐标。`File: …:22:28` 是文件坐标；`(1:12)` 是**表达式内**坐标——Vue 把引号里那段当独立小程序丢给 JS 解析器，所以行号恒为 1，**有用的是列号**，去引号里数字符即可。
2. **"没反应"分三段切**：① 函数根本没被调用（Network 空白基本就是这一段，在函数第一行打日志验）→ ② 调了但没走到 api → ③ 走到了但被拦截器/浏览器挡了。另有一个纯靠眼睛的信号：**`<script setup>` 的顶层调用都该顶格，缩进对不上就是括号配对错了**（坑 2 就是这么看出来的）。
3. **空 `catch {}` 会把"没预料到的错误"一起静音**。本意是"HTTP 错误拦截器已弹过"，但 catch 的作用域是整个 try 块，连赋值给 const 这种纯 bug 也一并吞掉（坑 3）。**改法：catch 里至少留一句 `console.error`。** 目前 `myStart` / `instance` 两页共 4 处空 catch，都是同一形状。

> 📌 **`dev 绿 ≠ 代码对`**：坑 3、4、5 在 `vite dev` 里全程无声（esbuild 只删类型不检查），坑 5 是 `npm run build` 的 `vue-tsc -b` 逮住的。**每写完一页 build 一次**这条规矩，这次第一次真正兑现了价值。

**顺带**：`03-完整代码.md:430` 的状态列写法和坑 5 完全相同 —— **那份参考代码本身没跑过 build**。文档不是权威，`vue-tsc` 才是。

### 同日 · 新登记：myStart 操作列的「轨迹」按钮推迟到 Day 18

**起因**：照 `03-完整代码.md` 写完后对原型，发现操作列少一个「轨迹」，而 `01`/`02`/`03` 三篇里**「轨迹」两个字一次都没出现过** —— 看起来像文档漏了，实际是排期切分，只是从没被登记。

- **原型有三节**：① 运行中实例页（只有「终止」）、② 我的发起·方案②（只有「撤回」）、③ 我的发起·方案①（**「轨迹」+「撤回」**，即最终采纳那版）。轨迹只在第 ③ 节，那是**最终形态**示意。
- **后端接口 Day 14 就有了**（`HistoryController.java:33-34`）；缺的是前端 `api/history.ts` + 轨迹组件 + 连线过滤，三样都在 **Day 18**。**不为一个按钮把 `api/history.ts` 提前拉进来。**
- **一个设计上的连带关系**：原型让**每一行都有轨迹**（撤回只给进行中的行），正是为了解决"已办结/已终止的行操作列全空"。`03-完整代码.md` 对同一问题给的是 `<span v-else class="muted">—</span>` 占位 —— **两种答法，原型那个更好**：破折号只是填空白，轨迹是真给了用户一件能做的事。**Day 18 接上轨迹后要把破折号删掉。**

已三处登记：`process-front/process_instance/01-页面定位与修改点.md` 第三节末尾（来源与理由）、`plan/P4` 的 Day 18 条目（执行清单）、`测试进度断点.md` 小尾巴清单（时机 Day 18）。

---

## 2026-08-06 · **`views/instance/index.vue` 从零自写完成**（08-07 对账补记）

**本项目第一个不照抄、由本人从零规划的前端页面。** 08-06 一天六个提交写完，178 行；08-07 `npm run build`（含 `vue-tsc -b`）实跑转绿。

**《页面思考法》三步法的首次完整实战**，提交序列正好对上三步：

| 步 | 提交 | 时间 |
|---|---|---|
| ① 规划布局 | `a3aa997` / `77d88b1` | 08-06 10:13–10:15 |
| ② 填充组件 | `548f236`（标题+下拉）→ `9caf403`/`e5983b7`（表格）→ `77aa455`/`d97e19a`（分页） | 11:43–17:20 |
| ③ 调整样式 | `dcb117f`（工具条）/ `5ccf065`（分页靠右） | 14:17 / 17:57 |

**成品落到的几个点**（都可作为后面三页的既定做法）：
- **4 div 结构守住了**：`.instance-page` > `.toolbar` / `.table-wrap` / `.pager-wrap`。① 布局阶段定的结构一路没被组件挤变形 —— 这是三步法「先定骨架再填肉」有效的直接证据。**该结构自此为 P4 后面三页的模板**，`views/process/index.vue`（CLI 生成、表格与分页裸放）不再作参考。
- **竞态在本页自愈**：`el-select`（`:11`）与 `el-pagination`（`:50`）都挂了 `:disabled="loading"`。这正是 Day 15 顺延下来那两笔欠账治的同一件事 —— 说明该原理已内化，**欠的只剩回头补流程管理页**（该项已第三次记入小尾巴清单）。
- **下拉数据源复用 Day 15 的 `pageDefinitions`**，没重封一遍（`01-页面定位与修改点.md:64` 的提醒生效）。
- **砍掉「实例 ID」列**：按 `02-填充组件.md` 早先定的（UUID 对用户无意义），终止直接取 `row.id`。原型 HTML 有这列、成品无，是有意偏离。
- 终止走 `ElMessageBox.prompt` + `inputValidator` 非空校验，删末页时 `current -= 1` 回退 —— **Day 16 清单里「终止用 prompt」这条就此关闭**；同清单的「发起弹窗」不在本页，归 `views/myStart`（`01:58`），实例页没有它不是漏。
- 保留了 `loadList` / `loadDefs` 的空 `catch`（07-30 采纳的方案 B），rejection 不逃逸。

**build 输出的两条噪音已确认与本页无关**，别当成待修问题：`@vueuse/core` 的 `#__PURE__` 注释位置警告（依赖侧，rolldown 报的）、chunk > 500 kB 提示（未做代码分割，全项目性质）。

**进度影响**：Day 16 从 4.5/6 → **5.3/6**。断点前移到 `views/myStart/index.vue`（未建），其后是 `/my-start` 路由菜单、造样本实测。已同步 `测试进度断点.md`、`plan/P4`、`05-重写排期计划.md` 三处。

> 📌 **对账发现的文档滞后**：断点文档 08-06 的记录停在「① 布局已收工，② 组件下一步」，而当天下午实际把 ②③ 都做完了 —— **收工回写发生在写完布局之后、当天后续工作之前**。维护约定第一条（每天收工回写）要落在**真正收工时**，不是中途某个自然停顿点。

---

## 2026-08-05 · 新系列《页面思考法》成文；学习方式换挡

**起因（本人自陈，重要）**：`views/user`、`views/process` 等已完成的前端页面**都是照着我给的代码抄写的**，并没有自己规划过布局 / 响应 / 样式。本人提出「不能一直这样下去，需要有自己的思维」，于是 Day 16 的实例页改成**先讲思考顺序、再自己动手**，并把这套顺序固化成文档系列。

> ⚠️ **对我的纠正**：以后**别把已交付的前端页面当成他的既有经验去援引**（「你 Day 15 自己写通的那页」这种话是错的，会把讲解建立在他没有的基础上）。已抄的页面只是**手边的参考样本**，拿来对比时要现场解释。已同步进长期记忆。

**新增目录 `process-front/页面思考法/`**（与 `前端答疑/` 平级：那边装「某个语法/组件是怎么回事」，这边装「怎么想」）：

| 篇 | 内容 |
|---|---|
| `00-总纲.md` | 三步法总览 + 四条铁律 + **三条元方法** + 应用进度表 + 与其他文档的分工 |
| `01-规划布局.md` | 划边界（`router-view` 那个坑）→ 拆条带（工具条/数据区/分页条）→ 定职责（谁是输入谁是输出、单向流）；命令式组件不参与布局；布局判据＝视觉第一性 + 使用频率（含 Gmail 反例） |
| `02-填充组件.md` | **选型三问**（动作 / 选项空间 / 形态匹配）；radio-select-input 分界线＝空间成本 vs 点击成本；选项来源与「全部」的值；**「能不能原样打印？」**；**「组件自己记不记状态？」**；选型被后端接口形状决定 |
| `03-调整样式.md` | 组件库替你决定了什么、你只补两类；`space-between` 是分配空间不是靠边；间距基数 4；颜色用语义变量；`width` vs `min-width`；`scoped` |

三篇都以**运行中实例页**为贯穿案例，每篇末尾带检查点自测题 + 折叠答案。

**沉淀下来的三条元方法**（`00` 第三节，比具体规则更上位）：
- **A · 规则清单记不住，要换成「一个能推出规则的问题」** —— 实测：给了三行插槽规则表，当场问答「看了一遍全忘了」。诊断是读的时候在「接收」没有「决策」，人只记得住自己做过的判断。于是把规则表压成一句能现场自问、能重新推出全表的话
- **B · 判断方案好坏，不能只问「会不会出错」，要问「出错时我会不会知道」** —— **静默失败**在这次讲解里出现三次（下拉硬编码漏项 / 受控组件忘接事件 / 样式漏写 scoped），全都不报错、控制台干净
- **C · 一处用行内，多处必收拢** —— 理由不是「重复不优雅」，是改的时候必漏一个，而漏掉的那处同样是静默失败

**顺带定的两笔**（已写进 `02`/`03`）：
- 表格**砍掉「实例 ID」列**（原型有、成品无）：UUID 对用户无意义，终止操作直接拿 `row.id`。排错需求改用将来加的「复制 ID」按钮补 —— 连带：操作列 `width` 届时要从 ~90 放大到 ~140
- 标题色建议从硬编码 `#303133` 改成 `var(--el-text-color-primary)`（换主题时才跟得住）

**新登记待深入主题**：`deep_dive/待深入主题清单.md` 第 7 条 —— **`<router-view/>` 与 slot 的关系**（都是「父组件留坑、子组件填内容」，区别在填充者是编译期的父模板还是运行期的 URL）。带五步深入路径和一个一分钟实验（删掉 `layout/index.vue:60` 看 `/instance` 变什么样）。**本人明确要求推迟到整个项目完工后**，P4~P8 期间不主动插入。

---

## 2026-08-05 · 进度对账（文档 ↔ 代码）：Day 16 实为 4.5/6

**起因**：断点文档停在 08-04 白天，之后又落了三笔代码没回写。本轮**逐文件核对了前后端实际代码**（不是照抄文档），把 `测试进度断点.md` 与 `plan/P4` 更新到实际位置。

**对账结果 —— 文档说未做、实际已做的**：

| 项 | 文档记的 | 代码实际 |
|---|---|---|
| `api/instance.ts` | ⬜ 未开工 | ✅ 完工并提交 `68eb001`（08-04 16:48）：59 行 / 4 函数 / 3 类型 |
| `views/instance/index.vue` | ⬜ 未开工（08-04 changelog 记「尚是空文件」） | 🔄 33 行、提交 `83b87c5`（08-04 17:48），**写到一半** |
| `/instance` 路由 + 菜单 | ⬜ 两处都没有 | ✅ `router/index.ts:34-38` + `layout/index.vue:26` |
| `ProcessInstance.endTime` | ⏳ 记作「待补」 | ✅ 已补（`api/instance.ts:17`） |
| `terminate` URL 改形 | 08-04 记作已同步 | 后端**实际是 08-05 08:49 才提交**（`30a7314`）；前端 08-04 就先写成新形状了，昨晚那半天两边其实是错配的 |

**对账结果 —— 文档说未做、确实未做的**（复核后原样保留）：`views/myStart` 及其路由菜单；`views/process/index.vue` 的两笔并发加固（`el-switch :loading` / `el-pagination :disabled`）；`api/process.ts:29` 形参命名；库里仍无实例样本。

**新登记的断点细节**（已写进断点文档「一」的 🔻 块）：`views/instance/index.vue` 的 `<script setup>` 里 6 个 ref 备好了，但没 import `pageInstances`、没 `loadList()`、没 `onMounted`；模板断在 `:23-25` 的 `<el-select v-model="definitionKey"` **标签未闭合** —— 所以**此刻 `npm run build` 是过不去的**，下次开工第一件事是把这个标签收掉。

**三仓库状态**（08-05 核对）：`process-back` / `process-front` 均在 `dev` 分支、工作区干净；`process-docs` 在 `master`、干净。后端最后一笔是 `30a7314`，**改了 Controller 映射，服务需重启一次**。

**流程没走全的地方**（下次注意）：08-04 那两笔前端提交没有当场回写断点文档，导致文档与代码差了两笔——`测试进度断点.md` 第四节的约定是「代码一改完就当场回写」，这次是隔天靠扫代码补回来的。

---

## 2026-08-04 · Day 16 前端开工；terminate URL 改形；TS 答疑三篇成文

**主线（Day 16 第 5–6 项，进行中）**：
- `api/instance.ts` 新建，五个函数就位（`pageInstances` / `myStarted` / `startInstance` / `terminateInstance`，含类型 `InstanceStatus` / `ProcessInstance` / `StartInstanceBody`）
- `/instance` 路由 + 菜单项已加；`views/instance/index.vue` **尚是空文件**，`views/myStart` 与其路由菜单未开始
- ⚠️ 顺带查出并已修：`terminateInstance` 的 URL 原用**单引号**包 `${instanceId}`（不是模板字符串，会把字面量 `${instanceId}` 发出去，后端报「实例不存在」，报错完全指不到根因）
- 待补：`ProcessInstance` 缺 `endTime: string | null`（写「我的发起」状态列时会撞上）

**接口改形 · `terminate`**（后端 + 前端 + 两篇文档同步）：

```
DELETE /process-instance/{instanceId}              （原）
DELETE /process-instance/terminate/{instanceId}    （一度改成，动词在中间）
DELETE /process-instance/{instanceId}/terminate    （最终）
```

- **动机**：原 URL 语义误导 —— 读起来像「删除这条实例」，实际是**终止**（清 RU、**保** HI，审计线索还在）。真正抹掉历史的是 `cascade=true` 删部署那条路
- **为什么是 `{id}/terminate` 而不是 `terminate/{id}`**：项目里两种形状本就并存（`{definitionId}/suspend`、`{definitionId}/activate` vs `delete/{deploymentId}`），选前者可让四个接口中三个同形；且「id 在前动词在后」读起来是一句话，动词夹中间语法上讲不通
- **同步改动**：`ProcessInstanceController.java`、`api/instance.ts`、`process-front/process_instance/03-完整代码.md`（第 1 节，带注释说明改因）、`02-操作步骤.md:43`。历史性记录（`process_history/07` 的 curl、`process_history/03-操作步骤.md`）**保持原样不改**——那是当时的实测流水
- ⚠️ **遗留**：`/process-definition/delete/{deploymentId}` 仍是老形状，没跟着改。要统一得连前端 `deleteDeployment` 一起动，**留到 P4 收尾评估**（与 `request.get<never,X>` 那条并作一批）

**答疑产出 · TypeScript 链开张**（`process-front/前端答疑/`，索引已建「第三条链」）：

| 篇 | 触发点 | 顺带挖出的真问题 |
|---|---|---|
| `字面量联合类型与enum.md` | `type InstanceStatus = 'RUNNING' \| ...` 是枚举吗 | `statusOptions` 未被类型守住（含 `''` 不属于联合），加第四种状态时它会安静少一项 |
| `null与undefined.md` | 为什么有的字段多个 `null`、还有个 `undefined` | **`suspended` 在 HI 路径恒 `false`**（`of(HistoricProcessInstance)` 没 set，Java boolean 默认 false）——已登记进断点文档小尾巴 |
| `Record与映射类型.md` | `Record<string,unknown>` 和后端 `Map` 等价吗 | 键开放=拼错不报错，**这就是 P6 表单 schema 存在的理由**（把 K 从无穷集换成有限集） |

三篇共用主线「**TS 的保护到 HTTP 边界为止**」。第三篇偏抽象，开头写了分两次读的指引（现在只看第零、四节，机制留到 P6）。

**口头讲过但未成文**：`request.get<never, X>` 两个泛型参数的由来（axios 签名 `get<T, R, D>` 返回 R + 拦截器拆包 + 去掉后会被 `.data` 误导）——素材已记进断点文档小尾巴那条，将来可落成 TS 链一篇。

---

## 2026-08-03 · 泛型支线收口；Day 16 改走无泛型版，主线解除暂停

**起因**：九篇泛型笔记已快速通读一遍（不懂的先跳过），本人判断「学识有限，继续停留也不会有大进展」，且**不想在非主线问题上继续花时间**。

**能力结算（②档达成，③档搁置）**：
- ✅ **② 会写泛型类/泛型方法** —— 在同包自己写出了等价的 `common/Result.java`：类上 `T`（因为 `private T data;` 是字段）、4 个 `static <T>` 工厂各自声明类型参数。**`泛型/08` 闭卷第一题记为实测通过，不必重做**。
- ⬜ **③ 通配符 / 自限定泛型** —— 未达，主动搁置。恢复触发点：**觉得 `toPage`/`toHiPage` 两份重复碍眼时**再回来重构。

**核对 `Result.java` 时发现的两点**（留档）：
- `new Result<>()` 用了菱形推断，比 `R.java:12` 的 `new R<T>()` 更地道；
- `failed(String msg)` 委托 `failed(msg, 500)` 时，内层的 `T` 靠**目标类型推断**接上（参数里没有任何 `T` 类型实参可推）——`04` 篇「类型实参三个来源」里最隐蔽的一种，无意中用对了。验证法：改成 `var r = Result.failed(msg,500); return r;` 会编译不过。

**决策：Day 16 第 4 项改走 `toHiPage` 复制版**，推翻 07-31 那条「不走绕路、这一关必须过」。
- 做法：保留 `toPage(ProcessInstanceQuery, long, long)` 查 RU，另复制 `toHiPage(HistoricProcessInstanceQuery, long, long)` 查 HI，两个方法各 5 行、末尾都调 `enrich()`。**零泛型声明**，`page()` 的 return 行真·零改动。
- 依据：`泛型/08` §4 本就把复制版列为**正当选项**而非将就（"收益门槛随读者数量变化"，应用作者与库作者判据不同）。
- 代价（已接受）：分页收尾的代码存在两份。功能与泛型版完全等价，验收清单一条不少。
- **两个 Query 接口无继承关系这件事仍然成立**（javap 已核实，共同祖先只有 `Query`）——所以"想用一个方法吃两种查询器就只剩泛型一条路"这个结论不变，只是现在选择不合并。

**产出**：新建 [`process-back/process_instance/10-Day16-完整代码-无泛型版.md`](./process-back/process_instance/10-Day16-完整代码-无泛型版.md)（当前采用），含：与 `09` 的差异对照表、完整 `ServiceImpl`、`applyStatusFilter` 的 `if/else` 版、「为什么两个方法体一样却合并不了」、**第 5 节「换回泛型版只动 4 处」清单**、验收清单。文中类名/方法名（`SystemUserMapper`、`SysUser.getId/getRealName/getUsername`）已对着实际文件核过。

**`09-Day16-完整代码.md`（泛型版）原样保留**，性质由「当前任务」改为「将来的重构目标」。

**顺带**：`common/Result.java` 加类级 javadoc 标注为泛型练习品，注明生产代码用 `R`、读代码可跳过。

**四处文档同步**：`测试进度断点.md`（头部 + Day 16 任务表 + 学习支线行 + 第三节路线，A 改为回主线、原 A 读泛型删除）、`plan/P4`（状态行 + 落地文档链接 + Day 16 第 4 项 + 打卡表）、`deep_dive/待深入主题清单.md` 第 5 条（收口说明 + 验收动作结算，原两张表保留存档）、本文档。

---

## 2026-07-31 · 主线暂停补泛型基础；九篇笔记写完但只读到 `01`

**起因**：读 `process_instance/09-Day16-完整代码.md` 里泛型化的 `toPage(Query<?, E> query, Function<E, ProcessInstanceVO> mapper, ...)` 卡住。追下来确认根因**不是这个方法**，而是**泛型基础一直没打牢**——此前用法只有 `List<User> list = new ArrayList<>()` 一种。

**决策**：
1. **主线 P4 Day 16 暂停**（当时已完成 1/6：`ProcessInstanceVO` 改造已提交 `5dabc3b`），先补泛型。
2. **Day 16 仍用泛型版 `toPage`，不走 `toHiPage` 复制版**——「这个以后会经常遇到，是必须要过的一道关卡」。
3. **目标从 ② 上调到 ③**（能自己设计泛型，不止读懂）。理由：`00` 篇自身矛盾（验收标准是"能写出 `R<T>`"，而那是 ③）；且读 `Query<T extends Query<?,?>, U>` 这类签名，**不懂"为什么让类型参数指向自己"就只是认字不是读懂**——设计意图是读懂的前提，不是进阶。
   附带纪律：**会设计 ≠ 到处用**，验收含「知道什么时候不该设计」，`08` 篇专门收这条。

**产出**：新建 `process-back/deep_dive/泛型/`，九篇 `00`–`08`。全程用**本项目自己的代码**当标本（`R.java` / `JwtUtil.java:44` / `SystemUserMapper` / `IPage.convert` / `toPage`），不拿抽象的 `Box<T>` 举例；`02`/`05`/`06`/`07` 的 javap 输出与编译报错原文**均当场实跑**（本项目 `R.class`、`java.lang.String` 的桥接方法、Flowable 7.1.0 的 `Query` 接口、javac 17 的四条报错）。

**⚠️ 写完 ≠ 读完**：当天时间不够（特殊安排），改成**先把九篇一次性写完，之后回来读**。实际只读了 `00`/`01`，`02`–`08` 未读未验。这意味着后七篇**没走「读一篇 → 跑实验 → 反馈 → 写下一篇」的循环，缺一道校验**——读的时候要留意难度断层 / 实验跑不通 / 前后归因不一致，发现当场回写。阅读进度表在 `process-back/deep_dive/待深入主题清单.md` 第 5 条。

**顺带修掉的三处过期文档**（都是 07-30 清库/改配置后没同步的）：
- `测试进度断点.md` 第三节还写着「开工第一件事是拍板决策」——该决策 07-30 已定（走 ① 查 HI）
- `测试进度断点.md:57` 说 `check-process-definitions` 是 `true`——实际 `application.yml:33` 已是 `false`
- `plan/P4` 第三节的 test-0004~0007 样本表——清库后全部作废，已换成「需要自己造哪四种状态」的操作表

**新登记的欠账**：`待深入主题清单.md` 第 6 条 `Stream.toList()`（为什么 Java 16 才加、为什么不可变、`final` vs immutable），排在泛型之后。

---

## 2026-07-30 · 整库重置 + 新增三个三级流程模板

**整库重置**：执行 `resources/sql/00_reset_db.sql` 清空 → 重启建表 → 跑 `sys_user.sql`。test-0004~0007 四条实例样本与原 `leaveProcess` 部署全部作废，历史表清零。此前记录的实例 id、deploymentId 一律失效。

**新增三个模板**（`src/main/resources/processes/`），均为**三级**（发起 + 一级 manager + 二级 admin），照 leave 的写法：`flowable:initiator` 记发起人、`candidateGroups` 按角色认领、手写 DI 坐标：

| 文件 | key | 二级审批节点名 |
|---|---|---|
| `reimburse.bpmn20.xml` | `reimburseProcess` | 财务审批 |
| `purchase.bpmn20.xml` | `purchaseProcess` | 总监审批 |
| `travel.bpmn20.xml` | `travelProcess` | 总监审批 |

节点 id 统一 `startEvent` / `managerApprove` / `adminApprove` / `endEvent`，连线 `flow1`–`flow3`。原 `leave.bpmn20.xml` 保持两级不动。

**建完做的三项校验**：
1. **XML 合法性** —— 四个文件均通过 XML 解析，`process` / `userTask` 结构正确。
2. **编码** —— 字节级确认四个文件全是 **UTF-8 无 BOM**，与产出过正常中文的 `leave.bpmn20.xml` 完全一致，不会重蹈 test-0004 那种任务名乱码。
3. **role ↔ candidateGroups 链路** —— `sys_user.sql` 里 role 字面值正好是 `admin`/`manager`/`employee`，而 `FlowableTaskServiceImpl:36` 走 `taskCandidateGroup(me.getRole())` 直接匹配候选组，故 `candidateGroups="admin"` 的二级节点 admin 登录能在待办看到。两级链路成立。

**配置**：`check-process-definitions` 已由用户改回 `true`（`application.yml:33`），重启即自动部署四个模板；**入库确认后需改回 `false`**。

**顺带带来的新测试维度**：以前 leave 单审批节点，manager 办完即结束；现在三个新流程办完一级会**流转到 admin 的待办**，可真实验证任务在两个角色间流转，也会第一次真实撞上多级审批 `comment` 变量互相覆盖的问题（治理点在 `plan/P6`，已写进三个模板的注释）。

---

## 2026-07-30 · Day 15 关闭（P4 第一个前端页跑通）

浏览器实测验收通过，**Day 15 关闭**：

| 功能 | 结果 |
|---|---|
| 列出已部署流程（分页） | ✅ |
| 挂起 / 激活 | ✅ |
| 删除带在途实例的部署 | ✅ **被友好拦下** —— 这是验收标准要的行为，不是失败 |

删除那条走的是 `cascade=false` 遇在途实例的 catch 分支（底层 `PROC_DEF_ID_` 外键冲突），与 Day 12 后端 knife4j 实测的是同一条路径，现在前端也接住了错误提示。

**顺延未做**（不卡验收，Day 16 开工前顺手清）：`<el-switch>` 的 `:loading="loading"`、`<el-pagination>` 的 `:disabled="loading"` 两笔竞态加固；`api/process.ts:29` 形参名 `definitionId` 实为 deploymentId 的命名误导。

> 注：删除失败时后端返回的 HTTP 状态码仍是 `500`（`BizException` 一律映射 500），提示文案友好但状态码不准 —— 属已登记的挂起项 #2「业务异常状态码粒度」，这里应是 409 状态冲突。P8 Day 31 统一处理。

---

## 2026-07-30 · 文档迁移收官：全部集中到 docs 仓库

**起因**：Day 16 的文档写在 `process-front/docs/` 下却塞着后端 VO 改造逻辑 —— 位置本身就是错的。根因是**按前端/后端分目录**，而跨端任务天然装不进去。

**做法**：71 篇文档全部迁入 `core2/process-docs/`（独立仓库 `process-docs.git`），主线（`05` 总控 + `plan/P0–P8` + `测试进度断点` + `变更日志`）上提到根目录（前后端共用），实施文档按 `process-back/` `process-front/` 分放。

**分四步执行，每步停下验证**：
1. 纯复制 71 个文件，原文件不动（`diff -rq` 校验逐字节一致）
2. 修链接：17 个文件、三类改动（去掉写死的 `/docs` 层、给裸引用补前缀、修正跨子树相对路径）
3. 拆 Day 16 那篇（后端部分独立成 `process-back/process_instance/07-`，前端篇留一行链过去）+ 建 `README.md`
4. 代码仓库**新建 `dev` 分支删除 `docs/`**，`master` 保持不变作为完整快照

**本地目录名对齐远程**：`core2/docs/` 改名为 `core2/process-docs/`，与 remote `process-docs.git` 一致。顺带修了代码里 5 处指向文档的注释（`application.yml`、三个 BPMN 模板、`layout/index.vue`）—— 那几处**原本就已过期**（还指向迁移前的 `docs/deep_dive/`），这次一并改对。

**顺带产出 `tools/check-links.sh`**：把每个反引号路径和 markdown 链接拿到磁盘上实际查存在性（不是正则自查），退出码 0/1 可挂 hook。已验证能抓到故意插入的坏链。当前 **276 处引用全部可达**。

**过程中修正的两处自己的错**：
- 第一版检查器把 6 条**该真修的**当误报硬编码忽略了（`待深入主题清单` 表格缺 `hi_tables/` 前缀等），后来逐条真修
- `→ value` 那条是提取器 bug（`generator["throw"](value)` 被当成 markdown 链接），修在提取环节而非忽略清单
- 统计文件数时被 `git ls-files` 的中文路径转义坑过一次，误报"master 只剩 4 个文档"，加 `-c core.quotePath=false` 后实为 48

---

## 2026-07-30 · 文档结构治理

**排期文档按阶段拆分**（原 `05-重写排期计划.md` 324 行 → 总控 87 行 + `plan/` 九篇）：
- 总控 `05` 留：阶段总览（链到详情）、技术栈、组织原则（含 07-13 放弃纵切的决策记录）、**⚠️ 挂起项总表**、风险缓冲、维护约定
- `plan/P0`–`P8` 各装：该阶段的 Day 任务清单 + 该阶段的打卡记录行
- 保留文件名 `05-重写排期计划.md` 不变，故其他文档里「对应排期 Day N」之类的引用全部仍然成立
- 新补的东西：挂起项收口（总控速查表 + `P8` 第三节带来源/方案/验收判据的展开版）、`P4` 的数据库样本节、`P5` 的「开工前现成弹药」节

**断点文档瘦身**（215 行 → ~110 行，且立了防膨胀约定）：
- 拆出本变更日志，历史流水账全部迁入
- 删/归档已过期内容（详见下方「已消化归档」）
- 新增「文档地图」节，标 ★ 的才是开场要读的

**确认的工作方式**：定位进度直接读断点文档，不再扫代码核对。07-30 那次扫描耗时 11 分钟，实测归因是「7 个串行往返 × 537 行文档」，不是文件多——`npm run build` 实测仅 20 秒（`vue-tsc -b` 全量 4.7s + vite 1.5s），解析文档一篇没读。

**Day 15 代码核对**（07-29 记录的四笔问题逐条验过，全部已清零）：

| 原问题 | 现状 |
|---|---|
| 导出名 `activeDefinition` 与导入 `activateDefinition` 对不上 | ✅ 两边统一为 `activateDefinition`（`api/process.ts:25`） |
| 漏 `onMounted(loadList)`，进页面是空表格 | ✅ 已补（`index.vue:138`） |
| URL 错配 `/{id}/suspended`、`/{id}/active` | ✅ 已改为 `/{definitionId}/suspend`、`/activate`，与 `ProcessDefinitionController.java:43,50` 一致 |
| `loadList()` 四处调用无 `await`，rejection 逃逸 | ✅ 按方案 B 在 `loadList` 内部补空 `catch`（`index.vue:80-82`），一处改四处受益 |

另：路由（`router/index.ts:29-33`）与侧栏菜单（`layout/index.vue:25`）也都已加上，`npm run build` 转绿。

---

## 2026-07-29 · 答疑 CLI 会话产出

> 六篇答疑笔记的**完整内容概要已由 `process-front/前端答疑/索引.md` 承担**，此处只记标题与触发点，避免两处重复维护。

| 笔记 | 触发点 | 一句话结论 |
|---|---|---|
| `讲解规则.md`（新建，仓库根） | 把「怎么讲我才最容易懂」固化 | 纵向剥层 + 横向讲史（旧写法→痛点→新方案→代价）+ 可粘贴运行的小实验 + 答疑笔记九节体例 |
| `process-front/前端答疑/模板语法-冒号与@.md` | "一直不知道组件里的 `:` 和 `@` 是啥意思" | `:`=`v-bind:`、`@`=`v-on:`、`#`=`v-slot:`；加不加冒号 = 传字符串字面量还是 JS 表达式求值结果。**地基级，索引第 0 位** |
| `process-front/前端答疑/受控组件与el-pagination.md` | `<el-pagination>` 字段看不懂 | 受控组件模式：组件不存状态不发请求，只显示 + emit。EP 源码级证据：传了 `current-page` 却不监听 `current-change` → 整个组件 `return null` |
| `process-front/前端答疑/配置型子组件与el-table.md` | `<el-table>` 那段逐字段拆 | **`<el-table-column>` 不渲染那一列**——它把自己揉成配置 commit 进父表格 store，真正画 `<td>` 的是 table-body（类比 `@Column` 注解不是控件）。索引第 7 位 |
| `process-front/前端答疑/原生控件的外衣与el-switch.md` | `<el-switch>` 那段逐字段拆 | `@change` handler 里读 `v-model` 变量读到的**一定是新值**（`emit('update:modelValue')` 在前、`emit('change')` 在后且同步）。底层是 `opacity:0` 藏起来的原生 checkbox。索引第 8 位，链条闭合 |
| `process-front/前端答疑/async-await与JS单线程.md`（两版） | "`loadList()` 会不会抢在 `await` 前面执行" | **await 挂起的是「函数」不是「线程」**。第二版把 async 用 `tsc --target es5` 编译出来看真身：`switch (_b.label)` 状态机 + `_b.trys.push` 跳转表解释了 try/catch 凭什么跨越 await 生效。全文时序结论在本机 Node v24.14.1 实跑验证 |

**由此确认并已落地的两个项目待办**：
1. `loadList()` 四处调用无 await 且内部无 catch → rejection 逃逸（已实测复现）。采纳**方案 B**：`loadList` 内部补空 catch，一处改四处受益。**07-30 已落地。**
2. `<el-switch>` 在请求期间没锁，快速连点会并发多请求且响应乱序，`list` 可能被旧结果覆盖。修法 `:loading="loading"`（`switch.vue.mjs:42-44` 里 loading 为真会自动连带 disabled），分页器同理加 `:disabled="loading"`。**零额外代码，待落地。**

---

## 2026-07-21 ~ 07-22 · P3 收尾期产出

- **`process-back/sql/reset-flowable.sql`**：按 `ACT_/FLW_` 前缀 DROP 全部引擎表的重置脚本（保留 SysUser；**必须整段一个批次执行**）
- **三份重测手册**：`process-back/process_deploy/06`（Day 11）、`process-back/process_instance/06`（Day 12）、`process-back/process_task/06`（Day 13，含「恢复现场」步骤 0）
- **`application.yml` 加** `springdoc.swagger-ui.persist-authorization: true`（token 贴一次刷新不丢）
- **`application.yml` 的 Flowable 段重写（07-21）**：把默认配置**显式写出来**并加中文注释，值全部等于官方默认值 → **行为不变**，只是从隐式变可见。标 `[改过]` 的才是真正偏离默认的（`check-process-definitions: false` + 五个子引擎关闭）。默认值取自 `flowable-spring-boot-autoconfigure-7.1.0.jar` 的 `META-INF/spring-configuration-metadata.json`（244 个 `flowable.*` 属性，查看方式写在 yml 末尾注释）。新暴露出来的三项值得记：
  - `activity-font-name` / `label-font-name` / `annotation-font-name` 默认全是 **Arial**（不含中文字形）→ **这就是「中文字体」挂起标记的落点**，要改就改这三项
  - `enable-history-cleaning: false` + `history-cleaning-after: 365d` + cron → 引擎**自带**历史定时清理能力，默认关闭；打开前须确认合规（审批记录常有法定保存期），一般更推荐归档而非直删
  - `async-history-executor-activate` 默认 **true**；异步历史一旦启用，历史会有秒级延迟 → 办完立刻查「我的已审批」可能查不到
- **新登记两个 Day 31 复查项**（现已收进总控挂起项总表与 `plan/P8` 第三节）：接口鉴权粒度、业务异常状态码粒度

---

## 已消化归档

> 以下内容曾在断点文档里占位，现已被别处取代或已落地完成。留档备查，**不必阅读**。

### Day 14 开工前的三条已知情况（已随 Day 14 于 07-22 关闭而消化）

1. `ACT_HI_ACTINST` 连线也进轨迹 —— test-0004 有 5 行（`startEvent`/`flow1`/`managerApprove`/`flow2`/`endEvent`），两条 `sequenceFlow` 的 `ACT_NAME_` 为空。→ **已决策**：后端不过滤，前端 `filter(a => a.activityType !== 'sequenceFlow')`，一份数据两用（P5 图高亮直接吃连线 id）。
2. `ACT_HI_TASKINST.ASSIGNEE_='2'` 已确认 —— 「我的已审批」的数据基础成立。→ **已验证通过**。
3. HI 行发起时即写入（Day 12 的发现）—— test-0005 虽在途，HI 表里都有它，轨迹查得到，只是未完结节点 `END_TIME_` 为空。→ **已成为 Day 14 轨迹接口能查运行中实例的依据**。

另两条 `02` 篇产出的必改项也已落地：轨迹排序加 `TRANSACTION_ORDER_` Java 侧二次排序（查询 API 无对应 orderBy，已查 jar 确认，只能 Java 侧兜底）；`sequenceFlow` 过滤采纳方案②。

### 运行时/历史表全景（已被 `process-back/deep_dive/hi_tables/00-历史表族-宏观.md` 完整取代）

四张运行时表：`ACT_RU_EXECUTION`（在哪走）/ `ACT_RU_TASK`（谁来办）/ `ACT_RU_VARIABLE`（带什么数据）/ `ACT_RU_IDENTITYLINK`（谁和任务是什么关系）。Day 13 实测日志又暴露两张：`ACT_RU_ACTINST`（与 `ACT_HI_ACTINST` 孪生同步，完结时删 RU 留 HI）、`ACT_HI_COMMENT`（claim 记为 `ACTION_='AddUserLink'`）。历史侧验过四张：`ACT_HI_PROCINST` / `TASKINST` / `VARINST` / `ACTINST`。

> 断点文档旧版这里写着「这些讲解目前只在对话里，未落成文档」——该说法自 `process-back/deep_dive/hi_tables/00`（八表全景 + 双写模型 + 历史级别四档 + 四个常见误解）落成后即已过期。
