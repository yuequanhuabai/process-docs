# 双 CLI 工作流

一种用两个 Claude Code CLI 并行协作的方式：一个专心改代码，另一个专心答疑。
本文档随项目走，换电脑也能照做（不依赖本机的 `.claude/MEMORY.md`）。

## 为什么这么做

在同一个 CLI 里既让它改代码、又不停追问原理，会把对话上下文搅得很杂乱，
后续改代码的判断也容易被这些问答带偏。拆成两个 CLI：

- **主 CLI（改代码）**：只干活——改代码、跑测试、提交。上下文干净。
- **问答 CLI（答疑）**：只读——回答"这是什么 / 为什么 / 底层原理"，不碰任何文件。

比起去问 ChatGPT 的好处：问答 CLI 直接在真实项目里读代码、Grep、跟调用链，
不用手动复制粘贴，也共享同一套 `CLAUDE.md`。

## 关键认知：两种"上下文"

| 类型 | 能否共享 | 说明 |
|------|---------|------|
| 代码本身的上下文 | ✅ 能 | 项目文件、`CLAUDE.md`、git 历史、`docs/`——两个 CLI 都能读磁盘 |
| 主 CLI 的对话过程 | ❌ 不能 | 它的推理、试过的方案、脑子里的想法，不落盘就看不到 |

绝大多数疑问的答案在**代码里**，属于第一种，所以问答 CLI 直接读项目就能答。
只有当疑问依赖"主 CLI 改了但还没存盘"的东西时，才需要你手动把那句话/报错贴过去。

## 操作要点

1. **分工**：主 CLI 改代码，问答 CLI 只读答疑。
2. **避免抢文件**：同一个文件别让两边同时改。问答 CLI 只读，天然不冲突；
   主 CLI 改完存盘，问答 CLI 读到的就是最新版。
3. **传递具体上下文**：想让问答 CLI 知道主 CLI 当前纠结的点，
   把那一句话或那段报错复制过去即可，它会顺着代码往下查。
4. **主 CLI 勤存盘**（它本来就会），问答 CLI 读到的才是最新代码。

## 问答 CLI 起手提示词

开问答 CLI 时，粘贴 [`问答CLI起手提示词.md`](./问答CLI起手提示词.md) 里的提示词，
它会进入"只读 + 剥层讲原理 + 不代劳"的模式。
