# 指令码 · 会话触发规则（跨机器生效）

> **这是什么**：一句固定的话 = 一套固定的动作。说出触发语，CLI 就按下面的清单执行，不必每次重复叮嘱前置步骤。
>
> **为什么写在仓库里而不是写进 CLI 的记忆**：CLI 的本地记忆是**单机**的，换台电脑就是空白上下文。本文件随 `process-docs` 提交，clone 到哪台机器都在。
>
> **给 CLI 的话**：本文件的规则是**硬约束**，优先级等同用户当场下达的指令。命中触发语时不要询问确认，直接按顺序执行。

---

## 指令码 #1 · 进度定位

### 触发语（原文）

> 基于当前目录下的 process-docs 下的「05-重写排期计划.md」以及「测试进度断点.md」文档以及对应的实际前后端代码，定位一下当前项目的进度；

**匹配方式：语义匹配，不要求逐字。** 只要意思是「读这两篇文档 + 对照前后端代码 + 定位当前进度」，就算命中——引号用什么样式、标点半角全角、有没有「帮我」「顺便」之类的口语壳子，一律不影响。

### 执行动作（顺序固定，不得跳步）

#### 第 1 步 · 先拉三个仓库

对 `process-back`、`process-docs`、`process-front` **各跑一次 `git pull`**。

- 拉之前先看当前分支和 `git status`；
- **工作区有未提交改动就停下来先告诉用户**，不许自作主张 `stash` / `reset` / `checkout --` / 强拉覆盖；
- 分支约定：`process-back` 与 `process-front` 在 **`dev`**，`process-docs` 在 **`master`**。分支不对先报告，别擅自切；
- 只 `pull`，**不 `push`、不 `commit`**。

> ⚠️ 这一步是**用户已经授权的固定前置**，不要再问「要不要先拉代码」。

#### 第 2 步 · 再定位

读 [`05-重写排期计划.md`](./05-重写排期计划.md) + [`测试进度断点.md`](./测试进度断点.md)，必要时连带当前阶段那一篇 [`plan/P*.md`](./plan/)，**并对照 `process-back` / `process-front` 的实际代码做交叉核对**（文件是否存在、字段是否已加、路由菜单是否已挂、最新提交是哪笔）。

> 📌 平时「问进度直接读断点文档、别扫代码核对」——**但本指令码明确要求交叉核对**，这一条在此处不适用。

#### 第 3 步 · 输出三件事

1. **当前阶段 / Day 位置**（哪个 P、哪个 Day、整体完成了多少块）；
2. **文档与代码是否一致**——不一致要**点名**说清差在哪个文件、哪个字段；
3. **下一步该做什么**（含开工前的已知坑与提醒）。

---

## 维护约定

- **新增指令码**：在本文件按 `## 指令码 #N · 名称` 追加一节，写全「触发语 / 匹配方式 / 执行动作」三块；
- **改动后同步**：本文件的规则同时被 [`CLAUDE.md`](./CLAUDE.md) 摘要引用，改了执行动作要两边对齐；
- **别把指令码写进 CLI 的本地记忆**——单机失效，是本文件存在的全部理由。
