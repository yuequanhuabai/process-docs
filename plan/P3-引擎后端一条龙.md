# P3 · 引擎后端一条龙（Day 10–14）

> 状态：**✅ 已关闭**（Day 10–14 全部实测通过，2026-07-22） ｜ 端：后端 ｜ 主题：流程定义 + 实例 + 待办 + 历史（knife4j / curl 验收）
> 返回 [总控进度](../05-重写排期计划.md) ｜ 当前位置见 [测试进度断点](../测试进度断点.md)

> 本阶段落地手册：`../process-back/process_deploy/`（Day 11）、`../process-back/process_instance/`（Day 12）、`../process-back/process_task/`（Day 13）、`../process-back/process_history/`（Day 14）。
> 配套学习支线：`../process-back/deep_dive/hi_tables/`（历史表族深挖，差 `05-外围四表` 收官）。

---

## 一、任务清单

**Day 10 · 后端：Flowable 配置 + 自动部署**　✅
- [ ] ⚠️ **中文字体挂起标记**（2026-07-09 决策，Day 31 复查）：`config/FlowableConfig` 中文字体不影响引擎功能，只影响后端生成流程图图片时的中文显示；BPMN 全部完成后再回头看（前端 bpmn-js 渲染不经后端出图，可能根本用不上）
- [x] `resources/processes/` 目录 + `check-process-definitions` 自动部署验证（`leave.bpmn20.xml`：employee 发起（`flowable:initiator`）→ manager 审批（`candidateGroups="manager"`）→ 结束，含 DI 坐标供 bpmn-js 渲染）
- [x] **验收**：2026-07-09 启动实测——引擎自动建表 + 自动部署 `leave.bpmn20.xml`，日志确认 `ACT_RE_PROCDEF` 插入 `leaveProcess:1`、`ACT_RE_DEPLOYMENT`/`ACT_GE_BYTEARRAY` 各入库，启动 14.5s 无报错
- [x] **验收后决策**（2026-07-09，提交「关闭自动部署模板」）：`check-process-definitions` 改回 `false`——模板已入库验证完毕，后续部署统一走 Day 11 的手动部署接口，避免每次启动重复扫描；`leave.bpmn20.xml` 保留在 `resources/processes/` 作为固定模板

**Day 11 · 后端：流程定义接口**　✅
- [x] `ProcessDefinitionService` / `Impl` / `Controller`：部署（multipart 上传 BPMN 文件而非 JSON 塞 XML）、分页列表（latestOnly 可选，手动装 MP `Page` 外壳对齐前端 `PageResult`）、按 deploymentId 删除（cascade 可选）、挂起/激活（防重复调用）+ `ProcessDefinitionVO`（id 天生字符串，无需 ToStringSerializer）
- [x] 附带：`OpenApiConfig` 加 `bearerAuth` 安全声明 → knife4j 出 Authorize 按钮，粘一次 token 全接口生效（原手册文件 5）
- [ ] ⚠️ **接口鉴权粒度挂起标记**（2026-07-21 发现，Day 31 复查）：当前 `LoginInterceptor` 只做「登录与否」的**认证**，没有任何「能干什么」的**授权**——即任何登录用户都能调 `deploy` / `deleteDeployment` / `suspend` 等管理类接口（实测 admin 部署成功，但 employee 同样能部署，接口层无角色校验）。演示无碍，生产不可接受。<br>可选方案：① `@PreAuthorize` 风格自定义注解 + 拦截器读 `UserContext.role` 校验（轻量，契合现有 ThreadLocal 方案）；② 引入 Spring Security 做完整 RBAC（重，与当前手写 JWT 方案冲突）。倾向 ①，Day 31 决策。<br>影响面：`ProcessDefinitionController` 全部写接口；`ProcessInstanceController.terminate`（当前任何人可终止任何实例）；`SystemUserController` 全部 CRUD（当前 employee 可增删改用户）
- [x] **验收**：2026-07-13 knife4j 实测 5 接口全通——重复部署同文件得 `leaveProcess` v2（同 key 升版）；挂起/激活实测 `ACT_RE_PROCDEF.SUSPENSION_STATE_` 1↔2；删除按 deploymentId 三表（DEPLOYMENT/BYTEARRAY/PROCDEF）连清。落地手册在 `../process-back/process_deploy/`
- 待补测：`cascade=false` 遇在途实例的冲突分支，需 Day 12（实例接口）后回头验（已排进 Day 12 清单）

**Day 12 · 后端：流程实例接口**　✅ 代码 + knife4j 实测全通过（2026-07-21 关闭）
- [x] `ProcessInstanceService` / `Impl` / `Controller`：发起（`start`，`Authentication.setAuthenticatedUserId` 接线记发起人 + 预检定义不存在/已挂起）、运行实例分页（`page`，可按 definitionKey 过滤）、我的发起（`my-started`，`startedBy` 过滤）、终止（`DELETE {instanceId}`，带原因，`deleteProcessInstance`）
  - 说明：我的发起复用 `ProcessInstanceVO`，未单列计划里的 `MyStartedVO`；分页仍是 `count()+listPage()` 装 MP `Page` 外壳（同 Day 11）。落地手册在 `../process-back/process_instance/`
- [x] Day 11 尾巴 + terminate 两笔欠账已清（2026-07-21）：`cascade=false` 遇在途实例走到友好报错分支（`ProcessDefinitionServiceImpl` 那段 catch 首次真正被执行，底层是 `PROC_DEF_ID_` 外键冲突）；`cascade=true` 三表 COUNT 全 0（连历史一并抹除）；`terminate` 清 RU 保 HI、`DELETE_REASON_` 有值、对已终止 id 再删报「运行中的流程实例不存在」
- [x] **验收（已完成）**：2026-07-21 knife4j 五步实测全通 —— 发起 + 四张 `ACT_RU_*` 核对、异常三连、查询两连（分页切片 total 恒定 / `definitionKey` 过滤 0 条不报错）、**隔离对照**（employee `my-started` 3 条 vs admin 0 条，而 admin `page` 仍 3 条）、终止 + cascade 两分支。逐项见 `../process-back/process_instance/04`
  - 意外收获：终止**前**查 `ACT_HI_PROCINST` 发现该行**早已存在**（`END_TIME_` 空、`REV_=1`）→ HI 行在**发起时**即写入，完结只是回来 UPDATE。故「RU 活 / HI 死」不准确，准确说法是 **RU 只装在途的、HI 装全部（含在途）** —— 这正是 Day 14 历史轨迹接口能查运行中实例的依据
  - ⚠️ 遗留：步骤 5 的 cascade 把库清空了，Day 13 开工前需先重新部署 `leave.bpmn20.xml` + 发起一条 `test-0004`（见 `../测试进度断点.md`）

**Day 13 · 后端：待办任务接口**　✅ 代码 + knife4j 实测全通过（2026-07-21 关闭）
- [x] `FlowableTaskService` / `Impl` / `TaskController`：待办列表（`todo`，`or().taskCandidateGroup(role).taskAssignee(userId)` 双条件）、任务详情（`GET {taskId}`，`TaskFormVO` 带流程变量）、办理（`complete`，未认领先 `claim` 再 `complete`）+ `CompleteTaskRequest` / `TaskVO`
  - 附带：`assertCanOperate` 越权校验（已认领只本人可操作 / 未认领须角色在候选组）；自动认领是为兑现 `ACT_HI_TASKINST.ASSIGNEE_` 有值，供 Day 14「我的已审批」查。落地手册在 `../process-back/process_task/`
- [x] **验收（已完成）**：2026-07-21 按 `../process-back/process_task/06` 全程实测 —— 恢复现场（重部署 + 发 test-0004）→ 待办**三账号隔离 manager1/employee0/admin0** → 详情（3 变量、formKey 空）→ **越权四连**（employee 拿泄漏 taskId 调 detail/complete 均被拒 + id 不存在 + `@NotBlank`）→ 办理（日志 `assignee=2`，RU 四表归零，`ACT_HI_TASKINST.ASSIGNEE_='2'` **命门通过**）。逐项见 `../process-back/process_task/04`
  - 收获①：运行时**不止四张表** —— 日志暴露 `ACT_RU_ACTINST`（与 `ACT_HI_ACTINST` 孪生同步，完结时删 RU 留 HI）与 `ACT_HI_COMMENT`（claim 动作记为 `AddUserLink`）
  - 收获②：`ACT_HI_ACTINST` **连线也进轨迹**（5 行含 flow1/flow2，`ACT_NAME_` 为空）→ 影响 Day 14，已在 Day 14 条目登记待决策
  - 收获③：只有 userTask 有 `ASSIGNEE_` —— 这正是「我的已审批」查 `ACT_HI_TASKINST` 而非 `ACT_HI_ACTINST` 的原因
  - 踩坑：`@NotBlank` 差点漏测两次 —— swagger 前端必填校验会拦下空字段（请求发不出去）、传字面量 `""` 是长度 2 的非空串；**正确造法是传一个空格**
  - 遗留：Day 14 备料已就绪 —— 办结实例 test-0004 `175ffafb-…` + 在途实例 test-0005 `6a897d3d-…`（轨迹 3 行、`managerApprove` 的 `END_TIME_` 为空）

**Day 14 · 后端：历史接口 + 引擎链路整体冒烟**　✅ 代码 + curl 实测全通过（2026-07-22 关闭，**P3 关闭**）
- [x] `FlowableHistoryService` / `Impl` / `HistoryController`：我的已审批（`GET /history/approved` → `IPage<ApprovedTaskVO>`）、实例历史轨迹（`GET /history/instance/{instanceId}/activities` → `List<ActivityTraceVO>`，运行中实例也可查）。落地手册在 `../process-back/process_history/`
- [x] ✅ **已落地：轨迹排序加 `TRANSACTION_ORDER_` 二次排序**（2026-07-22）：`activities()` 在 `.orderByHistoricActivityInstanceStartTime().asc()` 之上，Java 侧再 `.sorted(comparing(getStartTime).thenComparing(getTransactionOrder, nullsLast(naturalOrder())))`。**实测印证**：test-0005 与新发 test-0006 的 `startEvent`/`flow1`/`managerApprove` 三行 `START_TIME_` 撞在同一秒，二次排序后顺序稳定为 `startEvent→flow1→managerApprove`。查询 API 无 `TRANSACTION_ORDER_` 排序方法已再次确认（编译期 `getTransactionOrder()` 存在），故只能 Java 侧兜底。详见 `../process-back/deep_dive/hi_tables/02`
- [x] ✅ **已定：轨迹接口不过滤 `sequenceFlow`（采纳方案②）**（2026-07-22）：后端 `activities()` 如实返回引擎走过的每一步（办结实例 5 行含 `flow1`/`flow2`，`activityName` 为空），过滤交给前端 `filter(a => a.activityType !== 'sequenceFlow')`——`ActivityTraceVO` 已有 `activityType` 字段。一份数据两用：轨迹列表前端过滤连线，P5 的 BPMN 图路径高亮直接吃连线 id。后端零改动。
- [x] 整体冒烟：curl 双 token（employee 发起 / manager 审批）走通 发起 → 待办 → 办理 → 历史 完整生命周期，亲眼见数据从 `ACT_RU_*` 搬进 `ACT_HI_*`（办理后 `process-instance/page` 里 test-0006 从 RU 消失、`approved` 与 `activities` 接住）
- [x] **验收**：2026-07-22 curl 全程实测通过 —— `approved` 三账号隔离（manager 1→2 条 / employee 0 / admin 0，`assignee='2'`、`endTime` 有值、`durationInMillis` 毫秒、`deleteReason=null`）；`activities` 办结实例 5 行全有 `endTime`、在途实例（test-0005）停 `managerApprove` 且 `endTime=null`、终止实例（test-0007）3 行无 `endEvent`；任意 token 可查读接口；二次排序化解同秒撞车。**P3 关闭**
- 收获：test-0004 那条历史任务 `name` 显示乱码（`ACT_HI_TASKINST.NAME_` 旧数据编码问题），但**全新 test-0006 的 `name` 中文正常**→ 确认是那条遗留数据的问题，非接口逻辑缺陷（前端 P4「我的已审批」渲染 test-0004 会见到该乱码行，属既有脏数据，可忽略或后续清理）
- 踩坑：curl 请求体带中文经 Git Bash 会以 GBK 发出，触发后端 `Invalid UTF-8 middle byte` → `code:500`（这是**前端编码**问题不是接口 bug；实测改用纯 ASCII 变量值绕过）。另注：`HttpMessageNotReadableException` 当前落到 500 兜底，属 Day 31「异常状态码粒度」范畴

---

## 二、打卡记录

| Day | 主题 | 端 | 实际完成度 | 受阻/备注 |
|-----|------|----|-----------|-----------|
| 10 | Flowable 配置/部署 | 后 | ✅ 100% | 配置早已就位（`check-process-definitions`/`database-schema-update`/子引擎关闭）；新增 `processes/leave.bpmn20.xml` 固定模板；启动实测自动建表+部署成功（`leaveProcess:1` 入 `ACT_RE_PROCDEF`）。验收后已把 `check-process-definitions` 关回 `false`（模板已入库，后续走 Day 11 手动部署接口）。中文字体挂起 ⚠️（不影响功能，Day 31 复查） |
| 11 | 流程定义接口 | 后 | ✅ 100% | VO/Service/Impl/Controller 全就位 + `OpenApiConfig` 加 Authorize 按钮；2026-07-13 knife4j 实测 5 接口全通（重复部署升版 v2、SUSPENSION_STATE_ 1↔2、按 deploymentId 三表连清）。落地手册沉淀在 `../process-back/process_deploy/`；另建 `../process-back/deep_dive/待深入主题清单.md` 记了 2 个待剥主题（Flowable 命令模式、LambdaQueryChainWrapper 改造）。cascade 冲突分支待 Day 12 补测 |
| 12 | 流程实例接口 | 后 | ✅ 100% | VO/Service/Impl/Controller 全就位（start/page/my-started/terminate 四接口），`start` 用 `Authentication` 记发起人、`my-started` 用 `startedBy` 过滤；我的发起复用 `ProcessInstanceVO`（未做 `MyStartedVO`）。代码落于 2026-07-14。**2026-07-20~21 按 `../process-back/process_instance/06` 五步实测全通并关闭**：发起+四表核对 / 异常三连 / 查询两连（分页 total 恒定、过滤 0 条不报错）/ 隔离对照（employee 3 条 vs admin 0 条，admin `page` 仍 3 条）/ 终止+cascade 两分支。terminate 与 Day 11 cascade 两笔「已写未测」欠账**均已清零**。收获：HI 行发起时即写入（非完结才搬），RU 只装在途、HI 装全部。⚠️ cascade 清空了库，Day 13 前需重部署+发 test-0004 |
| 13 | 待办任务接口 | 后 | ✅ 100% | Service/Impl/Controller 全就位（todo/detail/complete），`complete` 未认领先 claim 再办理、`assertCanOperate` 越权校验。代码落于 2026-07-16。**2026-07-21 按 `../process-back/process_task/06` 全程实测并关闭**：三账号隔离（manager1/employee0/admin0）、越权四连、办理后 RU 四表归零 + `ACT_HI_TASKINST.ASSIGNEE_='2'` 命门通过。收获：运行时不止四表（`ACT_RU_ACTINST`/`ACT_HI_COMMENT`）、连线也进轨迹（影响 Day 14）、只有 userTask 有 ASSIGNEE_。新登记两个 Day 31 复查项（接口鉴权粒度 / 异常状态码粒度）。Day 14 备料就绪：办结 test-0004 + 在途 test-0005 |
| 14 | 历史接口+链路冒烟 | 后 | ✅ 100% | 2026-07-22 落地两决策点 + curl 全程实测关闭。**决策1**：`activities()` 加 `TRANSACTION_ORDER_` Java 侧二次排序（化解同秒撞车，实测 startEvent→flow1→managerApprove 顺序稳定）；**决策2**：不过滤 `sequenceFlow`（采纳方案②，前端过滤，一份数据两用）。实测：`approved` 三账号隔离（mgr 1→2/emp 0/admin 0，assignee='2'、毫秒 duration、deleteReason null）、`activities` 办结5行/在途停 managerApprove(endTime null)/终止无 endEvent、任意 token 可查、整体冒烟发起→办理→历史 RU→HI 迁移亲验。收获：test-0004 旧任务 name 乱码属遗留脏数据，新 test-0006 中文正常，非接口 bug。**P3 关闭** |
