# P0 · 地基（Day 1–4）

> 状态：**✅ 已关闭** ｜ 端：双端 ｜ 主题：后端基础设施收尾 + 前端工程脚手架 + 前端基座
> 返回 [总控进度](../05-重写排期计划.md) ｜ 当前位置见 [测试进度断点](../测试进度断点.md)

---

## 一、任务清单

**Day 1 · 后端基础设施收尾**　✅
- [x] `common/GlobalExceptionHandler`：参数校验 / 业务异常 / 兜底 → 统一 `R`
- [x] Knife4j/OpenAPI 配置，`/swagger-ui.html` 可访问
- [x] `config/MybatisPlusConfig`：分页插件 `PaginationInnerInterceptor(SQL_SERVER)`
- [x] 修 `SystemUserController`：补 `@GetMapping`、返回类型用 `R<IPage<SysUser>>`（分页，自带 total）
- [x] **验收**：非法参数返回统一 `R` 错误结构；分页查询返回 `total`（编译通过；运行期 swagger 冒烟待连库）

**Day 2 · 前端工程脚手架（npm init -y 起步）**　✅
- [x] 新建前端工程目录，`npm init -y`，装 vite / vue / typescript / @vitejs/plugin-vue / vue-tsc
- [x] 装运行时依赖：element-plus / @element-plus/icons-vue / pinia / vue-router / axios / sass
- [x] `vite.config.ts`（proxy 转发 `/workflow` → `http://localhost:9090`）、`tsconfig`、目录骨架（已建 api/utils/router/styles/views；components/stores/layout 留待 Day 3）
- [x] `main.ts` 挂载 Element Plus + Pinia + Router
- [x] **验收**：`npm run dev` 起一个空首页，控制台无报错（`vite build` 亦通过；已删 `tsconfig` 弃用的 `baseUrl` 修复 `npm run build`）

**Day 3 · 前端基座：HTTP 管道 + 路由 + 布局**　✅
- [x] `utils/request.ts`：axios 实例（注入 JWT、统一拆 `R<T>`、401 跳登录、非 JSON 不拆包）
- [x] `router/`：路由表 + 登录守卫（按 `fe_token` 拦截）；受保护页统一挂在 layout 子路由下
- [x] `layout/`：整体布局（el-container 侧栏 + 顶栏 + 退出）+ 侧边栏菜单壳
- [x] `stores/user.ts`：登录态/用户信息骨架（token 落 localStorage，setToken/setUserInfo/logout）
- [x] **验收**：访问受保护路由被守卫挡到 `/login`（守卫逻辑已实现，`vite build` + dev 启动均通过；浏览器实跳转建议手动确认一次）

> 附带：login 页做了占位（含「模拟登录」按钮写假 token 验证放行 + redirect 回原页，Day 7 换真实登录）；`.gitignore` 补忽略 `*.tsbuildinfo`。统一 token 键名为 `fe_token`（request / router / store 三处一致）。

**Day 4 · 双端地基联调缓冲**　✅
- [x] 前端 proxy 打通后端接口：`api/user.ts` 封装 `pageUsers()`，经 vite proxy `/workflow` → 9090 拿到 `/user/page` 真实数据
- [x] 统一返回结构前后端对齐：前端 `PageResult<T>`（records/total/current/size）对齐后端 `IPage`；`request.ts` 拆包读 `R.msg` 与后端 `R` 一致
- [x] **验收**：`Home.vue` 在 `onMounted` 调 `pageUsers()` 并用 `el-table` 渲染出用户列表（提交 `front query ok` 佐证连库成功）

---

## 二、打卡记录

| Day | 主题 | 端 | 实际完成度 | 受阻/备注 |
|-----|------|----|-----------|-----------|
| 1 | 后端基础设施收尾 | 后 | ✅ 100% | 验收通过（`mvnw clean compile` 成功）。超额：`SystemUserController` 已做完整 CRUD（本属 Day 8），分页用 `R<IPage>`（自带 total），异常处理覆盖 5 类，额外加防全表插件 |
| 2 | 前端脚手架 | 前 | ✅ 100% | 验收通过（`npm run dev`/`vite build` 均 OK）。修了一处隐患：`tsconfig.app.json` 删除已弃用的 `baseUrl`，`npm run build` 由失败转绿。HTTP 管道提前完成，已 `git mv` 至 `utils/request.ts`（与计划一致）；骨架缺 `components/stores/layout`（Day 3 补） |
| 3 | 前端基座 | 前 | ✅ 100% | request 升级（401 跳登录 + 非 JSON 不拆包）；`stores/user.ts`、`layout/`、`views/login`（占位+模拟登录）、路由守卫全部就位；token 键统一 `fe_token`。`build`+dev 启动均绿；浏览器实跳转待手动确认 |
| 4 | 双端地基联调 | 双 | ✅ 100% | 前端 `api/user.ts` + `Home.vue` 经 proxy 调通后端 `/user/page` 并 `el-table` 渲染；前后端返回结构（`R`/`IPage`）已对齐 |
