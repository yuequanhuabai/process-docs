# P2 · 用户管理（Day 8–9）

> 状态：**✅ 已关闭**（2026-07-07 admin 浏览器实测增改删通过） ｜ 端：双端 ｜ 主题：SysUser CRUD + 用户管理页
> 返回 [总控进度](../05-重写排期计划.md) ｜ 当前位置见 [测试进度断点](../测试进度断点.md)

---

## 一、任务清单

**Day 8 · 后端：SysUser 完整 CRUD**　✅
- [x] 列表（分页）/ 详情 / 新增 / 修改 / 删除；按用户名查询用 `LambdaQueryWrapper`
- [x] **验收**：swagger 全部走通，id 为整型正常返回
- 说明：page/create/update/delete 早在 Day 1 超额做完（见打卡）；`GET /user/{id}` 详情接口 2026-07-07 补齐，三层就位、`mvnw compile` 通过。
- 2026-07-07：page/detail 改回 `SysUserVO`（继承 `UserInfoVO` 补 managerId/enabled/createTime，**不含 password**），密码泄漏欠账已清；`IPage.convert(SysUserVO::of)` 逐条转、分页元信息不变

**Day 9 · 前端：用户管理页**　✅
- [x] `views/user/index.vue` 表格（角色中文映射 + 启用状态 tag + 分页）+ 新增/编辑同一弹窗（编辑态 username 只读、隐藏密码、多出启用开关）+ 删除二次确认；`api/user.ts` 补齐 get/create/update/delete
- [x] 路由 `/user` 挂到 layout 子路由；侧栏菜单加「用户管理」
- [x] 契约对齐：`CreateUserBody`/`UpdateUserBody` 对齐后端 `CreateUserRequest`/`UpdateUserRequest`；managerId 空串→null，用字符串传防雪花丢精度
- [x] **验收（代码级）**：`npm run build`（含 vue-tsc）通过，`user-*.js` chunk 产出
- [x] **验收（浏览器实测）**：2026-07-07 admin 登录实测增/改/删均 OK，列表正常渲染（三种子账号可见）。**P2 关闭**

---

## 二、打卡记录

| Day | 主题 | 端 | 实际完成度 | 受阻/备注 |
|-----|------|----|-----------|-----------|
| 8 | SysUser CRUD | 后 | ✅ 100% | page/create/update/delete Day 1 已超额做完；2026-07-07 补 `GET /user/{id}` 详情（Service `getUserById` 不存在抛 BizException）；同日 page/detail 改回 `SysUserVO`（不含 password）清掉密码泄漏欠账，`mvnw compile` 通过 |
| 9 | 用户管理页 | 前 | ✅ 100% | `views/user/index.vue`（表格+分页+增删改弹窗+删除确认）+ `api/user.ts` 补 get/create/update/delete + 路由/侧栏菜单；`npm run build` 通过。2026-07-07 admin 浏览器实测增改删 OK，**P2 关闭** |

---

> 📌 **P2 的页面是 P4 四个页面的模板**：表格 + 分页 + 操作列 + 弹窗这套结构，`views/process` 等全部同构复用。
