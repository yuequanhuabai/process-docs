# P4 · 业务前端批量（Day 15–18）

> 状态：**🔄 进行中** —— Day 15 已关闭；**Day 16 已 5.5/6，后端全关闭，实例页已完工、路由菜单已齐，myStart 页写到第 ② 步，欠它与实测**（2026-08-10 核对代码） ｜ 端：前端为主，Day 16 双端 ｜ 主题：流程管理 / 实例·我的发起 / 待办 / 已审批·历史 四个页面
> 返回 [总控进度](../05-重写排期计划.md) ｜ **当前位置与下一步以 [测试进度断点](../测试进度断点.md) 为准**

> 四个页面全部同构于 `views/user`（表格 + 分页 + 操作列 + 弹窗），复用 `PageResult<T>` 与 request 管道。
> ⚠️ 注意：流程域 id 全是字符串（TS 里别声明成 number）；删除传 deploymentId、挂起/激活传 definitionId，别接反。
> 指引文档：`../process-front/process_manage/` 四篇（Day 15）。学习方式是**你写、我答**，不代写。

---

## 一、任务清单

**Day 15 · 前端：流程管理页**　✅ **已关闭**（2026-07-30 浏览器实测通过）
- [x] `views/process/index.vue` 流程管理列表（表格/分页/挂起激活/删除二次确认）+ `api/process.ts`（page / deleteDeployment / suspend / activate；deploy 留给 Day 22 设计器接）
- [x] 路由挂 layout 子路由（`router/index.ts:29-33`）+ 侧栏菜单项（`layout/index.vue:25`）
- [x] 编译转绿 —— ① `activateDefinition` 命名两边一致（`api/process.ts:25`）；② `onMounted(loadList)` 已补（`index.vue:138`）；`npm run build`（`vue-tsc -b` + `vite build`）2026-07-30 实跑通过
- [x] URL 已对齐后端：`/{definitionId}/suspend`、`/{definitionId}/activate`（`ProcessDefinitionController.java:43,50`）。delete 路径与 `row.deploymentId` 传参本就是对的（`api/process.ts:29` 形参名仍叫 `definitionId`，命名误导不影响运行）
- [x] `loadList` 内部已补空 `catch`（`index.vue:80-82`），四处调用点的 rejection 不再逃逸——采纳的是方案 B，原理见 `../process-front/前端答疑/async-await与JS单线程.md` 第七、十节
- [ ] ⏳ **顺延未做**：并发竞态——给 `<el-switch>` 加 `:loading="loading"`、分页器与操作按钮加 `:disabled="loading"`（`loading` ref 已现成）。不卡验收，Day 16 开工前顺手做掉。`await` 治不了这个，见 `../process-front/前端答疑/请求竞态与控件禁用.md`（§5 实验 3 专证这一点）
- [x] **验收（浏览器实测）**：2026-07-30 前后端起起来，登录实测 —— **查询 / 挂起 / 激活三项 OK**；**删除带在途实例的部署被拦下并友好报错**（`cascade=false` 走 catch 分支，底层 `PROC_DEF_ID_` 外键冲突，与 Day 12 后端实测的同一条路径），**这正是验收标准要的行为**。**Day 15 关闭**

**Day 16 · 双端：实例 + 我的发起**　🔄 **5.5/6，后端已全部关闭，实例页与路由菜单已完工，欠 myStart 页与实测**

> ✅ **决策已定（2026-07-30）：采纳方案①** —— 后端 `myStarted()` 改查 `HistoricProcessInstanceQuery.startedBy()`，在途+办结+终止一页看全，配状态筛选。
> 理由：① 产品上是主流做法（钉钉/飞书审批「我发起的」都是单页+状态筛选，用户心智是"我提交过的申请单"，不该暴露引擎 RU/HI 分表）；② 总代码量反而更少（拆两页要两个路由/两个组件/两个 api/**外加一个新后端接口**）；③ 那个带 `endTime`/`status` 的 VO 躲不掉，Day 17/18 还要接着用。
> **代价（已接受）**：Day 16 从纯前端变双端，工作量约 1.5 个块。

**落地文档（先后端后前端）**：
- 后端原理：[`../process-back/process_instance/07-Day16-查询改造为历史表.md`](../process-back/process_instance/07-Day16-查询改造为历史表.md)
- 后端落地：[`08-Day16-操作步骤.md`](../process-back/process_instance/08-Day16-操作步骤.md)（步骤+坑）+ **[`10-Day16-完整代码-无泛型版.md`](../process-back/process_instance/10-Day16-完整代码-无泛型版.md)（← 当前采用）**
  - [`09-Day16-完整代码.md`](../process-back/process_instance/09-Day16-完整代码.md)（泛型版）**暂不采用**，留作将来重构目标；换回去只动 4 处，清单在 `10` 第 5 节
- 前端：[`../process-front/process_instance/01-页面定位与修改点.md`](../process-front/process_instance/01-页面定位与修改点.md) + [`组件选型解析`](../process-front/process_instance/组件选型解析.md) + [`原型`](../process-front/process_instance/原型-实例与我的发起.html)

- [x] **后端**：`ProcessInstanceVO` 补 5 字段（`endTime`/`status`/`deleteReason`/`startUserName`/`currentActivityName`）+ 加 `of(HistoricProcessInstance)` 工厂方法（判据写在里面）—— ✅ 已提交 `5dabc3b`
- [x] **后端**：`myStarted()` 改查 HI；抽 `enrich()` 批量填姓名与当前节点（**别写成 N+1**），`page()` 复用但**继续查 RU** —— ✅ 已提交 `122418d`（`ProcessInstanceServiceImpl.java:105-108` 走 `startedBy()`；`enrich()` 在 `:194`，整页固定 2 次查询）
- [x] **后端**：~~`toPage()` 泛型化~~ → **改为新增 `toHiPage(HistoricProcessInstanceQuery, long, long)`**（与 `toPage` 逐行同构，只有 Query 类型和元素类型不同，末尾同样调 `enrich()`）。**零泛型声明**，`page()` 的 return 行真·零改动。原因见 `10` 文档开头：两个 Query 接口无继承关系，想合并只剩泛型一条路，而泛型不值得卡住主线 —— ✅ 同上（`toPage:131` / `toHiPage:180`，各 5 行）
- [x] **后端超出清单顺手做掉的两笔**（同在 `122418d`）：`applyStatusFilter()`（`:146-161`，if/else 三态过滤，未知值抛 `BizException`）；Controller `my-started` 加 `status` 入参（`ProcessInstanceController.java:38-46`）
- [x] **契约改形**：`terminate` 从 `DELETE /process-instance/{instanceId}` 改成 `{instanceId}/terminate`，去对齐前端 `api/instance.ts:55` —— ✅ 已提交 `30a7314`（2026-08-05）。⚠️ **后端需重启一次**才生效
- [ ] **后端验收**：knife4j 确认 `my-started` 能返回已办结/已终止的实例，三态 `status` 正确，`startUserName` 是姓名不是 userId　←（代码已完工，**这一步只欠实测**；无实例样本，得先造）
- [ ] **前端**：`views/instance` + `views/myStart` + `api/instance.ts` + 路由 2 条 + 菜单 2 项
  - [x] `api/instance.ts` 完工（`68eb001`）：59 行 / 4 函数（`pageInstances` / `myStarted` / `startInstance` / `terminateInstance`）/ 3 类型（`InstanceStatus` / `ProcessInstance` / `StartInstanceBody`），与 Controller 四个签名逐个核对无错配
  - [x] ✅ `views/instance/index.vue` **已完工**（178 行，08-06 一天写完，`548f236`→`5ccf065` 六个提交，**本人从零写、CLI 只答疑不代写**）：按 `页面思考法` ① 布局 → ② 组件 → ③ 样式三步走完；`npm run build`（含 `vue-tsc -b`）**08-07 实跑转绿**。含分页查询 / 流程下拉过滤（复用 `pageDefinitions`）/ `ElMessageBox.prompt` 终止（`inputValidator` 非空 + 删末页回退）/ `onMounted` 双加载；两处控件都挂了 `:disabled="loading"`（竞态本页已自愈）；按 `02-填充组件.md` 的决定砍掉「实例 ID」列。**这一版的 4 div 结构（`.instance-page` > `.toolbar`/`.table-wrap`/`.pager-wrap`）是 P4 后面三页的模板**
  - [ ] 🔄 `views/myStart/index.vue` **已起头 74 行**（`e15e2fa`，08-07）　←**当前光标**。① 布局 ✅（4 div 结构照实例页搭好）；② 组件 🔄 只到状态下拉空壳——`el-select` 未绑 `v-model="status"`、`statusOptions` 还注释着（`:35-40`）、表格与分页两个 div 仍空、发起弹窗未写；③ 样式 ⬜（`.title` 选择器选不中，模板那行是 `<h2>`）。数据侧 `loadList()` 已写对但**缺 `onMounted` 调用**
  - [x] 路由 `/instance`（`router/index.ts:34-38`）+ 菜单「运行中的实例」（`layout/index.vue:28`）
  - [x] 路由 `/my-start`（`router/index.ts:39-43`）+ 菜单「我的发起」（`layout/index.vue:29`）—— `ada25b4`，URL 短横线 / 目录小驼峰 / 子路由不带前导斜杠三条约定均写对
- [ ] **前端**：发起弹窗（`el-dialog`+`el-form`，第一次向后端提交表单）—— **归属 `views/myStart`**（`01-页面定位与修改点.md:58`），随下一页一起做；~~终止用 `ElMessageBox.prompt`（要填 reason）~~ ✅ **已在实例页落地**（`index.vue:98-127`）
- [ ] ⏳ **Day 15 顺延来的两笔，仍未做**（2026-08-10 查代码复核，第四次记）：`views/process/index.vue:5-9` 的 `<el-switch>` 加 `:loading="loading"`、`:39-46` 的 `<el-pagination>` 加 `:disabled="loading"`。📌 同一件事在实例页已经做到了（`views/instance/index.vue:11`、`:50`），欠的只是回头补流程管理页。📖 **原理已成文**：`../process-front/前端答疑/请求竞态与控件禁用.md`（2026-08-10）——不加的后果是**静默错位**，不报错不崩，这是它被记了四次都没做的原因
- [ ] **验收**：界面发起流程 → 在「我的发起」看到；经理+总监两级批完后该条**仍在列表里**且状态变「已办结」

**Day 17 · 前端：待办页**　⬜
- [ ] `views/task` 列表 + 办理弹窗 + `api/task.ts`
- [ ] **验收**：界面办理待办，流程流转到下一节点

**Day 18 · 前端：已审批 + 历史**　⬜
- [ ] `views/approved` + `views/history` + `api/history.ts`
- [ ] 轨迹列表前端过滤连线：`filter(a => a.activityType !== 'sequenceFlow')`（后端不过滤，见 P3 Day 14 决策2）
- [ ] **验收**：界面看到审批记录与完整流转轨迹。**P4 关闭**：登录→发起→审批→历史全程界面可走

---

## 二、打卡记录

| Day | 主题 | 端 | 实际完成度 | 受阻/备注 |
|-----|------|----|-----------|-----------|
| 15 | 流程管理页 | 前 | ✅ 100% | 2026-07-30 关闭。四个改动点全就位（`api/process.ts` + `views/process/index.vue` + `router` 子路由 + `layout` 菜单项），07-29 记的四笔问题（导出名不一致 / 漏 `onMounted` / 两处 URL 错配 / `loadList` rejection 逃逸）全部清零，`npm run build`（含 `vue-tsc -b`）通过。**浏览器实测：查询 / 挂起 / 激活 OK，删除带在途实例的部署被友好拦下（预期行为，非失败）**。⏳ 两笔竞态加固（el-switch `:loading`、分页器 `:disabled`）顺延到 Day 16 开工前 |
| 16 | 实例/我的发起页 | **双端** | 🔄 5.5/6 | `ProcessInstanceVO` 已改完提交（`5dabc3b`）。07-31 因 `toPage()` 泛型化读不动而暂停，转去写 `deep_dive/泛型/` 九篇。**08-03 解除暂停**：九篇已快速通读、自己写出等价的 `common/Result.java`（②档达成），判定继续停留收益低，**改走 `toHiPage` 复制版**（`08` 篇 §4 早已列为正当选项），当天后端第 2–4 项一并落地（`122418d`）。**08-04 前端开工**：`api/instance.ts` 完工（`68eb001`）、`views/instance/index.vue` 起了个头（`83b87c5`，写到 `<el-select` 断在半路）、`/instance` 路由菜单已挂。**08-05 早**后端把 `terminate` URL 改形对齐前端（`30a7314`）；**08-05 09:49 主动清空 `views/instance/index.vue`**（`b453106`）——不是回退，是学习方式换挡：照抄不长判断力，改为按《页面思考法》从零自写。**08-06 一天写完这一页**（`548f236`→`5ccf065`，178 行，三步法走完，08-07 `npm run build` 转绿），**本项目第一个本人从零规划的页面**。**08-07 起 myStart 页**：先补齐 `/my-start` 路由与菜单（`ada25b4`，第 6 项就此关闭），再写页面第 ① 步布局与 ② 步开头（`e15e2fa`，74 行，停在状态下拉空壳）。**断点：接着写 myStart 的 ② 组件**——放开 `statusOptions` + 绑 `v-model` + 表格 + 分页 + 发起弹窗 + `onMounted`，然后 ③ 样式、造样本实测 |
| 17 | 待办页 | 前 | | |
| 18 | 已审批/历史页 | 前 | | |

---

## 三、本阶段可用的数据库样本

> 🔄 **2026-07-30 已整库重置**（`resources/sql/00_reset_db.sql`）。**旧的 test-0004~0007 四条样本全部作废**，历史表清空，**当前一条实例都没有**。
> 详细现场见 `../测试进度断点.md` 第二节。

**做页面前需要自己造样本**：用 employee 账号发起若干条，凑齐下面四种，才能把状态列的所有分支都验到。

| 需要的状态 | 怎么造 | 验什么 |
|---|---|---|
| 在途，停 `managerApprove` | 发起后不办 | 「实例」页、「待办」页（manager 视角） |
| 在途，停 `adminApprove` | 发起报销/采购/出差 → manager 办掉一级 | **两级流转**，Day 17 待办页（admin 视角） |
| 已办结 | 上一条再让 admin 办掉 | 状态列「已办结」；且**仍应留在「我的发起」里**（改查 HI 的意义） |
| 已终止 | 调 terminate 接口，填 reason | 状态列「已终止」+ `deleteReason` 展示 |

> 三种结局的判据（`END_ACT_ID_` / `DELETE_REASON_` / 有无 `endEvent` 行）见 `../process-back/deep_dive/hi_tables/03` 第五节。

流程模板四个：`leaveProcess`（两级）、`reimburseProcess` / `purchaseProcess` / `travelProcess`（三级，多一个 admin 审批节点）。

账号：employee id=3（发起人）、manager id=2（一级审批）、admin id=1（二级审批）。密码均 `123456`。role 字面值与 BPMN 的 `candidateGroups` 严格对应。
