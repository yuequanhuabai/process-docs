# P4 · 业务前端批量（Day 15–18）

> 状态：**🔄 进行中** —— **Day 15、16、17 已关闭**（Day 17 于 2026-08-14 浏览器实测通过：两级流转跑通）；**下一个也是最后一个：Day 18 已审批 + 历史，做完 P4 关闭** ｜ 端：前端为主，Day 16、17 双端 ｜ 主题：流程管理 / 实例·我的发起 / 待办 / 已审批·历史 四个页面
> 返回 [总控进度](../05-重写排期计划.md) ｜ **当前位置与下一步以 [测试进度断点](../测试进度断点.md) 为准**

> 四个页面全部同构于 `views/user`（表格 + 分页 + 操作列 + 弹窗），复用 `PageResult<T>` 与 request 管道。
> ⚠️ 注意：流程域 id 全是字符串（TS 里别声明成 number）；删除传 deploymentId、挂起/激活传 definitionId，别接反。
> 指引文档：`../process-front/process_manage/` 四篇（Day 15）。学习方式是**你写、我答**，不代写。

---

## 一、任务清单

**Day 15 · 前端：流程管理页**　✅ **已关闭**（2026-07-30 浏览器实测通过）
- [x] `views/process/index.vue` 流程管理列表（表格/分页/挂起激活/删除二次确认）+ `api/process.ts`（page / deleteDeployment / suspend / activate；deploy 留给 Day 22 设计器接）
- [x] 路由挂 layout 子路由（`router/index.ts:29-33`）+ 侧栏菜单项（`layout/index.vue:25`）
- [x] 编译转绿 —— ① `activateDefinition` 命名两边一致（`api/process.ts:25`）；② `onMounted(loadList)` 已补（`index.vue:138`）；`npm run build`（`vue-tsc -b` + `vite build`）2026-07-30 实跑通过
- [x] URL 已对齐后端：`/{definitionId}/suspend`、`/{definitionId}/activate`（`ProcessDefinitionController.java:43,50`）。delete 路径与 `row.deploymentId` 传参本就是对的（`api/process.ts:29` 形参名仍叫 `definitionId`，命名误导不影响运行）
- [x] `loadList` 内部已补空 `catch`（`index.vue:80-82`），四处调用点的 rejection 不再逃逸——采纳的是方案 B，原理见 `../process-front/前端答疑/async-await与JS单线程.md` 第七、十节
- [x] ✅ **并发竞态已补**（2026-08-11，`3024e5e`，顺延四次后关闭）：`views/process/index.vue:7` 的 `<el-switch>` 挂上 `:loading="loading"`、`:43` 的 `<el-pagination>` 挂上 `:disabled="loading"`。`await` 治不了这个，见 `../process-front/前端答疑/请求竞态与控件禁用.md`（§5 实验 3 专证这一点）
- [x] **验收（浏览器实测）**：2026-07-30 前后端起起来，登录实测 —— **查询 / 挂起 / 激活三项 OK**；**删除带在途实例的部署被拦下并友好报错**（`cascade=false` 走 catch 分支，底层 `PROC_DEF_ID_` 外键冲突，与 Day 12 后端实测的同一条路径），**这正是验收标准要的行为**。**Day 15 关闭**

**Day 16 · 双端：实例 + 我的发起**　✅ **已关闭**（2026-08-11 晚造样本 + 浏览器实测通过）

> ✅ **决策已定（2026-07-30）：采纳方案①** —— 后端 `myStarted()` 改查 `HistoricProcessInstanceQuery.startedBy()`，在途+办结+终止一页看全，配状态筛选。
> 理由：① 产品上是主流做法（钉钉/飞书审批「我发起的」都是单页+状态筛选，用户心智是"我提交过的申请单"，不该暴露引擎 RU/HI 分表）；② 总代码量反而更少（拆两页要两个路由/两个组件/两个 api/**外加一个新后端接口**）；③ 那个带 `endTime`/`status` 的 VO 躲不掉，Day 17/18 还要接着用。
> **代价（已接受）**：Day 16 从纯前端变双端，工作量约 1.5 个块。

**落地文档（先后端后前端）**：
- 后端原理：[`../process-back/process_instance/07-Day16-查询改造为历史表.md`](../process-back/process_instance/07-Day16-查询改造为历史表.md)
- 后端落地：[`08-Day16-操作步骤.md`](../process-back/process_instance/08-Day16-操作步骤.md)（步骤+坑）+ **[`10-Day16-完整代码-无泛型版.md`](../process-back/process_instance/10-Day16-完整代码-无泛型版.md)（← 当前采用）**
  - [`09-Day16-完整代码.md`](../process-back/process_instance/09-Day16-完整代码.md)（泛型版）**暂不采用**，留作将来重构目标；换回去只动 4 处，清单在 `10` 第 5 节
- 前端：[`../process-front/process_instance/01-页面定位与修改点.md`](../process-front/process_instance/01-页面定位与修改点.md) + [`组件选型解析`](../process-front/process_instance/组件选型解析.md) + [`原型`](../process-front/process_instance/原型-实例与我的发起.html)

- [x] **后端**：`ProcessInstanceVO` 补 5 字段（`endTime`/`status`/`deleteReason`/`startUserName`/`currentActivityName`）+ 加 `of(HistoricProcessInstance)` 工厂方法（判据写在里面）—— ✅ 已提交 `5dabc3b`
- [x] **后端**：`myStarted()` 改查 HI；抽 `enrich()` 批量填姓名与当前节点（**别写成 N+1**），`page()` 复用但**继续查 RU** —— ✅ 已提交 `122418d`（`ProcessInstanceServiceImpl.java:105-108` 走 `startedBy()`；`enrich()` 在 `:194`，整页固定 2 次查询）
- [x] **后端**：~~`toPage()` 泛型化~~ → **改为新增 `toHiPage(HistoricProcessInstanceQuery, long, long)`**（与 `toPage` 逐行同构，只有 Query 类型和元素类型不同，末尾同样调 `enrich()`）。**零泛型声明**，`page()` 的 return 行真·零改动。原因见 `10` 文档开头：两个 Query 接口无继承关系，想合并只剩泛型一条路，而泛型不值得卡住主线 —— ✅ 同上（`toPage:131` / `toHiPage:180`，各 5 行）
- [x] **后端超出清单顺手做掉的两笔**（同在 `122418d`）：`applyStatusFilter()`（`:146-161`，if/else 三态过滤，未知值抛 `BizException`）；Controller `my-started` 加 `status` 入参（`ProcessInstanceController.java:38-46`）
- [x] **契约改形**：`terminate` 从 `DELETE /process-instance/{instanceId}` 改成 `{instanceId}/terminate`，去对齐前端 `api/instance.ts:55` —— ✅ 已提交 `30a7314`（2026-08-05）。⚠️ **后端需重启一次**才生效
- [x] **后端验收** ✅（2026-08-11，经前端实测覆盖，不必再单独打 knife4j）：「我的发起」页状态下拉走的就是 `my-started?status=` 同一条接口，浏览器逐态切换各自只剩对应行 ⇒ `applyStatusFilter()` 三态正确；`startUserName` 显示姓名不是 userId、`currentActivityName` 有值 ⇒ `enrich()` 正确
- [x] **前端**：`views/instance` + `views/myStart` + `api/instance.ts` + 路由 2 条 + 菜单 2 项 —— ✅ **全部完工（2026-08-11）**
  - [x] `api/instance.ts` 完工（`68eb001`）：59 行 / 4 函数（`pageInstances` / `myStarted` / `startInstance` / `terminateInstance`）/ 3 类型（`InstanceStatus` / `ProcessInstance` / `StartInstanceBody`），与 Controller 四个签名逐个核对无错配
  - [x] ✅ `views/instance/index.vue` **已完工**（177 行，08-06 一天写完，`548f236`→`5ccf065` 六个提交，**本人从零写、CLI 只答疑不代写**）：按 `页面思考法` ① 布局 → ② 组件 → ③ 样式三步走完；`npm run build`（含 `vue-tsc -b`）**08-07 实跑转绿**。含分页查询 / 流程下拉过滤（复用 `pageDefinitions`）/ `ElMessageBox.prompt` 终止（`inputValidator` 非空 + 删末页回退）/ `onMounted` 双加载；两处控件都挂了 `:disabled="loading"`（竞态本页已自愈）；按 `02-填充组件.md` 的决定砍掉「实例 ID」列。**这一版的 4 div 结构（`.instance-page` > `.toolbar`/`.table-wrap`/`.pager-wrap`）是 P4 后面三页的模板**
  - [x] ✅ `views/myStart/index.vue` **已完工**（292 行，08-07~08-11，`e15e2fa`→`3eaf0d7` 十一个提交，`npm run build` 转绿）—— **三步法第二次完整走通，仍是本人写、CLI 只答疑与验收**。① 布局（照实例页 4 div 范本）→ ② 组件（状态下拉 / 7 列表格 + `STATUS_MAP` / 分页 / 撤回 / **发起弹窗** `:74-109`）→ ③ 样式（`.title` / `.toolbar-right` `flex+gap:10px` / `.muted` / `.pager` 四条全接上）。竞态两处都挂了 `:disabled="loading"`。**弹窗段的六个决策与两次样式静默失效见 `../测试进度断点.md` 的 🔻 段与 `../变更日志.md` 08-10、08-11 两节**<br>操作列的 `label`/`width`/`fixed`（`:45`）已随 08-11 下午收尾提交入库（`575c68c`→`e96c1e1` 七笔：弹窗细节 + css 调整），工作区已干净
  - [x] 路由 `/instance`（`router/index.ts:34-38`）+ 菜单「运行中的实例」（`layout/index.vue:28`）
  - [x] 路由 `/my-start`（`router/index.ts:39-43`）+ 菜单「我的发起」（`layout/index.vue:29`）—— `ada25b4`，URL 短横线 / 目录小驼峰 / 子路由不带前导斜杠三条约定均写对
- [x] ✅ **前端：发起弹窗已完工**（`views/myStart/index.vue:74-109`，`0d83a61`→`369e46c`）—— **本项目第一次向后端提交表单**。`el-dialog` + `el-form` + `rules` + `handleSubmit`。六个决策：`:value="d.key"` 不是 id ｜ `FormRules<typeof form>` 带泛型才查得出字段名拼错 ｜ `.trim() || undefined` 让空值在 `JSON.stringify` 时消失 ｜ 成功后 `status.value=''` 否则筛选停在「已办结」时**新实例不出现** ｜ `:show-close="!submitting"`（前两个 close 属性管不到右上角 X）｜ `loadDefs` 的 catch 自己弹 warning。~~终止用 `ElMessageBox.prompt`~~ ✅ 已在实例页落地
- [x] ✅ **Day 15 顺延来的两笔已清**（2026-08-11）：竞态 `3024e5e`、命名 `ab4dd5d`（`deleteDeployment` 形参改 `deploymentId`）。竞态那条前后记了四次才做掉，原因是**说不出后果的待办排不进优先级**，直到 `请求竞态与控件禁用.md` 把"静默错位"讲透
- [x] **验收（浏览器实测）** ✅（2026-08-11 晚，四条全过）：① 界面发起 → 立刻在列表可见（`status.value=''` 重置生效）；② 进行中/已办结/已终止三态 tag + 当前节点渲染正确；③ **办结/终止的实例仍留在列表里**——改查 HI 的全部意义达成；④ `startUserName` 姓名、`currentActivityName` 有值。期间撞上一次「流程定义已挂起，无法发起」：`leaveProcess` 的 `SUSPENSION_STATE_=2` 是 Day 15 挂起/激活实测的遗留态，swagger 激活后通过（排查链路 + businessKey 不查重结论见 `../变更日志.md` 08-11 最新节）。**Day 16 关闭**

**Day 17 · 待办页**　✅ **已关闭**（2026-08-14 浏览器实测通过）（2026-08-11 需求定稿于 [`../process-front/process_task/00-需求文档.md`](../process-front/process_task/00-需求文档.md)，三决策已定：① 后端补三字段 ② 单按钮办理 ③ 意见选填。**决策①使本 Day 从纯前端变双端**）
- [x] ✅ **后端小改（决策①）已关闭**（2026-08-12 上午，knife4j 双接口实测）：`TaskVO` 加 `processDefinitionName` / `businessKey` / `startUserName` 三字段（`TaskFormVO extends TaskVO` 自动继承，详情接口零改动）；`FlowableTaskServiceImpl` 注入 `RuntimeService` + `SystemUserMapper`，加 `enrich()`（整页固定 **2 次**查询，未写成 N+1）并在 `toPage()` / `detail()` **两处**挂载。`FlowableTaskService` 接口、`TaskController`、`CompleteTaskRequest` 零改动。提交 `b25c356`→`e141dab`
  - [x] **实测**：`GET /task/todo`（manager）3 条待办三字段全有值（「请假流程」「采购流程」/「李員工」/ businessKey）；`GET /task/{taskId}` 三字段 + `variables` 齐
  - ⚠️ **中途两处静默失效**（编译全绿、字段却恒 null，两次都靠人工核对抓出）：① `detail()` 末尾 `return TaskFormVO.of(...)` 又造了个新对象，把 `enrich()` 填好的 `vo` 丢了 → 应 `return vo;`；② `toPage()` 漏挂 `enrich()` → 列表整页三字段全空。**自查手段：全项目搜 `enrich(` 应为 3 行 = 1 处定义 + 2 处调用**（`09` 文档验收清单第三条早就写了，当时没照做）
  - [x] 🧹 遗留已清：`FlowableTaskServiceImpl.java` 那行无用 `import ...Autowired` 已删（`27fa849`，2026-08-12 15:20）
- [x] ✅ **`views/task` 列表 + 办理弹窗 + `api/task.ts` 已完工**（2026-08-12~08-14，`c081d70`→`901aaf8` 八个提交，**三步法第三次走通，本人从零写**）：`api/task.ts` 43 行（3 函数 3 类型）、`views/task/index.vue` 187 行（表格 5 列 + 分页 + 办理弹窗）、路由 `router/index.ts:45-47` + 菜单 `layout/index.vue:28`。`npm run build`（含 `vue-tsc -b`）**08-14 实跑转绿**。断点文档那六条约束逐条对照都写对（`variables` 用 `Object.entries` 不假设键 / 不放 `assignee` 列 / `POST /task/complete` body 形式 / 弹窗三个 close 属性照 myStart / 分页 `:disabled` / 删末页回退）。**逐条对照表与两笔新登记的小尾巴见 [`../变更日志.md`](../变更日志.md) 08-13~14 节**
  - [x] ✅ **对账查出的两笔当天就了结**（08-14）：① 「办理」按钮连点已修 —— 加 `openingId` 存行 id，被点那行 `:loading`、其余行 `:disabled`，函数首行 `if (openingId.value !== null) return` 兜底，build 转绿。源码验证 `use-button.mjs:31-32,50`：`loading` 本身就写进原生 `disabled` 且拦 `handleClick`，`:disabled` 那半句是给同页其他行用的；② div 类名不统一 **✅ 结案：不统一**，本页 `.top`/`.middle`/`.bottom` 保留，以后新页面也不必对齐任何一页命名（理由见变更日志 08-13~14 节）
- [x] ⚠️ **挂起项已登记（决策②）**：「真拒绝」需改 BPMN 加排他网关，本页只做单按钮「提交办理」；展开见 [P8 第三节第 6 条](./P8-联调收尾.md#6-审批拒绝无真实分支bpmn-无排他网关)
- [x] ✅ **验收（浏览器实测）**（2026-08-14）：manager 登录看到待办 → 界面点「办理」办掉**三级流程的一级** → **切 admin 登录，`adminApprove` 的待办确实出现** → admin 点同意办掉。⇒ **界面办理 ✅ + 两级流转 ✅**，同时反证了 `taskCandidateGroup(UserContext.role)` 的查询方向、BPMN `candidateGroups` 与 `sys_user.role` 的字面值对应、以及 Day 17 后端补的三字段在真实页面上渲染正常。**Day 17 关闭**

**Day 18 · 已审批 + 历史轨迹**　🔄 **需求与原型已定稿（2026-08-14），代码未开工** ｜ ⚠️ **决策②使本 Day 从纯前端变双端**

> **需求基线**：[`../process-front/process_history/00-需求文档.md`](../process-front/process_history/00-需求文档.md)（✅ 已确认）＋ [`原型-已审批与轨迹.html`](../process-front/process_history/原型-已审批与轨迹.html)
> **后端落地三篇**（2026-08-14 起草）：[`08-Day18-已审批补字段与轨迹姓名.md`](../process-back/process_history/08-Day18-已审批补字段与轨迹姓名.md)（原理/改动清单）→ [`09-Day18-操作步骤.md`](../process-back/process_history/09-Day18-操作步骤.md)（自己写，含 Step 0 的 SQL 实测）→ [`10-Day18-完整代码.md`](../process-back/process_history/10-Day18-完整代码.md)（参考答案）
> **四个决策**：① **A 弹窗**承载轨迹（⇒ 原清单里的 `views/history` **不作为页面存在**，是个弹窗组件，不新增路由与菜单）② **B 补后端**三字段 ③ **A `el-timeline`** ④ 已审批页也放轨迹入口

- [ ] **后端小改（决策②）**：`ApprovedTaskVO` 加 `processDefinitionName`/`businessKey`/`startUserName`，`ActivityTraceVO` **新增 `assigneeName`（保留原 `assignee`，方案 A）**；`approved()` 挂 `enrich()`、`activities()` 挂 `enrichAssigneeName()`。⚠️ **不能照抄 Day 17 的 `enrich()`** —— 已审批对应的实例大概率已办结，`RuntimeService` 查不到，必须走 `historyService.createHistoricProcessInstanceQuery().processInstanceIds(ids)`，照抄会得到整页 null（第三次静默失效）。写完照旧数 `enrich(` 的调用点
- [ ] `api/history.ts` + `views/approved` + 轨迹弹窗组件（⚠️ 轨迹接口**返回数组不是分页**，别套 `PageResult`）
- [ ] 轨迹列表前端过滤连线：`filter(a => a.activityType !== 'sequenceFlow')`（后端不过滤，见 P3 Day 14 决策2）
- [ ] 🔗 **回头改 Day 16 的 myStart 页**（2026-08-10 登记，别只顾着写新页面就忘了）：操作列加「**轨迹**」按钮 —— 原型第 ③ 节是**每一行都有轨迹**、撤回只给进行中的行。接上后**把 `<span v-else class="muted">—</span>` 那个占位破折号删掉**（它当初就是为了填补已结束行的空操作格，轨迹一来就不需要了）。后端接口 Day 14 已就绪：`GET /history/instance/{instanceId}/activities`。来源与理由见 `../process-front/process_instance/01-页面定位与修改点.md` 第三节末尾
- [ ] ⚠️ **挂起项已登记（需求 4.4）**：**审批意见无法按节点还原**——`complete()` 写实例级变量导致两级审批的 `comment` 互相覆盖，`history-level: audit` 又不写 `ACT_HI_DETAIL`。故轨迹只展示「节点 + 办理人 + 时间 + 耗时」，不展示意见。展开见 [P8 第三节第 7 条](./P8-联调收尾.md#7-审批意见comment无法按节点还原)，总表第 7 条。**开工前先跑一条 SQL 实测确认**（语句在需求文档 4.4）
- [ ] **验收**（七条，见需求文档第六节）：已审批列表字段与耗时可读、employee 空态不报错、轨迹**过滤掉 sequenceFlow**、轨迹三态（进行中/已办结/已终止）各看一条、myStart 破折号已删、build 转绿。**P4 关闭**：登录→发起→审批→历史全程界面可走

---

## 二、打卡记录

| Day | 主题 | 端 | 实际完成度 | 受阻/备注 |
|-----|------|----|-----------|-----------|
| 15 | 流程管理页 | 前 | ✅ 100% | 2026-07-30 关闭。四个改动点全就位（`api/process.ts` + `views/process/index.vue` + `router` 子路由 + `layout` 菜单项），07-29 记的四笔问题（导出名不一致 / 漏 `onMounted` / 两处 URL 错配 / `loadList` rejection 逃逸）全部清零，`npm run build`（含 `vue-tsc -b`）通过。**浏览器实测：查询 / 挂起 / 激活 OK，删除带在途实例的部署被友好拦下（预期行为，非失败）**。⏳ 两笔竞态加固（el-switch `:loading`、分页器 `:disabled`）顺延到 Day 16 开工前 |
| 16 | 实例/我的发起页 | **双端** | ✅ 100% | 2026-08-11 关闭。`ProcessInstanceVO` 已改完提交（`5dabc3b`）。07-31 因 `toPage()` 泛型化读不动而暂停，转去写 `deep_dive/泛型/` 九篇。**08-03 解除暂停**：九篇已快速通读、自己写出等价的 `common/Result.java`（②档达成），判定继续停留收益低，**改走 `toHiPage` 复制版**（`08` 篇 §4 早已列为正当选项），当天后端第 2–4 项一并落地（`122418d`）。**08-04 前端开工**：`api/instance.ts` 完工（`68eb001`）、`views/instance/index.vue` 起了个头（`83b87c5`，写到 `<el-select` 断在半路）、`/instance` 路由菜单已挂。**08-05 早**后端把 `terminate` URL 改形对齐前端（`30a7314`）；**08-05 09:49 主动清空 `views/instance/index.vue`**（`b453106`）——不是回退，是学习方式换挡：照抄不长判断力，改为按《页面思考法》从零自写。**08-06 一天写完这一页**（`548f236`→`5ccf065`，177 行，三步法走完，08-07 `npm run build` 转绿），**本项目第一个本人从零规划的页面**。**08-07 起 myStart 页**：先补齐 `/my-start` 路由与菜单（`ada25b4`，第 6 项就此关闭），再写页面第 ① 步布局与 ② 步开头（`e15e2fa`，74 行）。**08-08~08-10 写到 177 行、`npm run build` 转绿**（`4cf53f2`→`e39a798`）：表格 7 列 + 分页 + 撤回 + `onMounted` 全就位，途中踩了五个坑（多余点号 / `onMounted` 被关进函数体 / `defs` 漏 `.value` 被空 catch 吞 / `inputValidator` 漏 `return` / `TS7053`），**都已讲透并修掉，方法沉淀见 `../变更日志.md` 08-10 节**。**08-11 一天把这页收工**（`0d83a61`→`3eaf0d7`，292 行）：发起弹窗 → 三处体验加固 → 两个顺延欠账 → ③ 样式。**③ 样式踩了同一个坑两次**（`.muted` 先是没定义、修好后又嵌错在 `.toolbar` 里选不中），两次都是**静默失效**——不报错不警告，只能靠看编译产物 `dist/assets/*.css` 里的选择器确认。同日答疑产出两篇（`Teleport与弹窗渲染位置.md` 成篇 + `ref` vs `reactive`、`FormRules` 两段未成篇），并把「源码交叉验证」立成 `讲解规则.md` 第五节。**08-11 晚造样本 + 浏览器实测四条全过，Day 16 关闭**——期间被 `leaveProcess` 挂起遗留态拦了一次发起，激活后通过；操作列改动与弹窗/样式收尾随 `575c68c`→`e96c1e1` 七笔入库，工作区干净（详见变更日志 08-11 最新节） |
| 17 | 待办页 | **双端** | ✅ 100% | 2026-08-14 关闭。2026-08-11 需求定稿（三决策：后端补三字段 / 单按钮办理 / 意见选填，决策① 使本 Day 从纯前端变双端），前后端四篇落地文档 + 原型齐备。**08-12 上午后端小改一次性写完并 knife4j 实测关闭**（`b25c356`→`e141dab`）：`TaskVO` 三字段 + `enrich()` 两处挂载，整页固定 2 次查询。**中途两处静默失效**——`detail()` 返回了未 enrich 的新对象、`toPage()` 漏挂 `enrich()`，编译全绿但三字段恒 null，靠「搜 `enrich(` 数调用点」抓出（`09` 文档验收清单本就写了这一条，当时跳过了）。教训与 08-11 的 `.muted` 样式静默失效同类，已记入变更日志 08-12 节。**08-12~08-14 前端三天写完**（`c081d70`→`901aaf8` 八笔）：08-12 ① 布局（路由 + 菜单 + 骨架）→ 08-13 `api/task.ts` + 表格分页主体（顺手把大写 `views/Task/` 目录改成小写 `task/`，本地大小写不敏感看不出，Linux/CI 上会构建失败）→ 08-14 办理弹窗。187 行，`npm run build` 转绿。**08-14 下午浏览器实测关闭**：manager 办掉三级流程一级 → 切 admin 待办里出现 `adminApprove` → admin 同意办掉，两级流转跑通。同日对账查出的两笔当天了结：「办理」按钮连点已修（`2503640`，加 `openingId` 存行 id，被点那行 `:loading`、其余行 `:disabled`；源码 `use-button.mjs:31-32,50` 证实 `loading` 本身就拦点击）；div 类名不统一**结案为不统一**，本人风格优先，以后新页面不必对齐任何一页命名 |
| 18 | 已审批/历史页 | 前 | | |

---

## 三、本阶段可用的数据库样本

> 🔄 2026-07-30 整库重置（旧 test-0004~0007 作废）后，**2026-08-11 已重新造样本**：进行中 / 已办结 / 已终止三态俱全（含 08-03 停在 `managerApprove` 的 leave、purchase 两条在途）。现场以库为准，另见 `../测试进度断点.md` 第二节。

**样本已基本造齐**，下表留作补造参考——Day 17 验 admin 视角待办时，需确保有一条三级流程停在 `adminApprove`：

| 需要的状态 | 怎么造 | 验什么 |
|---|---|---|
| 在途，停 `managerApprove` | 发起后不办 | 「实例」页、「待办」页（manager 视角） |
| 在途，停 `adminApprove` | 发起报销/采购/出差 → manager 办掉一级 | **两级流转**，Day 17 待办页（admin 视角） |
| 已办结 | 上一条再让 admin 办掉 | 状态列「已办结」；且**仍应留在「我的发起」里**（改查 HI 的意义） |
| 已终止 | 调 terminate 接口，填 reason | 状态列「已终止」+ `deleteReason` 展示 |

> 三种结局的判据（`END_ACT_ID_` / `DELETE_REASON_` / 有无 `endEvent` 行）见 `../process-back/deep_dive/hi_tables/03` 第五节。

流程模板四个：`leaveProcess`（两级）、`reimburseProcess` / `purchaseProcess` / `travelProcess`（三级，多一个 admin 审批节点）。

账号：employee id=3（发起人）、manager id=2（一级审批）、admin id=1（二级审批）。密码均 `123456`。role 字面值与 BPMN 的 `candidateGroups` 严格对应。
