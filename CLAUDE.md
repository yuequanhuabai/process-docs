# CLAUDE.md · 本仓库对 CLI 的硬约束

> 本文件随仓库提交，**跨机器生效**（CLI 的本地记忆是单机的，不能用来放这类规则）。

## 指令码（触发语 → 固定动作）

完整定义见 [`指令码.md`](./指令码.md)。**命中即执行，不要反问确认。**

### 指令码 #1 · 进度定位

**触发语**（语义匹配，不要求逐字）：

> 基于当前目录下的 process-docs 下的「05-重写排期计划.md」以及「测试进度断点.md」文档以及对应的实际前后端代码，定位一下当前项目的进度；

**动作，顺序固定：**

1. **先拉三个仓库**：`process-back`(dev) / `process-docs`(master) / `process-front`(dev) 各跑一次 `git pull`。拉前看分支与 `git status`；**工作区脏就停下来先报告**，禁止 `stash`/`reset`/强拉覆盖；只 pull，不 push 不 commit。
2. **再定位**：读 `05-重写排期计划.md` + `测试进度断点.md`（必要时加当前阶段 `plan/P*.md`），**并对照前后端实际代码交叉核对**。
3. **输出三件事**：当前阶段/Day 位置；文档与代码是否一致（不一致要点名）；下一步该做什么。

---

## 其他常驻约束

- **进度以文档为准**：一般性地问进度，直接读 `测试进度断点.md`，不必扫代码核对；**但指令码 #1 明确要求交叉核对，那里以指令码为准**。
- **讲解风格**：见 [`讲解规则.md`](./讲解规则.md)——纵向剥层 + 横向讲史 + 动手实验；第五节「查证优先于回忆」要求依赖库行为翻 `node_modules` 源码验证，不凭记忆作答。
- **前端页面不代写**：学习方式是「你写、我答」。写新页面前先读 [`process-front/页面思考法/00-总纲.md`](./process-front/页面思考法/00-总纲.md)，按 ① 布局 → ② 组件 → ③ 样式 推进。
- **文档写哪**：新产出/新决策 → `变更日志.md`；知识沉淀 → `deep_dive/` 或 `前端答疑/`；每日打卡 → `plan/P*.md`。`测试进度断点.md` 刻意压着不让它膨胀。
- **git**：除非用户明确要求，不主动 `commit` / `push`。
